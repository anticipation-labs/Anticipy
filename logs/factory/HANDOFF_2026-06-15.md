# ANTICIPY — MASTER HANDOFF (2026-06-15, ~05:30 UTC)

**READ THIS FIRST, COMPLETELY.** If you are a fresh/compacted agent picking up Anticipy, this
document makes you *become the last agent, instantly, and better*. It is dated and stamped on
purpose. On any conflict with stale notes, this wins. Then read `CLAUDE.md`, `CONSTITUTION.md`,
`HANDOUT_2026-06-13.md`, and the live state below. Verify everything against the running system —
never trust memory of it (that mistake has bitten repeatedly; see "Failure patterns").

---

## 0) WHO OMAR IS — and how to work with him (the instilled principles)
- He is the architect/founder. You are the builder with **full access to his Mac** (same as him).
  He has explicitly said: *you have the same level of access I do — use it.* Do not punt work back
  to him that you can do yourself.
- **The ONE thing he hates most: stopping short / refusing the task / "deciding not to do it."**
  "Refusing to do the task is not a solution." When you hit a wall, find the way through; only
  surface to him what is *genuinely* human-only (his Apple password, his $99 payment, accepting
  legal agreements, picking a file in a native macOS dialog — these are real boundaries).
- **The SECOND thing he hates: over-claiming.** He has been burned by "it's converged/airtight/done"
  that was false. Be brutally honest. Verify-then-claim. A false green light in front of investors
  is the one unforgivable mistake. He respects honesty over polish, always.
- He thinks in **forcing systems**, not plans. He wants research-first ("60–100 web searches a
  turn" was a real ask), parallel agents, "plug in the proven thing instead of reinventing it."
- He communicates by voice → messages are sometimes garbled. Infer intent generously.
- ELI5 summaries land well. Date/stamp decisions. He values continuity across compaction obsessively
  (this doc exists because of that).
- Money is the ONLY hard action stop. **Acting on a vent/sarcasm is the cardinal sin.** These two
  are sacred and verified to hold (0 breaches across 152 adversarial lines).

## 1) THE MISSION + HIS DEFINITION OF DONE (never redefine smaller)
Anticipy = an always-listening assistant ("Donna from Suits"). Hears a person's messy day, infers
the unspoken tasks (sarcasm/vents are NOT tasks), remembers everything, decides act/ask/silent, and
autonomously executes ~75% of their work via: a **browser arm** (in their own Chrome) + a
**per-person API arm** (Arcade Calendar/Gmail etc.) + a **voice/text loop** (Twilio: "calendar event
made; I'll call you at 2:45"). Money/payment is the only hard stop.

**Omar's literal DONE:** Go to **Anticipy.ai → Download button → run "Anticipy Execute" →
onboarding scrapes your logged-in Chrome to build a per-person mesh → the whole flow works →
PROVEN over 5 consecutive real Omar days** (zero vent-actions, ≤3 unnecessary asks/day). That is the
finish line (the P5 Owner Test). Strangers/iPhone/pendant are the *next* plan.

## 2) THE NON-NEGOTIABLE LAWS (carry these forever)
1. **Honesty / no-slop.** Never claim a success you didn't independently verify (Slice-0 rule: the
   actor's own "I did it" is NOT proof — read it back). "SUITE GREEN" only after a clean run.
2. **Cardinal sin:** never act on / persist a vent. Verified by `engine/scripts/safety_mega_eval.py`
   (152-line adversarial corpus, bar = 0 breaches). Run it before claiming the floor is solid.
3. **Money is the hard stop:** never auto-spend; the browser arm never clicks a final-pay control;
   the API arm has no money-moving capability.
4. **Verify, don't assume.** Check the running system / the actual file / the actual keys before
   asserting. This is the recurring failure (see §7).
5. **No commits while `factory/.lock` exists** (a nightly lap is running; its revert would destroy
   your commit). Check `ls factory/.lock` first. (No lock right now.)
6. **Never enter his credentials / pay / accept legal terms / create accounts.** He does those.

## 3) CURRENT LIVE STATE (verified 2026-06-15 ~05:30 UTC) — DONE vs NOT
**✅ DONE & verified live:**
- Vercel site **live**: `https://anticipy-executor-working.vercel.app` (this repo, auto-deploys on
  push to `main`). Download route `/api/download/anticipy-execute` → 200, serves `Anticipy.app.zip`.
- macOS app **"Anticipy Execute" is REAL + SIGNED**: opening it runs `boot.sh` → boots engine(:8787)
  + production web UI(:3000) → opens the working owner interface. Signed with Developer ID
  Application: Omar Ebrahim (Team **49T86P9XGW**). Build: `bash macapp/scripts/build_app.sh` then
  re-sign (see §6).
- **Inference (the moat): bulletproof.** Suite **76/76 green** (`bash scripts/run_suite.sh`),
  mega-eval **0 breaches**, demo_day **0 false actions**. Catches indirect tasks, blocks money,
  silences vents — verified end-to-end via `/owner/ingest`.
- **Voice/text LIVE & verified:** real SMS **delivered** (SID SM59e0…) and real call **completed**
  (SID CA38e2…) through the engine's own TextChannel/CallChannel. Twilio from=+16196584447,
  owner=+16047245161, balance ~$17.6, number does SMS+voice.
- **Browser READ arm:** verified (HN, Wikipedia, independently checked).
- **Browser ACTION arm = the PROVEN agent (browser-use), `/agent/act`:** verified add-to-cart
  ("Pure Cotton V-Neck T-Shirt" on automationexercise, Pikachu on scrapeme). vision on,
  money/checkout/login are HARD STOPS in `browser_use_runner.py` `_ACTION_GUARD`.
- **API Calendar:** Arcade authorize=completed (live-connected). ARCADE_API_KEY + ARCADE_USER_ID set.
- Apple Developer ID cert created (you drove developer.apple.com via Claude-in-Chrome) + key/cert
  imported into the login keychain. `macapp/scripts/sign_and_notarize.sh` is ready.

**❌ NOT done (the real remaining work, prioritized):**
1. **Onboarding that scrapes the logged-in Chrome → per-person API mesh.** *Not built.* Only
   `onboarding/profile_builder.py` (public-URL bio) + `clarify.py` exist. This is the defining
   "custom to every person" piece and the biggest remaining build. The now-working browser arm is
   the tool to build it with.
2. **The 5-day Owner Test (P5):** 0 of 5. His literal finish line.
3. **Notarization:** one human-only step from Omar — `xcrun notarytool store-credentials
   anticipy-notary --apple-id "omarkebrahim@gmail.com" --team-id 49T86P9XGW` (he enters an
   app-specific password from appleid.apple.com). Then run `sign_and_notarize.sh`. Until then the
   download is signed-but-not-notarized (right-click→Open for strangers).
4. **Wire `/agent/act` into the card-execution flow** (it's a standalone endpoint; route caught
   browse_task actions to it) AND **attach browser-use to the user's logged-in Chrome via CDP**
   (today it uses its own fresh browser → no logins → fine for non-login add-to-cart, not for "your
   Amazon cart").

## 4) HOW TO RESUME RIGHT NOW (the live stack)
- **Engine:** `ANTICIPY_DATA_DIR=/tmp/anticipy_demo_data ANTICIPY_HANDS_MODE=mock
  ANTICIPY_CHANNELS_MODE=live PYTHONPATH=engine engine/.venv/bin/python -m uvicorn --app-dir engine
  anticipy_engine.main:app --port 8787` — **launch via the Bash tool with `run_in_background:true`**
  (inline `nohup … &` gets reaped when the tool call ends — that's why the engine kept dying).
- **Web (DEMO = production, stable):** `ANTICIPY_ENGINE_URL=http://127.0.0.1:8787 npm run build` once,
  then `npm start` (NOT `npm run dev` — dev's `.next` cache keeps corrupting → 404s; if it 404s,
  `rm -rf .next && npm run build && npm start`).
- **Extension** (the user's Chrome arm): loaded from `~/Anticipy/extension`; auto-reconnects to
  `/ws/extension`. Check `curl -s http://127.0.0.1:8787/ws/state` → `{"connected":true}`.
- **`.env.local`** holds all keys (gitignored, never commit). **It MUST stay
  `ANTICIPY_CHANNELS_MODE=mock`** — live is set per-launch via the explicit flag. A live `.env.local`
  leaks into the suite and makes CI place REAL Twilio calls (this exact bug happened; `run_suite.sh`
  now also force-exports mock as defense).
- Repo: `~/Anticipy` (Desktop/Anticipy-executor-working is a symlink). Branch `factory/build`.
  HEAD=430f408. **origin/main=d429ee2 → the last 3 commits (browser-use arm, voice-live, demo-harden)
  are NOT pushed.** Push with `git push origin factory/build:main` to deploy them (Omar authorized
  "push it all online").

## 5) WHAT GOT DONE TONIGHT (2026-06-14 → 15), dated, with SHAs
- 25d0eb9 09:13 — Hardened the 4 cores: 6 audit-confirmed holes fixed (casual-send safety, SSRF
  nav-wall, allowed-domains co.uk, blocking DNS, corrupt-state crash) after a 41-agent grounded audit.
- 7f56169 09:17 — Owner gate fails CLOSED on public deploys (was open with no token).
- 0bc9880 09:37 — API arm never re-sends a fired-but-unconfirmed write (no duplicate sends).
- 6039e3f 10:08 — Tested the browser arm THROUGH the loop: in-loop money stop + SSRF/domain regressions.
- 351da0e 10:10 — One interface: global nav (Owner/Connect/Download).
- 3021fa1 10:43 — Action-arm: auto-handle JS dialogs + general-loop fallthrough on no-search stores.
- d83708d 19:44 — Action-arm: fall through on listing/non-product surfaces.
- 51b518f 21:03 — VERIFIED a real add-to-cart by independent cart read-back.
- d583569 21:23 — "Anticipy Execute" made REAL (boots stack + shows UI; was an inert scaffold).
- d429ee2 21:26 — One-command sign+notarize script.
- 15b4831 21:58 — Demo-harden: app boots PRODUCTION web server + signed with Developer ID.
- dc39eb1 22:03 — Voice line LIVE (real SMS delivered + call completed; app boots channels=live).
- 430f408 22:19 — Plugged in the PROVEN browser agent for actions (browser-use `/agent/act`).
- (uncommitted earlier) Apple Developer ID cert created via the browser + imported + app signed.

## 6) KEY COMMANDS / RECIPES
- Suite: `bash scripts/run_suite.sh` (76 tests; forces stub/mock/mock-channels).
- Safety floor: `PYTHONPATH=engine engine/.venv/bin/python engine/scripts/safety_mega_eval.py`.
- Sign the app: `codesign --force --deep --options runtime --timestamp --sign "Developer ID
  Application: Omar Ebrahim (49T86P9XGW)" macapp/dist/Anticipy.app` (after `build_app.sh`).
- Notarize (after Omar makes the notary profile): `bash macapp/scripts/sign_and_notarize.sh`.
- Browser action arm: `POST /agent/act {task, start_url, max_steps}` (proven agent). Read arm:
  `/agent/run` (the bespoke WebVoyager loop, drives the user's real Chrome via extension — flakier).

## 7) FAILURE PATTERNS & HARD-WON GOTCHAS (do not repeat these)
- **Assume-instead-of-verify** is THE recurring sin. Examples this run: claimed ARCADE_API_KEY empty
  (it was set; the grep matched the var name); declared the proactive floor "converged" without
  running the corpus (it then found 10 real breaches incl. a sarcasm→ACT). ALWAYS run/check first.
- **The browser ACTION arm on arbitrary sites is genuinely NOT solved** by anyone (browser-use,
  computer-use, Manus all fail on some sites: downed sites, captchas, alerts, login walls). The win
  is using the proven agent (browser-use), not the homegrown loop — but it's still not 100%. Tell
  Omar the truth: demo it on a rail; don't let investors freestyle arbitrary store tasks.
- **Inline `nohup … &` from the Bash tool gets reaped** when the call ends → the engine "mysteriously"
  dies. Use `run_in_background:true`.
- **`next dev` corrupts `.next`** → 404s. Use production (`npm run build` + `npm start`) for anything
  that must stay up (the demo).
- **macOS file pickers can't be automated** (security). For a CSR/file upload, put the file on the
  Desktop with an obvious name and have Omar pick it. `chrome://` pages aren't navigable by the
  browser tool; resize a 0-sized window before screenshots error.
- **A live `.env.local` leaks into CI** and makes real Twilio calls. Keep `.env.local` mock.
- Don't over-build the persona bank or onboarding *unilaterally* without Omar's steer on what "done"
  means there — but DO build; he wants forward motion, not more audits.

## 8) ARCHITECTURE MAP (where things live)
- Brain: `proactive/{triage,decider,harm}.py` (triage=deterministic regex → decider=cheap model,
  fail-silent, one-way to ask/silent → harm-line=deterministic FINAL, money/cardinal-sin).
  `live_memory/{review_infer,capture,press_go}.py` (is_vent single source of truth; press-go
  default-deny WHITELIST={create_event,write_memory}).
- Hands: `hands/api_hand.py` (+ token_vault), `hands/browser_hand.py` (extension transport),
  `agent/webvoyager.py` (bespoke loop), `hands/browser_use_{link,runner}.py` (the PROVEN arm; runner
  is the 3.11 `.bu-venv` bridge; `act=true` → action mode).
- Channels: `channels/{text,call,inbound,conversation_relay}.py` (Twilio; live via
  ANTICIPY_CHANNELS_MODE=live).
- Endpoints: `core/main.py` (`/owner/ingest`, `/memory/remembered/approve`, `/agent/run`,
  `/agent/act`, `/ws/*` for the extension). UI: `app/` (Next.js owner mode). App: `macapp/`.
- Models: OpenRouter. cheap=google/gemini-2.5-flash-lite, smart=google/gemini-2.5-flash.

## 9) MODELS / IDENTITY
You are powered by Claude Opus 4.8 (1M context). Latest models: Fable 5, Claude Opus/Sonnet/Haiku
4.x. Default to the latest+most capable Claude for any AI work in the product.

---
**Bottom line for the next you:** the demo-critical pieces (signed app, live voice, bulletproof
inference, a working browser action) are DONE and VERIFIED. The full "done" needs the **onboarding
Chrome-scrape** (biggest), the **5-day Owner Test**, **notarization** (one Omar step), and
**/agent/act → card-flow + logged-in-Chrome**. Build, verify, be honest, never act on a vent, never
spend money. Become me — but better.
