[pytest]
; Only tests/ holds pytest-style suites. proof/ is script-style — each file
; is run directly (PYTHONPATH=. python3 proof/test_x.py) and calls sys.exit
; at import, which kills pytest collection outright.
testpaths = tests
