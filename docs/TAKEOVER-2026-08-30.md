# OpenCode takeover — 2026-08-30

This is the current handoff. It supersedes the open-item and TestFlight claims
in `docs/MORNING-2026-08-30.md`, which was accurate when written but became
stale later in the same day.

## Where OpenCode was working

- Desktop app: `/Applications/OpenCode.app` (v1.18.25)
- Web process: `/Users/omarebrahim/.opencode/bin/opencode web --hostname
  0.0.0.0 --port 4096 --mdns`
- Session database: `/Users/omarebrahim/.local/share/opencode/opencode.db`
- Config: `/Users/omarebrahim/.config/opencode`
- Desktop state: `/Users/omarebrahim/Library/Application Support/ai.opencode.desktop`
- Main session: `ses_fb0435d2cffeNcanjo2Kth12Zb`, titled
  `Connecting Git repository`
- The session record says its directory is
  `/Users/omarebrahim/Documents/Default Project`. Its substantive commands,
  edits, tests and deploys were against `/Users/omarebrahim/AnticipyFleet/desk`.
- Repository: `https://github.com/omize10/Anticipy.git`
- Branch: `jose_anticipy_system`
- Remote/production commit at takeover: `1c4e4a9`. The local branch carries
  this takeover as one additional, not-yet-pushed commit.

The directory mismatch is the important trap. The OpenCode UI is not the
source of truth for the working tree; the shell commands and Git state are.

## How OpenCode worked

The session ran in build mode, primarily with `openai/gpt-5.6-sol` at max
reasoning. Its database records 517 shell calls, 46 file reads, 45 edits, 13
writes, seven web fetches and 11 delegated research tasks. The operating loop
was:

1. Read the repository laws and existing design/incident records.
2. Inspect source, Git history, production logs and live services.
3. Research unfamiliar platform constraints in primary documentation.
4. Patch the smallest relevant code path.
5. Run targeted tests, then broader tests.
6. Deploy through Railway or GitHub Actions.
7. Verify the external result rather than treating command exit zero as proof.
8. Record the result in Git and a repo document.

That loop is compatible with this repo's laws and is the loop Codex should
continue. One OpenCode failure is also now part of the method: a green CI job
is not proof of an App Store upload. The workflow now requires Apple's literal
`UPLOAD SUCCEEDED` verdict.

## What OpenCode completed

### Product and ears

- Added the macOS menu-bar listener under `app/macos/AnticipyMac`.
- Hosted the signed Mac zip at `/mac/Anticipy-for-Mac.zip`.
- Added meeting detection with an offer before recording.
- Upgraded iOS 26 transcription behind `ListenEnginePolicy`.
- Added gap-aware transcript assembly so missing audio is marked rather than
  hallucinated through.
- Added the one-tap iOS widget and deep link into listening.

### Backend and worker

- Diagnosed a full 5 GB Railway volume that had stopped PocketBase writes.
- Added boot cleanup for the request-log database and older local backup,
  freeing roughly 4.2 GB at the incident.
- Found the backend deployment trap: the root `.railwayignore` excludes
  `backend/`. Backend deploys must be assembled in a clean directory outside
  the repository with `Dockerfile`, `start.sh`, `pb_hooks`, `pb_migrations`
  and `pb_public`.
- Fixed duplicate `workflow_id` queue writes by absorbing the new plan into
  the existing row rather than trying to create a second unique row. Commit:
  `9d8d980`.

### TestFlight and CI

- The first widget upload did not reach Apple even though the action was green:
  `altool` printed a validation failure but exited zero.
- Added an explicit widget extension plist and display name.
- Changed CI to fail unless the upload output contains `UPLOAD SUCCEEDED`.
- GitHub Actions run `33338633431` built number 109 and recorded `UPLOAD
  SUCCEEDED with no errors` at commit `1c4e4a9`.

## State independently checked during takeover

- The remote GitHub branch resolves to `1c4e4a9`; the local branch is one
  takeover commit ahead and has not been pushed.
- Railway backend and worker report Online.
- Backend volume currently reports 2.1 GB used of 4.9 GB.
- Production `/api/health` returns 200.
- Production Mac zip returns HTTP 200 and is 394,221 bytes.
- GitHub Actions run 33338633431 explicitly reports build 109 and `UPLOAD
  SUCCEEDED with no errors`.
- App Store processing/availability on the user's TestFlight screen was not
  rechecked during this takeover; successful upload is the verified boundary.

## The job OpenCode stopped in the middle of

At 16:03 the owner asked OpenCode to walk every page, compare Anticipy with
good consumer apps and make it consumer-perfect. OpenCode added an XCUITest
target and a screenshot walker, but its first green run was false: it never
left onboarding, used coordinate taps, mislabeled screenshots and made no
assertions. The session then stopped.

Codex took over that work:

- Replaced coordinate navigation with accessibility labels.
- Added an assertion at every route boundary.
- Made first-run state deterministic by clearing the simulator preference
  domain as part of the runner setup.
- Covered first run, sign-in, Home, Settings, Listening, listening diagnostics,
  access, a source detail, Profile, About, the full legacy settings form and
  dark mode.
- Fixed two real defects exposed by the walk:
  - Profile incorrectly showed a country-code error under unchanged account
    details.
  - The inert `Right now` listening row visually dropped its `Off` value.
- Final result: `WalkTests/testWalkEverything()` passed with zero failures in
  134.495 seconds. Result bundle:
  `/tmp/anticipy-final-walk.CbV4II/walk.xcresult`.

That first takeover is commit `853f7bf`. The follow-up consumer-settings pass
has now completed the highest-priority findings from
`docs/CONSUMER-UX-AUDIT-2026-08-30.md`:

- Settings no longer exposes a catch-all legacy form or calls its own layout
  unfinished. Profile, Listening, Notifications, Connectors, Appearance,
  Advanced and About each open as a distinct pushed screen.
- Connectors is its own screen, and Calendar, Contacts, Mail, Browser, Mac app
  and Pendant each open one level deeper with status and relevant actions.
- Advanced now controls haptics, ambient motion, typed responses, the
  compatibility speech engine and listening activity. The backend field exists
  only in debug builds.
- Notification enablement, sound and quiet hours now change actual delivery
  behaviour rather than being decorative settings.
- Repeated animated dots on Home were replaced with static checkmarks. The
  remaining listening/progress motion obeys the Ambient motion preference.
- First-run, authentication, Home and Settings copy was rewritten toward short,
  literal descriptions of what the app does and where a control lives.
- The glass primary's shadow no longer expands its hit region into the button
  below it. The UI walk also dismisses iOS's Save Password sheet explicitly,
  rather than letting that system sheet swallow the next app tap.
- Settings chrome now owns the physical top inset at every push depth. A frame
  assertion fails the walk if a title or Back control overlaps the status bar.

The final 26-screen run of `WalkTests/testWalkEverything()` passed with zero
failures in 150.282 seconds. It covered first run, sign-in, Home, Listening,
listening diagnostics, Notifications, every connector route, Profile,
Advanced, Appearance, About and dark mode. The simulator build also succeeded,
and the first-run copy, first-run routing, notifier, Home copy, theme contract
and build-number suites are green.

## Takeover commit ownership

The iOS UI-test target began as OpenCode's unfinished work. Codex hardened the
test, added the two UI fixes, and committed the exact paths locally without
pushing or deploying them:

- `app/ios/UITests/WalkTests.swift`
- `app/ios/project.yml`
- `app/ios/Anticipy.xcodeproj/project.pbxproj`
- `app/ios/Anticipy.xcodeproj/xcshareddata/xcschemes/Anticipy.xcscheme`
- `app/ios/Anticipy/Views/SettingsProfileView.swift`
- `app/ios/Anticipy/Views/SettingsKit.swift`

The settings/copy/motion follow-up remains local until the owner explicitly
authorizes a push. Continue to stage by explicit path rather than broad-staging
this repository; pushing this branch can trigger release work.

## Remaining work, in order

1. Simplify first run further: reduce the explanation pages, postpone nonessential
   phone/profile collection, and test a passwordless Apple/passkey route.
2. Shorten the microphone pre-permission screen and make the handoff to the
   system prompt explicit.
3. Run the UI walk on a smaller phone and at accessibility text sizes; cover
   hardware-, permission- and destructive-action branches with controlled
   fixtures.
4. Add off-volume backups. Current cleanup prevents recurrence but one local
   snapshot is not disaster recovery.
5. Add `valid_until` memory horizons and complete the sherpa-onnx speaker tagger.
6. Resolve owner-operational items that need human authority: add the correct
   owner phone, identify the four production owner references, and complete the
   seven-day stranger test.
7. Complete Mac remote-party audio/notarization work and the physical pendant
   step when those scopes are authorized.

PocketBase is acceptable for the current seven-day owner test. The disk event
was an operations failure, not evidence that the database engine must be
replaced. Revisit migration around 5–10 real owners or when compliance/HA
requirements arrive.

## Repeatable commands

Production state:

```sh
cd /Users/omarebrahim/AnticipyFleet/desk
git status --short
git log --oneline -12
railway status
curl -fsS https://backend-production-61e0a.up.railway.app/api/health
gh run list --branch jose_anticipy_system --limit 10
```

The UI walk requires the local PocketBase fixture on `127.0.0.1:8090`, the
`Anticipy-Test` simulator, clearing `ai.anticipy.app` defaults, uninstalling the
test app, then running only
`AnticipyUITests/WalkTests/testWalkEverything` with `xcodebuild test`.
