# NEXT GATE — exactly one, with objective user-visible receipts

## ✅ Gate 0 — Current Truth + Read-Back (CLOSED 2026-06-17)
Re-verified from scratch with live commands (not old docs): branch/commit, app↔engine connected,
OpenRouter real (BRAIN ALIVE 0.69 s), live/mock/stub map, Calendar live, Gmail owner-gated,
Twilio mock, browser ready. See `CURRENT_TRUTH.md`.

## 🎯 ACTIVE — Gate 1: Product surface opens & talks to engine (proven in Chrome)
**Goal:** Omar can open the product and it is wired to the real brain.
**Receipts (each must be able to FAIL):**
- Chrome opens `localhost:3000`; the day surface renders (not an error page).
- A user action in the UI round-trips to the engine and back (visible result).
- Screenshot saved to `docs/e2e/current_gate/`.

## ON DECK (in order)
- **Gate 2 — Onboarding / profile / tool mesh through the UI:** welcome → connect → a sourced profile
  the app shows; connected tools listed; missing auth shown as an honest blocker (not fake-done).
- **Gate 3 — Messy day through the UI:** the directive's 9-line scenario typed into the real app →
  Amazon AUTO_DO_WITH_OPT_OUT (human "I'm on it, tell me to stop"), coffee/lottery vents silent,
  Sam deck ONE thread, "it"→Sam deck, retainer→CRM/honest blocker, Jarvis desk resolved, money never
  crosses, proof + follow-up shown, human language. Screenshot the result.
- **Gate 4 — Live funded arms (read-back):** Calendar create→read-back→delete; Browser prepare a
  cart/form on a real site (screenshot+DOM+URL, never buy); Twilio outbound to owner # (read-back) +
  inbound YES resolves the exact ask — Twilio/Gmail legs are OWNER-gated (flip live / authorize).
- **Gate 5 — Representative human-life coverage:** founder/lawyer/doctor/accountant/exec/VC/family/
  student/sales × browser/API/voice/vague-memory/vents/money/follow-up/reingest → 0 critical.
- **Gate 6 — SOFTWARE_CERTIFIED_READY_FOR_OWNER_5DAY** when all above hold + proof bundle + commits.

## OWNER-gated (cannot finish autonomously — see CURRENT_TRUTH)
Gmail toolkit · Twilio live + public wss · hosted deploy · Apple signing · the 5 lived days.
