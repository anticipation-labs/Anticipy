{
  "gate": "B_onboarding",
  "pass": true,
  "mode": "deterministic",
  "evidence": "NEW front door app/welcome/page.js (guided 4-step first run) + rewired app/connect/page.js (real in-app connect, dead arcade.dev deep-link removed) + demo scaffolding killed in app/page.js (SAMPLE='' , DEFAULT_MEMORY seeds no people/owner, reset button blanks). Wiring proven by engine/scripts/test_onboarding_frontdoor.py PASS (exit 0): engine side runs through the REAL anticipy_engine.core.control_core.ControlCore.owner_onboard (control_core.py:1526) + authorize_connection_loop (control_core.py:1995); app side asserts the front-door source posts to the right routes. Next proxies already present: app/api/owner/onboard/route.js -> engine POST /owner/onboard (main.py:832), app/api/connections/authorize/route.js -> engine POST /connections/authorize (main.py:697), app/api/onboard_scan/route.js -> engine POST /onboard/scan_api (main.py:868). Added to scripts/run_suite.sh unit list (token 'onboarding_frontdoor'). Next production build compiles all 28 routes including /welcome (4.17 kB) with zero errors.",
  "verified_at": "2026-06-17",
  "detail": "WHAT A REAL USER CAN NOW DO (the front door): 1) /welcome opens trust-first ('Before I start listening — who am I helping?'), captures name + optional timezone/phone, then people + preferences, with NO seeded world (a brand-new person starts blank). 2) On step advance it POSTs to /api/owner/onboard the exact onboardingPayload (source 'first_run', owner_name, people, preferences, and the two unlock accounts registered status='needs_auth' route='api'), which the engine writes as a SOURCED profile (every memory carries fields['source']) plus a 'connect_account' open-loop per unconnected account; the front door captures each loop's memory_id from the response. 3) The connect step calls POST /api/connections/authorize with that memory_id; the engine's authorize_connection_loop asks the Arcade connector's authorize() and returns either status='connected' or a connect_url (the PROVIDER's own consent screen). The page renders/opens that connect_url in a NEW TAB (window.open) and then POLLS /connections/authorize every 4s for ~2 min, flipping the row to 'connected' only when the connector confirms authorization actually completed. 4) Post-connect 60-second reward: the read-only recap (/api/onboard_scan -> /onboard/scan_api) shows 'here's what I gathered' with the 'I invented nothing' honesty line. /connect was ALSO rewired: the calendar+email row now connects FOR REAL (ensures the connect open-loops via /api/owner/onboard, launches /api/connections/authorize, opens the real consent, polls) instead of the dead-end arcade.dev deep-link (removed). DEMO DATA GONE (asserted in test): app/page.js SAMPLE transcript is '' (was the Omar/Maya/Sam coffee-machine sample), DEFAULT_MEMORY seeds NO people and no owner name (was 'Omar' + 'Maya, wife...' + 'Sam, contractor...'), and the capture-box reset button blanks the textarea (setText('')) instead of restoring the sample (no setText(SAMPLE) anywhere). LIVE-WIRED vs OWNER-GATED (honest boundary): LIVE-WIRED end to end at the contract level — the profile write, the sourced read-back the main app consumes, the connect open-loops, the authorize call, the connect_url render, and the poll-until-connected loop are all real code paths exercised by the test through the real ControlCore. OWNER-GATED (expected, not a gap): (a) the FINAL OAuth consent tap on the provider's own screen is the user's — we never enter their credentials, which is correct; the flow is built up to and through LAUNCHING that real consent. (b) For authorize_connection_loop to emit a real provider connect_url (rather than the deterministic 'mock' status it returns under ANTICIPY_HANDS_MODE=mock), the engine must run with live hands + Arcade configured (ANTICIPY_HANDS_MODE=live + ARCADE_API_KEY); that is the same money/keys owner-gate the rest of the live path already carries, not new onboarding work. (c) Off-localhost deploy requires ANTICIPY_APP_OWNER_TOKEN (the existing default-secure owner gate in app/api/_engine.js); the front door inherits it via same-origin credentials, no extra work. TEST COMMAND: PYTHONPATH=engine ANTICIPY_MODEL_PROVIDER=stub ANTICIPY_HANDS_MODE=mock engine/.venv/bin/python engine/scripts/test_onboarding_frontdoor.py -> 'PASS frontdoor engine: ...' / 'PASS frontdoor app source: ...' / 'PASS onboarding_frontdoor: a new user can onboard -> connect (real consent) -> recap', exit 0. LINT/BUILD: Next bundled-babel JSX parse of app/welcome/page.js, app/connect/page.js, app/page.js, app/layout.js + the two proxy routes all PARSE OK; `next build` -> 'Compiled successfully', 28/28 static pages generated, zero errors. REGRESSION: existing onboarding tests all PASS unchanged (owner_onboarding, onboarding_profile, onboarding_profile_endpoint, onboard_discover, onboard_scan, clarify). The premium-copy gate (check_premium_copy.py) is CLEAN on the new pages (no vendor/implementation name leaks — copy passes through the same humanCopy guard the connect page uses). NOT-MY-CHANGE NOTE (honest): engine/scripts/test_public_backend_path.py fails on this branch, but it fails IDENTICALLY with all my app/ changes stashed (confirmed) — it is a pre-existing engine ingest-path assertion (source_text appearing in visible follow-up loops) that touches no onboarding/front-door code; my work neither caused nor weakened it."
}
