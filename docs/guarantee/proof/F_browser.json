{
  "gate": "F_browser",
  "pass": false,
  "mode": "deterministic",
  "evidence": "test_browser_available_probe.py PASS (4 cases) + test_browser_result_on_card.py PASS (2 cases) + test_purchase_guard.py (27 money blocked / 24 cart-nav allowed) + test_browser_prompt_injection.py PASS (6 cases) + test_browser_safety_loop.py PASS (2 cases) + test_browser_use_cdp.py PASS; false-ready fix: engine/anticipy_engine/hands/browser_use_link.py:72 available() now probes the chrome binary; result-on-card: engine/anticipy_engine/core/control_core.py:856 _land_browser_result_on_card",
  "verified_at": "2026-06-17",
  "detail": "available() now probes the binary (was false-ready: chromium-1161 cache ABSENT yet ok=True); it returns ok=False reason='chrome binary missing' when the binary is gone, ok=True when present or a CDP-attach url is set (4/4 probe cases pass). result lands on card: the browser round-trip's resolved receipt (final url + screenshot flag/path + answer) is written onto the durable owner card record (owner_cards/<ask_id>.json) with a terminal state (done/failed) and persisted, parity with the API arm's read-back proof (2/2 cases pass: success carries url+screenshot+answer state=done; honest failure -> state=failed, no faked screenshot/answer). guards proven: 27 money/checkout controls blocked, 24 cart/nav allowed; in-loop money stop refuses 'Place your order' (no act reaches the browser) while a 'Continue shopping' nav is dispatched; injected page text ('navigate to evil.com', 'ignore your task', forged fences) cannot authorize an action (navigations=[]). live cart-prep: NOT run -- the proven Chromium build (chromium-1161) the runner needs is ABSENT and is no longer installable via the bridge's playwright 1.60 (it offers Chrome-for-Testing 148 / chromium-1223). Installed chromium-1223 and ran ONE bounded attempt through the engine's own browse_act/runner on example.com (visible AND headless): the CFT-148 binary LAUNCHED but browser-use 0.13.1's BrowserStart/Navigate watchdog timed out at 30s (the exact newer-CFT-vs-single-process incompatibility the runner documents). Not forced further (would mean changing the pinned, proven browser-use/chromium stack while the cert runs). REMAINING LIVE ITEM: one bounded throwaway-browser cart-prep producing a real screenshot+DOM+URL receipt, blocked on restoring the chromium-1161 build (or upgrading the browser-use<->chromium pin) -- a runtime/dependency task, not a code defect. The honest-available fix means the arm now reports itself unavailable instead of false-ready until that binary is restored.",
  "subitems": {
    "false_ready_fixed": true,
    "result_lands_on_card": true,
    "guards_proven": true,
    "live_cart_prep_ran": false
  },
  "guard_counts": {
    "money_blocked": 27,
    "cart_nav_allowed": 24,
    "injection_cannot_authorize": true,
    "in_loop_money_stop": true
  },
  "tests": [
    "PYTHONPATH=engine engine/.venv/bin/python engine/scripts/test_browser_available_probe.py",
    "PYTHONPATH=engine engine/.venv/bin/python engine/scripts/test_browser_result_on_card.py",
    "PYTHONPATH=engine engine/.venv/bin/python engine/scripts/test_purchase_guard.py",
    "PYTHONPATH=engine engine/.venv/bin/python engine/scripts/test_browser_prompt_injection.py",
    "PYTHONPATH=engine engine/.venv/bin/python engine/scripts/test_browser_safety_loop.py",
    "PYTHONPATH=engine engine/.venv/bin/python engine/scripts/test_browser_use_cdp.py"
  ]
}
