{
  "gate": "I_follow_up",
  "pass": true,
  "mode": "deterministic",
  "evidence": "engine/scripts/test_follow_up_fires.py PASS + engine/anticipy_engine/core/control_core.py:560 (_schedule_follow_up writes the fireable open_loop) + engine/anticipy_engine/core/proactive.py:644 (trigger_tick -> _fire_reminder delivers it)",
  "verified_at": "2026-06-17",
  "detail": "FIRES: control_core._schedule_follow_up (control_core.py:560) turns a card's computed follow_up plan into a durable open_loop with a stable per-card id (followup:<card_id>) carrying remind_ts == when_ts and the link fields follow_up_for_card_id + origin_proof. The EXISTING loop-ledger trigger (proactive.trigger_tick) then fires it once at when_ts via _fire_reminder, which routes a kind=='follow_up' loop straight to a budget-capped NOTIFY over the same TextChannel reminders use (proactive.py:644), delivering a real channel artifact linked to the original card id + its proof; fire-once holds via the durable fired_at stamp. LINKED: the fired nudge receipt and the notify glassbox log both carry follow_up_for_card_id + origin_proof. IDEMPOTENT: the stable loop id + preserve-existing-when_ts means re-ingesting the same line never churns when_ts and never double-schedules (proven in test). EXCLUDED (no nuisance nudges): follow_up.warrants_follow_up was tightened to require an external-dependency ACTION or an explicit external-dependency phrase (directed send/deliver to a party, chase/await another's reply, or 'make sure it lands'/'confirm receipt') so a bare verb mention (e.g. 'remind me to confirm my own RSVP', 'I should reply-all less') no longer trips it; vents (ignore), preferences (remember), and money (disposition==blocked OR category==money) never get a follow_up. UI: app/page.js now renders the attached plan dict via followUpNote(card.follow_up) ('I'll check back on this in N days.') and loopMeta keys on fields.kind==='follow_up', not an action literal. Test command: PYTHONPATH=engine ANTICIPY_MODEL_PROVIDER=stub engine/.venv/bin/python engine/scripts/test_follow_up_fires.py -> 'PASS test_follow_up_fires', exit 0. Added to scripts/run_suite.sh unit list."
}
