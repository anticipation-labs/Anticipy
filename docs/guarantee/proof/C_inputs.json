{
  "gate": "C_inputs",
  "pass": true,
  "mode": "deterministic",
  "evidence": "engine/scripts/test_inputs_same_brain.py PASS (exit 0) + engine/anticipy_engine/main.py:722 (typed POST /owner/ingest -> core.owner_ingest), main.py:748-774 (POST /owner/ingest-file: is_audio_file -> transcribe_audio -> core.owner_ingest), main.py:788-799 (POST /listen/start sink -> core.owner_ingest(execute_actions=True)). Added to scripts/run_suite.sh unit list (token 'inputs_same_brain').",
  "verified_at": "2026-06-17",
  "detail": "Proves at RUNTIME (not by code-reading) that typed + MP3/audio + mic all feed the SAME brain, core.owner_ingest. ONE sentence ('remind me to send Priya the deck Friday') is driven through all three REAL wiring paths and each must produce a card naming the task (markers 'priya'/'deck'). TYPED: core.owner_ingest('transcript', text, execute_actions=False) -> 1 card, disposition='ask', title='Prepare message for Priya' (this is the body POST /owner/ingest passes through at main.py:722). MP3/audio: mode='FULL-AUDIO' on this machine — a REAL audio file is synthesized with macOS `say -o anticipy_gatec.aiff '<sentence>'`, converted by ffmpeg to anticipy_gatec.mp3 (libmp3lame), then run through the ACTUAL file-endpoint path is_audio_file() -> transcribe_audio() (local Whisper model 'tiny.en', transcriber=='local_whisper') which recovered '[00:00:00-00:00:02] Remind me to send Priya the deck Friday.', and that recovered transcript was fed into core.owner_ingest('audio_upload', ...) -> 1 task card. (The test has an HONEST fallback: if `say`/ffmpeg/Whisper were genuinely absent it would prove the same is_audio_file->transcribe_audio->owner_ingest wiring with a monkeypatched transcribe and report mode='path-verified'; that fallback did NOT trigger here — full audio ran, verified twice, deterministic.) MIC: the EXACT sink /listen/start installs (main.py:788-799) is rebuilt byte-for-byte and driven with a synthetic heard event from a WORKER THREAD (as MacMicSource's daemon thread does) via MacMicSource.emit_stub, which builds the same CaptureEvent (source=='mac_mic') the real loop emits; asyncio.run_coroutine_threadsafe marshals event.source/event.text into core.owner_ingest(..., execute_actions=True) -> 1 card, disposition='do'. No microphone hardware required. SAME BRAIN: all three routes produced a card for the same task; the dispositions differ only because typed ran preview (execute_actions=False -> ask) while mic ran the live execute path (execute_actions=True -> do) — same intake, same task, one shared pipeline. Deterministic via ANTICIPY_MODEL_PROVIDER=stub + ANTICIPY_HANDS_MODE=mock so it adds no live-model load (the running cert is untouched); the wiring is what Gate C proves, not the model. Test command: PYTHONPATH=engine ANTICIPY_MODEL_PROVIDER=stub engine/.venv/bin/python engine/scripts/test_inputs_same_brain.py -> final line 'PASS inputs_same_brain: TYPED + AUDIO/MP3 + MIC all reach the SAME brain (core.owner_ingest) and each produced a card for the same task.', exit 0."
}
