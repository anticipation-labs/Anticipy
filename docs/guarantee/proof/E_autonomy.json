{
  "gate": "E_autonomy",
  "pass": true,
  "mode": "deterministic",
  "evidence": "engine/scripts/test_autonomy_modes_gate.py PASS (exit 0) through REAL anticipy_engine.core.control_core.ControlCore.owner_ingest (control_core.py:1081); per-card labelling via anticipy_engine.proactive.autonomy.classify_autonomy (autonomy.py:24) wired at control_core.py:1308-1310 (c['autonomy_mode']) + :1313-1320 (out['middle_trace']['autonomy'])",
  "verified_at": "2026-06-17",
  "detail": "All 6 autonomy modes asserted on card['autonomy_mode'], cross-checked equal to out['middle_trace']['autonomy'][*]['chosen_mode'] (the test fails if the two disagree). Modes proven via FULL INGEST through the real spine (one scenario per mode, asserting what the stub spine actually yields — no forcing): REMEMBER_ONLY = 'I like the dark roast coffee' -> remember/write_profile_memory card [full-ingest]. IGNORE = vent 'ugh I could scream about this week' -> out['cards']==[] [full-ingest, asserted as 0 cards]; the IGNORE mode itself is additionally pinned with a direct classify_autonomy({disposition:None,action:None})=='IGNORE' unit assertion, because IGNORE's runtime expression is the ABSENCE of a card (a vent yields nothing), not a card carrying mode==IGNORE. PREPARE_THEN_STOP = money 'just pay the overdue invoice now with the card' -> disposition=='blocked' card AND external send 'send Priya the deck by Friday' -> action=='draft_or_confirm_message' card [full-ingest x2]. CLARIFY_FIRST = ambiguous 'can you handle that thing' (execute) -> ask card [full-ingest]. AUTO_DO_WITH_OPT_OUT = 'open chrome and find me a cheap flight to NYC next week' -> action=='browser_action' card [full-ingest]. AUTO_DO = 'remind me to call the dentist at 3pm tomorrow' (execute) -> do/act card [full-ingest]. HONEST SUMMARY of how each was proven: 5 of 6 modes (REMEMBER_ONLY, PREPARE_THEN_STOP, CLARIFY_FIRST, AUTO_DO_WITH_OPT_OUT, AUTO_DO) proven SOLELY via full owner_ingest; IGNORE proven via full-ingest (0 cards) AND backed by a direct classify_autonomy unit assertion (since 'no card' is the correct runtime form of IGNORE). No mode required a forced/synthetic card to elicit. STUB-MODE NOTE: run with ANTICIPY_MODEL_PROVIDER=stub (moat OFF), so dispositions come from the deterministic spine; this proves the autonomy-mode mapping is correctly WIRED to the real card stream with zero live-model load. Live inference quality is covered by Gate J (10k live cert). Test command: PYTHONPATH=engine ANTICIPY_MODEL_PROVIDER=stub engine/.venv/bin/python engine/scripts/test_autonomy_modes_gate.py -> 'PASS autonomy_modes_gate (Gate E): all 6 modes proven', exit 0. Added to scripts/run_suite.sh unit list."
}
