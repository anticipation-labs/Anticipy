{
  "gate": "A_front_door",
  "pass": true,
  "mode": "live-local",
  "evidence": "engine :8799 /health 200 + /status 200 + /owner/ingest contract OK (observed_lines + cards[disposition/route/action/proof/follow_up] + middle_trace); web UI :3099 next start home GET 200 with 'Anticipy' SSR markup + /api/health and /api/owner/ingest proxied live to engine :8799 (200, full cards round-trip); extension MV3-valid + background.js node --check OK + extension link test PASS (token-auth WS, ping/pong, reject-unauth, reload broadcast).",
  "verified_at": "2026-06-17",
  "detail": "LOCAL open path proven end-to-end on this Mac. (1) ENGINE: started safe on spare port 8799 (ANTICIPY_CHANNELS_MODE=mock, ANTICIPY_HANDS_MODE=mock, ANTICIPY_INBOUND_POLL_SECONDS=0, ANTICIPY_MODEL_PROVIDER=stub, isolated tmp ANTICIPY_DATA_DIR) without clobbering the pre-existing :8787 engine. GET /health -> 200 {\"status\":\"ok\",\"service\":\"anticipy-engine\",\"version\":\"0.1.0\"}. GET /status -> 200 (control_core, channels mock ready_to_enable, readiness overall=local_mock). POST /owner/ingest {execute_actions:false} -> 200 with the UI's exact contract: observed_lines[], cards[] each carrying id/disposition(ask|do)/route(voice_text|api)/action/args/confidence/reason/status/proof[memory_write,memory_read_back,capture_memory_status,card_record]/follow_up/autonomy_mode, plus middle_trace and ignored_line_count. (2) WEB UI: clean production `next build` exit 0, BUILD_ID 5dgTnrTVyzN1t6ugN9q0e (28 routes). `next start -p 3099` with ANTICIPY_ENGINE_URL=http://127.0.0.1:8799; GET / -> 200, 6426 bytes, 'Anticipy' + 'your day' present in SSR markup; GET /api/health -> 200 proxied live from engine; POST /api/owner/ingest -> 200 full cards round-trip proving the app talks to the engine through app/api/_engine.js (ENGINE_URL = process.env.ANTICIPY_ENGINE_URL || http://127.0.0.1:8787). (3) EXTENSION: extension/manifest.json JSON-parses, manifest_version=3, background.service_worker=background.js, permissions=[storage,alarms,scripting,tabs,tabGroups,debugger], host_permissions=[http://127.0.0.1/*,<all_urls>]; node --check passes on background.js, engine_client.js, popup.js; engine exposes the link endpoints the extension uses (GET /ws/token, WS /ws/extension token-gated, POST /extension/hello, POST /onboard/discover); engine/scripts/test_extension_link.sh -> 'PASS piece 2: extension link (token-auth WS, ping/pong keepalive, reject unauth, reload)'. Both servers I started were killed; the pre-existing :8787 engine was left intact. OWNER-GATED (not provable without Omar's credentials/deploy): hosted real-URL deploy of the web app, and a signed + notarized downloadable Mac app. macapp/ today is a launcher shell that boots the engine+UI from the repo path on this Mac (per macapp/README.md, an unsigned dev preview requiring right-click->Open); turning it into an 'anybody can download it' distributable needs the engine + prebuilt UI bundled inside the .app and Apple Developer ID signing/notarization."
}
