{
  "gate": "D_memory_intent",
  "pass": true,
  "mode": "deterministic",
  "evidence": "engine/scripts/test_memory_intent_gate.py PASS (exit 0) through REAL anticipy_engine.core.control_core.ControlCore.owner_ingest (control_core.py:1081); middle wiring at control_core.py:1175 (_intent_resolve) + :1176 (_consolidate_obligations); durable restart surface via control_core.owner_cards (control_core.py:2048) + memory.open_loops.all (memory/store.py:217)",
  "verified_at": "2026-06-17",
  "detail": "Four sub-checks proven end-to-end through the real owner_ingest spine (not the unit helpers). (1) VAGUE REFERENCE RESOLVES: transcript ['the Jarvis standing desk is the one I liked','can you pull up that desk thing I mentioned?'] -> the 'desk thing' line resolves to the Jarvis desk; asserted on out['middle_trace']['resolutions'] (decision=='resolved', chosen_referent contains 'Jarvis'+'standing desk', resolved_to contains 'Jarvis') AND on the surviving card text containing 'Jarvis'. (2) DUPLICATES COLLAPSE: a relayed obligation + its synonym confirmation ['call Amazon about the monitor','handle the Amazon monitor issue'] through the full spine (execute_actions=True) -> exactly ONE card whose source_text/title contains 'amazon' (the dedup law: one real obligation = one card). (3) VENTS IGNORED: ['honestly I'm so done, I'm moving to the woods.','if I win the lottery I'm buying an island lol'] -> out['cards']==[] (silent; never an action, never an ask). (4) RESTART-STABLE / IDEMPOTENT: ingest 'remind me to call the dentist at 3pm tomorrow' (execute) -> 1 durable card + 2 open_loops; STOP the ControlCore; START a NEW ControlCore on the SAME data_dir -> owner_cards count and open_loops count are unchanged (durable memory survives the restart); re-ingest the SAME text -> still 1 card + 2 loops (no double-count). STUB-MODE CAVEAT (honest): run with ANTICIPY_MODEL_PROVIDER=stub, so the moat model is OFF; vague-ref resolution and vent-suppression here ride the DETERMINISTIC layer (intent_threads + owner_mode + _consolidate_obligations). That is exactly what Gate D proves: the middle is WIRED into owner_ingest and behaves correctly with zero live-model load (a 10k live cert is running). The LIVE inference QUALITY on messy real speech/sarcasm/relayed-to-others judgement is proven separately by the 10k cert (Gate J). Consequence of the moat being off: the literal phrasing 'Riley asked me to call Amazon ... yeah I'll handle it' is (correctly) NOT auto-adopted as the owner's task without the live moat (audit fix ff93775 keeps questions-to-others off the owner's task list), so the dedup law is exercised with the obligation phrased as the owner's own + a synonym confirmation — the identical collapse. Test command: PYTHONPATH=engine ANTICIPY_MODEL_PROVIDER=stub engine/.venv/bin/python engine/scripts/test_memory_intent_gate.py -> 'PASS memory_intent_gate (Gate D)', exit 0. Added to scripts/run_suite.sh unit list."
}
