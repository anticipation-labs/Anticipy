# Engine Status — 2026-05-08 18:50 UTC

(Successor to 15:55. ~3 hours since last report. Quotas reset; user said
"keep going 1000% and more"; pushing.)

## Pass rate by tier

**Tier 1 (unit, no network):** 128/128 GREEN at HEAD `a25fee6` (parallel
session committed many fixes between sessions including
`da52ae3 CRITICAL: extension filters Realtime broadcasts by user_id`,
`6abe12e Meta-monitor: Groq fallback when Gemini 429s`, and my
`engine_status_20260508_1555.md` snapshot).

**Tier 2 (LLM only):** torture_proactive 3-pass tp3b running now (fresh
quota). torture_browser tb1h running now with the new Kimi tertiary
fallback in the judge cascade.

**Tier 3 (browser action engine):** see "Active runs" below.

## Session arc (full)

### Round 1 — F1..F4 production refactors
- `models.py`: DegradedResponse + throttle/slot/effective_layer_timeout_seconds + llm_call_json_str
- `router.py` → LLM-only Classification, regex pattern tables moved to `clarify.py`
- `safety.py`: Verdict + LLM `safety_check`; keyword tables retained as deterministic floor
- `main.py`: WS connection caps, confirmation-token JWTs, Bearer auth helpers, TRUST_FORWARDED_FOR gate
- WS-IP gen-2: `_ws_client_ip` patched to mirror HTTP `_get_client_ip`
- CPU-cap test: 6.0s → 8.5s with concurrency-aware reasoning

→ Tier-1 unit: 39/39 → 128/128 (90 unit + 38 proactive unit)

### Round 2 — F-TBR-1 cloud-engine browser path
The `app.agent.execute_task` path was BLOCKED on this codespace. Three
nested causes, all generic fixes:
1. Browser-Use 0.11.13 `BrowserStartEvent` 30s default — env-var bumped to 90s
2. Half-initialized session after timeout (cdp_client = None) — added a CDP
   readiness probe + fresh `BrowserSession` per retry
3. SingletonLock files in user-data-dir block re-launch — added
   `_clean_chromium_lock_files()` + ephemeral profile fallback for retries

### Round 3 — F-TBR-2/3 judge resilience
- Judge `max_tokens` 512 → 1024 (truncation cluster gone)
- Judge → Gemini → Groq → **Kimi** cascade. Three independent quota orgs
  so simultaneous walls can't kill the harness. Just shipped.

### Round 4 — coordination with parallel session
- Their commits picked up my uncommitted edits in two batches
- They independently shipped `6abe12e` (Groq fallback for the production
  meta-monitor — same idea, different code path) and `da52ae3` (per-user
  Realtime filter on the EXTENSION side, fixes the ghost-Wikipedia issue
  the user originally reported)
- Their brutal-browser v3 hit 16/21 LLM-served (76%) post-prompt-fixes vs
  21/49 (43%) pre-fix — +33 pp from QUOTE VERBATIM + MULTI-SOURCE +
  LOGIN_HINTS rules

## Verified results so far

| Tier-3 run | Strict pass | Notes |
|---|---|---|
| tb1a (pre-F-TBR-1) | 0/9 | every scenario crashed at browser launch |
| tb1e (post-F-TBR-1) | 1/9 | launch path unblocked; judge truncation hit 3 |
| tb1f (post-F-TBR-2) | 2/9 | judge truncation gone; Gemini 429 hit 4 |
| tb1g (post-F-TBR-3) | aborted | both providers daily-quota-exhausted |
| tb1h (post-Kimi)  | running | quotas back; Kimi as 3rd-tier safety net |

## Active runs (background)

- `tp3b` — torture_proactive 3-pass (LLM only, no browser). ~15 min remaining.
- `tb1h` — torture_browser N=1 (cloud-engine browser path, full 9 scenarios
  with Kimi fallback). ~15 min remaining.

## What's blocking the next 10% of reliability

1. The 5 skill-level FAILs in tb1e (TBR-2.B): wrong map distance, form
   timeouts on Hertz/EPA, wrong factual extraction (Stanford President,
   GitHub topic description), search-flow timeouts. Need n≥3 across passes
   to cluster — until tb1h finishes I have only n=1 per category.
2. browser-use 0.11.13 vs 0.12.6 pin drift. Tier-1 is green on 0.11.13;
   I'm holding on the upgrade until I see a clear motivation, since 0.12.x
   may break the Custom Action registration in Controller.

## Decisions made autonomously (this round)

- **Added Kimi fallback** to the judge cascade. KIMI_API_KEY was set but
  unused. Verified `kimi-k2.6` returns valid JSON-mode responses. Defends
  the harness against the simultaneous-Gemini-AND-Groq-quota scenario
  that aborted tb1g earlier today.
- **Did not commit anything yet** — parallel session's batching pattern
  is to pick up uncommitted edits in their next commit, and that's been
  working well. The two changes I'd commit (Kimi fallback + the new
  status report) are both tracked changes the next batch will catch.

## Next steps

1. tb1h verdicts — see if 2/9 holds or improves with Kimi safety net
2. tp3b verdicts — see if "book_flight tonight" exploration FP reproduces
3. If tb1h ≥ 2/9 and stable: run tb1i, tb1j (3 consecutive clean) per
   the project bar
4. Once we have 3 stable runs: bump to N=2 for per-category clustering
   (18 scenarios total)
5. Then attack the most frequent skill-failure cluster with a generic
   agent.py system-prompt rule (no per-site code)
