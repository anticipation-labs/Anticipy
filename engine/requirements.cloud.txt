# Slim cloud deps for the hosted engine. NO whisper/torch — in the cloud, audio arrives via Twilio
# (ConversationRelay) or Deepgram streaming, never the Mac mic, so the heavy local-transcription stack
# is dropped (whisper is lazy-imported in capture/transcribe.py and never reached on the cloud path).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
httpx>=0.27
arcadepy>=1.0
websockets>=12.0
python-dotenv>=1.0
