"""MH-P2/P3 disciplined memory write+retrieval layer. Composes the
existing app.memory backend (not modified); adds chunk+embed+dedup,
time-decay, the non-promotable invariant, and the draw path. Frozen
reasoning + action engine untouched."""
