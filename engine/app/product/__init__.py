"""The real Anticipy product: conversational onboarding + real-mic
product loop + designed UI, packaged into the openable .app.
Reuses the frozen brains (onboarding.py, audiostack, proactive_day,
reasoning); modifies no frozen code."""
