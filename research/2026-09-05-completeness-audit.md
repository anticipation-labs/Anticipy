# Completeness audit — 2026-09-05, before TestFlight

Seven readers swept every ledger and live surface (the Omi port ledger, the browser-region ledger, the ears/iOS build, the brain on Cloudflare, Worker-vs-hook parity, every gate and test, memory and mouth); every finding was then handed to an independent refuter told to reproduce it or throw it out. 44 survived. Severity is the refuter's; the fix is the refuter's minimal correct one. The sweeps' full text is in the workflow journal for run wf_1ad82ae0-394.

Clusters building the fixes (2026-09-05 evening): W1a Worker routes (F01–F03, F14, F40), W1b Worker write path (F13, F15, F27, F39, F42), B1 brain and gates (F04, F11, F17, F19, F25, F28–F30, F32–F34, F43), E1 extension Law-1 round two (F05–F08, F10, F24, F36), C1 CI, iOS build, ledgers (F09, F12, F16, F18, F20–F23, F26, F31, F35, F37, F38, F41, F44).

| # | severity | evidence (refuter, abridged) | fix (refuter, abridged) |
|---|---|---|---|
| F01 | SHIP-BLOCKER | CONFIRMED, independently, on a different paired agent than the sweep used. Source (all read this session): - migration/workers/src/routes/agent.ts:126-147 `agentKey` returns exactly `{ llm_proxy: true, model, owner_ref }`. No owner_profile lookup, no vision_model. - backend/pb_hooks/agent_key.pb.js:34-60 returns `owner` (first_name/last_name/email/phone/birthday/facts from owner_profile) and `vision_model` ($os.geten | In migration/workers/src/routes/agent.ts `agentKey` (:143-147): (a) look up `SELECT first_name,last_name,email,phone,birthday,facts FROM owner_profile WHERE owner_ref=? LIMIT 1` on env.DB and return it as `owner` (null when absent, matching agent_key.pb.js:34-52; wrap in try/catch → null); (b) return `vision_model: enabledModels(env).vision` (import from ../ |
| F02 | SHIP-BLOCKER | CONFIRMED in source and reproduced live. Source: migration/workers/src/routes/service.ts:82-98 — authClaim, phoneRemove and profileUpsert each verify the token and then unconditionally `return json(503, {ok:false, message:"... not yet ported"})`. index.ts:138-145 dispatches all three paths to these functions. `git log --all -- migration/workers/src/routes/service.ts` shows exactly one commit (5116c2a5, 2026-09-04 10: | Port the three bodies into migration/workers/src/routes/service.ts from backend/pb_hooks/owner_profile_upsert.pb.js (allowlisted eight text fields, 400 messages the contract already pins, read/merge/write on owner_ref derived from the token, echo the canonical row with owner_ref/owner_id/timezone), phone_remove.pb.js (clear owners.phone and every owner_profi |
| F03 | SHIP-BLOCKER | Could not refute; reproduced in source AND in the deployed bundle. 1. Source: migration/workers/src/routes/service.ts:82-98 — authClaim/phoneRemove/profileUpsert each do `verifyToken` then `return json(503, {ok:false, message:"... not yet ported"})`. `git log --all -- service.ts` shows exactly one commit ever (5116c2a5), across every branch including the 16 worktree branches, so no ported version exists anywhere in t | Port the three PocketBase hooks into migration/workers/src/routes/service.ts (backend/pb_hooks/owner_profile_upsert.pb.js 198 lines — read/merge/write of owner_profile keyed on the token's owner id, owner_ref derived from the session, echo the full canonical row as {ok:true, profile:{...}} since AnticipyBackend.swift:410-420 verifies every sent field round-t |
| F04 | SHIP-BLOCKER | Reproduced, not refuted. (1) Source: brain/worker.py:1061-1073 post_event() sets body["owner"]=owner_id or ACTIVE_OWNER_ID; :2555-2557 does the same for uninvited_slot rows; ACTIVE_OWNER_ID comes from ANTICIPY_OWNER_ID (:4576-4583), which the container always sets non-empty at migration/workers/brain/src/index.ts:137 (`env.ANTICIPY_OWNER_ID = legacy \|\| ref`). The Worker's events column map (migration/workers/src/pb | Stop sending `owner` on events rows from the brain: delete the `if legacy: body["owner"] = legacy` at brain/worker.py:1072-1073 and the matching two lines at :2556-2557 (events never had that column on the Worker/D1; _event_matches_owner at :1960-1962 already tolerates a missing event.owner, and owner scoping is done by owner_ref). Redeploy the brain contain |
| F05 | DEFECT | Confirmed in source, not refuted. extension/agent_loop.js:8678-8682 (spawned-tab adoption after a click): `if (loopbackTarget(url) && !allowLoopback) { ...BLOCKED... } else { await navigateWorkingTab(tab.id, url); ... }`. `navigateWorkingTab` (:3462-3469) is a bare `chrome.tabs.update(tabId, { url })` with no refusal check. `loopbackTarget` (:2800-2815) matches only localhost/*.localhost/::1/0.0.0.0/127.x — it does n | In extension/agent_loop.js:8678 replace the spawned-tab guard with the shared gate so all five navigation paths use one check: `const spawnRefusal = navigationRefusal(url); if (spawnRefusal) { history.push(\`step ${step}: BLOCKED UNEXPECTED TARGET — closed ${url.slice(0,120)} because ${spawnRefusal}\`); } else { await navigateWorkingTab(tab.id, url); ... }`  |
| F06 | DEFECT | Reproduced from the checked-in extension (branch jose_anticipy_system, HEAD 98b547b1; #74 fix is 9d83e056). Code: extension/agent_loop.js - :2113-2131 comparisonNames — `^\s*compare\b` + lastIndexOf(" for ") + comma/and split + capitalised-word regex over the OWNER's first sentence. - :2139-2152 completionShapeGap — refuses on (a) any comparisonNames entry not substring-present in the claim, (b) `/\b(provide\|include | Same shape as #65/#74: delete comparisonNames and both branches of completionShapeGap (:2113-2152) and the completionShapeGap call/return in verifyDone (:2544-2546); move the "every named entity present" and "one direct URL per named entity when the goal asks for direct URLs" questions into the auditor prompt as two teaching examples (the auditor already ask |
| F07 | DEFECT | Reproduced from source, and the finding under-states the reach. 1. The picker is a word list. extension/agent_loop.js:5078-5096 (`sideTripDeps.clickText`, blame cf4b5e3f4 2026-08-21, untouched by the 09-05 #78/#79 commit e21e0379): `purpose` is split into words >3 chars and the FIRST `<link\|button\|row\|listitem\|option>` line of `state.elements` whose text `.includes()` any of them is clicked via `trustedClick`. `p | Minimal correct fix: have the model name the row. Extend the list-page judge (or add a second one-token judge, `rowJudge`, in agent_loop.js next to codeJudge) that receives the page-map lines in a fenced block and replies with exactly one `[idx]` of the message that is the code we came for, or NONE; contain it in side_trip.js by checking the idx exists in th |
| F08 | DEFECT | Reproduced in source, not refuted. extension/agent_loop.js:4781-4784 — `cookieLike` is computed from the label plus 1200 chars of any enclosing dialog/aside, and `if (searchLike \|\| calendarLike \|\| cookieLike \|\| choiceLike \|\| disclosureLike) return false;` runs before any commit test. Lines 4791-4803 — inside the per-control loop, `explicitSubmit` (button type=submit inside a form) is computed, then `if (rever | In extension/agent_loop.js:4802 change `if (reversible.test(label)) return false;` to `if (reversible.test(label) && !commit.test(label)) return false;` so a commit verb anywhere in the label ("Continue and pay", "Next: Place order") beats a reversible prefix; apply the same precedence to the cookieLike short-circuit at :4784 (`cookieLike && !commitVerb.test |
| F09 | DEFECT | Could not refute; every factual claim reproduces. 1. The fixes exist only on cloudflare-backend. `git merge-base --is-ancestor` for a4f9f2f1 (09-04 15:17 PDT), c7d4a039 (15:23), 3eb67cec (16:04), 30f3a7dd, 0a21177c: all "no" on jose_anticipy_system, "yes" on cloudflare-backend. jose is 165 commits behind cloudflare-backend. On jose, `app/ios/Anticipy/AnticipyApp.swift` has no `queueLimit`, no `.speechDropped`, no `.a | In research/2026-09-04-omi-port-coverage.md: change row 03 (line 38) to "DONE in repo (a4f9f2f1, c7d4a039, 3eb67cec), NOT LIVE — no TestFlight build from cloudflare-backend yet; lands with build 124 (plan step 6)". Rewrite line 15's "the build these iOS changes went into" to say b122 was built from jose_anticipy_system at f1d59bb1 on 09-02 and predates the f |
| F10 | DEFECT | Reproduced against the real module (extension/agent_loop.js at HEAD, manifest 0.14.0) by importing it through extension/tests/chrome_mock.mjs and driving the exported guards with the kinds Map shape fieldKind() actually reads (`kinds.get(index).kind`). Code: extension/agent_loop.js:909-912 is the 17-word `boundaries` set; :921-922 reads any capitalised word before the matched run that is not in that set as a missing  | Stop letting the `boundaries` word list decide. Minimal law-abiding change in extension/agent_loop.js: keep completeNamedValue only as the T3 TRIGGER (:1857), and at :1588 turn the NAME-kind branch from a decided refusal into a deferred question — push the field onto `deferred` (the same path the native-temporal "defer" uses at :1603-1618) with a one-token j |
| F11 | DEFECT | Reproduced against LIVE, not refuted. 1. Code: overnight/are_the_ears_live.py:271 — `heard = count(f'kind="transcript" && created >= "{since_s}"')`. No device_id / source filter. verdict() at :162-184 returns exit 0 "the ears are alive" whenever `transcripts > 0`. PB resolves to https://api.anticipy.ai (:81-83, and the run banner confirms it). 2. Consumer: overnight/done_gate.py:157-158 — on returncode 0 leg 1 return | In overnight/are_the_ears_live.py, exclude probe traffic from the "heard" half and from `newest('kind="transcript"')`: e.g. `heard = count(f'kind="transcript" && device_id !~ "e2e-" && created >= "{since_s}"')` (PocketBase-style filter the Worker already maps) and the same clause in the newest() call, so the "from <device>" line names a real build. Better st |
| F12 | DEFECT | The symptom is real; the claimed mechanism is wrong, and the ledger already records it. What is true in the source: migration/workers/src/routes/sms.ts:86 returns `json(503, {ok:false, message:"inbound routing not yet ported"})` after signature/account/number checks; index.ts:150 routes POST /sms/inbound to it; the deployed Worker version f909fc2f (2026-09-05 04:42Z, `wrangler deployments list`) is from the tree with | Ship order, not code: do not put build 124 in front of a real owner until plan rows 2, 5, 6, 7, 8 are green against live (sms_reply row on D1 from a real phone, brain answers). Correct the plan's row-2 wording: the 503 stub was never on the live path — Twilio's number is bound to Railway and no Railway->D1 sync exists; the Sendblue webhook on api.anticipy.ai |
| F13 | DEFECT | Could not refute; every link reproduces in source and live. (a) Multipart deposit is dropped. migration/workers/src/index.ts:482-488 readBody returns null unless content-type includes application/json. extension/background.js:1364-1377 (depositEvidence) sends FormData with owner_ref/job/effect_key/image and deletes Content-Type so it is multipart. guard.ts:187 `const b = ctx.body ?? {}` then :173-175 requires `String | Minimal correct fix, in migration/workers/src: 1. index.ts: route `GET /api/files/*` to assets.serveFile(request, env, ownerId) BEFORE the records regex (that is chain step 1 as ARCHITECTURE.md:197 and chain.ts:35 already promise), and add `POST /evidence/share` as a port of backend/pb_hooks/evidence.pb.js:157-227 (service-token only; set share_expires to no |
| F14 | DEFECT | CONFIRMED, not refuted. 1. Response-shape mismatch is real. - migration/workers/src/routes/account_delete.ts:151 returns `{ ok: true, message: "Deleted.", deleted }` -- no `account_deleted`, no `memory_purge`. Its account-survived branch (:141-149) returns 500, not the hook's 409. - backend/pb_hooks/account_delete.pb.js:203-222 returns 409 `{ok:false, account_deleted:false, memory_purge:"waiting on the account closin | In migration/workers/src/routes/account_delete.ts: (a) add `legacy_uuid` to the purges INSERT (:130-132), binding the `legacy` value already read at :91; (b) change the account-survived branch (:141-149) to `json(409, { ok:false, message, deleted, account_deleted:false, memory_purge:"waiting on the account closing" })`; (c) change the success return (:151) t |
| F15 | DEFECT | Could not refute; reproduced on repo and live. 1. Hook exists only on PocketBase: backend/pb_hooks/job_commitment_identity.pb.js:10-19 blanks `commitment_key` on done/failed/cancelled (onRecordCreate + onRecordUpdate). Worker has no equivalent: migration/workers/src/pb/records.ts `create` (:414-470) and `update` (:476-532) copy body fields straight into INSERT/UPDATE; `grep -rn commitment_key migration/workers/src` h | Port the model hook into the Worker write path, then release the live rows, then prove it against live: |
| F16 | DEFECT | Reproduced every load-bearing claim at HEAD f59c26d2 (the finding cited its ancestor f24f9691; nothing relevant changed between them) and against live surfaces on 2026-09-05: 1. Twilio pointer: read-only GET IncomingPhoneNumbers on the account — one number, +16***47, sms_url=https://backend-production-61e0a.up.railway.app/sms/inbound, POST. Railway's /sms/inbound is alive (unsigned POST → 403 = its signature hook is  | Three small things, in this order: (a) After step 2 (sms.ts writes the sms_reply row to D1) deploys — and not before, or texts go from "heard by Railway" to "503 on Cloudflare" — repoint the number once: PUT https://api.twilio.com/2010-04-01/Accounts/{sid}/IncomingPhoneNumbers/{numberSid}.json SmsUrl=https://api.anticipy.ai/sms/inbound (or the Twilio console |
| F17 | DEFECT | CONFIRMED, not refuted. Code: brain/worker.py:450-452 takes ts from profile.get("created"); :469 `fresh = bool(ts) and now - ts <= 3600`; :470-475 not-fresh with no hold appends to welcomed_phones, saves clock_state, returns False with no send and no row. A FAILED send does not stamp (:517-518 `if not anticipy.notify_owner(said): return False` precedes the stamp at :519), and the only other stamp-without-row path (:4 | In brain/worker.py maybe_welcome_new_owner: measure freshness from when the NUMBER arrived, not from signup — use profile["updated"] (the phone save is always an update to the row created by the timezone upsert), and only take the silent-stamp branch (:470-475) when there is evidence the stamp was actually lost (e.g. this owner already has anticipy_says/welc |
| F18 | DEFECT | Reproduced, not refuted. (1) .github/workflows/brain-state-to-r2.yml is invalid YAML: `ruby -ryaml -e 'YAML.load_file(...)'` -> "could not find expected ':' while scanning a simple key at line 76 column 1". Cause is structural: lines 76-78 (`import hashlib,sys` / `print(...)` / `PY`), 92-98 (the sqlite integrity_check heredoc) and 99 (`)`) sit at column 1 inside a `run: \|` block scalar whose content indent is 10 spa | In .github/workflows/brain-state-to-r2.yml, keep every line of the `run: \|` block at >= 10 spaces of indentation so the block scalar stays intact — the runner strips the common indent, so a heredoc terminator written at 10 spaces lands at column 0 of the shell script and still closes the heredoc. Concretely: indent lines 76-78 (`import hashlib,sys`, `print( |
| F19 | DEFECT | Reproduced end to end, nothing in the claim is wrong. 1. Code: overnight/are_the_ears_live.py:184-185 — `if transcripts > 0: return 0, OK, ...`. The `heard` count at :271 is `kind="transcript" && created >= since` with no device_id/source exclusion; `newest('kind="transcript"')` at :291 likewise. The only device filter in the file is the control half (`device_id="anticipy-brain"`, :90/:272). 2. Probe stamp: proof/e2e | In overnight/are_the_ears_live.py, exclude synthetic probe rows from the ears half (provenance filter, Law-1-legal): - :271 `heard = count(f'kind="transcript" && created >= "{since_s}" && device_id !~ "e2e-"')` (or, if the Worker's filter parser lacks `!~`, compute `count(total) - count(... && device_id ~ "e2e-")`). - :291 `last = newest('kind="transcript" & |
| F20 | DEFECT | Reproduced from live surfaces and the repo, 2026-09-05 16:52Z. Both brains are running against the same real owner with the same Twilio credentials, and the ledger's stated reason for tolerating it is no longer true. 1. Railway supervisor is alive, not stopped. `railway status` (project anticipy-production): `worker: ● Online · worker-volume`. `railway logs -s worker` (65 lines): `worker up · llm=live:google/gemini-2 | Two decisions, both recorded in repo files the day they are made (law 4): (1) Declare R2 `anticipy-owner-state` the source of truth for every owner the container has served since 13:50Z, and make brain-state-to-r2.yml refuse to overwrite: before the `wrangler r2 object put` at line 92, HEAD the key via the Cloudflare API and abort (exit 1, no put) if its las |
| F21 | GAP | Reproduced the red leg, but the blast radius is overstated. `sh app/ios/Tests/run_build_number_tests.sh` exits 2 in the tree with exactly the claimed message: "The iOS source has changed since build 124 was set ... app/ios/Anticipy/AnticipyApp.swift \| 2 +-". `git log -G'CURRENT_PROJECT_VERSION: "'` puts the 124 bump at 0a21177c (2026-09-05 01:45 -0700); `git diff 0a21177c -- app/ios/Anticipy/AnticipyApp.swift` is th | Bump `CURRENT_PROJECT_VERSION` in app/ios/project.yml from "124" to "125" and add a comment line ("125: expectedExtensionVersion 0.14.0 — the stale-extension nag tracks the Cloudflare-default extension"); run `cd app/ios && xcodegen generate` so the pbxproj follows; change research/2026-09-05-cloudflare-era-plan.md:59 to "iOS build 125"; confirm `sh app/ios/ |
| F22 | GAP | Reproduced, not refuted. app/ios/scripts/app_store_connect.py:133-135 `next_build_number` is `max([source, *numeric]) + 1` — it unconditionally adds one, and `live_next_build` (line 169-170) returns `source + 1` even when Apple holds no builds. Simulated: next_build_number(['122','123'],124) = 125, (['123'],124) = 125, ([],124) = 125. The workflow (.github/workflows/ios-testflight.yml:48-58) then runs `xcrun agvtool  | In app/ios/scripts/app_store_connect.py: `next_build_number` should return `source` when it already exceeds every live number and only bump on collision — `return max([source, *(n + 1 for n in numeric)])`; `live_next_build` should return `source` (not `source + 1`) when no pre-release version exists. In ios-testflight.yml, only run `agvtool new-version` when |
| F23 | GAP | Source: app/ios/Anticipy/Backend/AnticipyBackend.swift:717-720 builds `["device_id": deviceID, "kind": kind, "text": text, "decision": decision ?? "", "goal": goal ?? ""]` unconditionally; the same line is at 0266a0fb:36 and db43db14:719 (TestFlight 123 bytes). No other Swift file posts "decision" (grep over app/ios, Tests excluded). Conditional keys in pushEvent are parent_line, speaker, explicit, source, external_e | Correct the attribution in three places, no code change: (1) migration/workers/src/pb/records.ts:92-93 — replace "The phone never sends `decision` or `goal` (AnticipyBackend.swift sets keys conditionally)" with "The phone always sends decision:\"\" and goal:\"\" (AnticipyBackend.swift:717-720); the NULL row was a hand-made POST that omitted the key. Any othe |
| F24 | GAP | Reproduced in extension/agent_loop.js (shipping in extension 0.14.0 per manifest.json). All six regexes are present at the cited lines and read `verdict.reason` as free text: `outputOnlyCompletionGap` :2155-2158, `missingCompletionEvidence` :2168-2171, `nonAuthoritativeCompletionEvidence` :2173-2176, inline `/load\|spinner\|progress\|wait/i` :7511 (sleeps 5000 ms then calls verifyDone a second time), `/not (?:present | Add a structured `gap` enum to the verifier contract — e.g. `still_loading \| missing_on_page \| source_unvisited \| non_authoritative \| result_omits \| contradiction` — returned by the auditor model in its JSON (extend the prompt at ~:2664 and extractVerifierVerdict :530) and set directly by the deterministic gap functions in verifyDone (they already know  |
| F25 | GAP | Reproduced, and the truth is slightly worse than the sweep said. 1. The instrument reads Railway only. overnight/is_the_gateway_live.py:114 runs `["railway", "logs", "-s", service, ...]`; `grep -rn wrangler overnight/` returns nothing. Running it now: `[....] log lines read from worker 60 / [FAIL] 1 THE BANNER the newest banner carries no fallback= field — a worker from before port 09b is what is running / exit=1`. T | (a) Put GEMINI_API_KEY on the anticipy-brain worker (`wrangler secret put GEMINI_API_KEY --config migration/config/wrangler.brain.jsonc`); it is already in FORWARD_KEYS, so no code change is needed for fallback to become real. (b) In overnight/is_the_gateway_live.py, make fetch_messages read the Cloudflare brain's container log (`npx wrangler tail anticipy-b |
| F26 | GAP | Reproduced on branch cloudflare-backend (the checked-out branch; jose_anticipy_system has neither the fence nor the ledger's 156 added lines, so the inconsistency lives inside one branch). (1) research/2026-09-04-omi-port-coverage.md:140-144 still says "Prompt-injection fencing at triage (10b)... The memory sinks are already fenced; triage is not." The file contains no other mention of the fence or of 75ba42cec (grep | Edit research/2026-09-04-omi-port-coverage.md on cloudflare-backend: (a) replace ranked item 3 with a DONE line — "Triage injection fence: `75ba42cec`, brain/orchestrator.py:32-41, in the deployed brain image (run 33966119164, head 69eac667); no live/eval leg yet" — and add a `75ba42cec` bullet under "What was actually changed"; (b) rewrite items 1, 2, 4 to  |
| F27 | GAP | Reproduced, could not refute. Repo: backend/pb_hooks/audit_retention.pb.js:71-83 is an onRecordAfterCreateSuccess hook on agent_llm_audit that trims to AUDIT_KEEP=300 (25 per fire); evidence.pb.js:244-269 trims evidence to 20/owner, 60 total. In the Worker, migration/workers/src/llm.ts:311-313 says "KEEP audit_retention's sweep" but auditBegin (llm.ts:376-398) is a bare INSERT and auditFinish (:406-431) a bare UPDATE | In migration/workers/src/cron.ts prune(), append two bounded statements to the existing batch: `DELETE FROM agent_llm_audit WHERE id NOT IN (SELECT id FROM agent_llm_audit ORDER BY created DESC LIMIT 300)` (the exact SQL routes/service.ts:75-78 already has — lift it into a shared helper) and the evidence cap `DELETE FROM evidence WHERE id NOT IN (SELECT id F |
| F28 | GAP | CONFIRMED, not refuted. (1) brain/container_entry.py:129-131 returns True for any HTTPStatusCode 404 before looking at the Error Code, and :133 also accepts Code "404". (2) Live reproduction with the .env.local R2 credentials (R2_ENDPOINT/R2_ACCESS_KEY_ID/R2_SECRET_ACCESS_KEY, boto3 1.42.97, values never printed): `download_file("anticipy-owner-state", ...)` -> ClientError Code=404 HTTP=404, `_is_not_found` = True; ` | In brain/container_entry.py pull_state(), before the per-file loop, prove the bucket exists with the credentials in hand and abort on anything else: `s3.head_bucket(Bucket=R2_BUCKET)` wrapped so that ANY exception raises RuntimeError("owner-state bucket {R2_BUCKET} is not reachable with these credentials — refusing to boot on an empty dir; underlying: {err!r |
| F29 | GAP | Reproduced end to end from this laptop, no phone. (1) No gate leg covers the R2 half of memory: `grep -rniE "anticipy-owner-state\|r2 object\|snapshot" overnight/*.py` hits only overnight/consolidation_gate.py:121-134, which globs `state_root()/*/memory.db` on the LOCAL host — it never reads R2, so on Cloudflare it measures nothing durable. (2) The ledger line is as quoted: research/2026-09-05-hands-live-run.md:174 ` | Add one overnight leg, e.g. overnight/is_memory_durable.py: for each served owner (D1 `owners` rows passing the index.ts:183-189 filter, plus ANTICIPY_SERVE_OWNERS), fetch `anticipy-owner-state/owners/<ref>/memory.db` via `wrangler r2 object get … --remote --file` (never without --remote), read `max(ts) FROM episodes`, and compare against D1 `MAX(updated) FR |
| F30 | GAP | Reproduced. `python3 overnight/tejas_gate.py` today prints `[6] FAIL THE SPEAKER TAGGER IS LINKED ... speaker stays empty on every event until somebody adds the package product to the target (build 76)` and closes with `NOT DONE - first failing leg: 6`. The leg (overnight/tejas_gate.py:390-404) reads app/ios/Anticipy.xcodeproj/project.pbxproj, where every `packageProductDependencies = ( );` is empty (pbxproj:445, 462 | Keep the leg red (Law 3: do not soften a predicate to reach green) but make its failure text agree with the ledger. In overnight/tejas_gate.py:396-399 replace the LegFailed message with something like: "packageProductDependencies is EMPTY: speaker engine unlinked, speaker stays empty on every event. RED BY DESIGN — do NOT re-link sherpa-onnx/onnxruntime-libs |
| F31 | GAP | Reproduced, not refuted. (1) .github/workflows/system-invariants.yml:3-5 — `on: push: branches: [jose_anticipy_system]` plus `pull_request`; HEAD is `cloudflare-backend` at 51e79566, 170 commits past merge-base 98b547b1 with jose_anticipy_system (13 commits touching brain/, 120 files / +19904 −2780 across brain/ tests/ extension/ backend/ overnight/). (2) `gh run list --workflow=system-invariants.yml`: every run is h | In .github/workflows/system-invariants.yml line 5 change `branches: [jose_anticipy_system]` to `branches: [jose_anticipy_system, cloudflare-backend]` (or `branches: ['**']`) and push, so HEAD gets a 3.11 run; additionally add a `python -m pytest -q` (3.11) step to .github/workflows/brain-deploy.yml before the wrangler deploy step so the Containers image cann |
| F32 | GAP | Reproduced live at 2026-09-05T16:55Z: `npx wrangler secret list --name anticipy-brain` returns exactly 11 names (ANTICIPY_BACKUP_S3_{ACCESS_KEY,BUCKET,ENDPOINT,SECRET}, ANTICIPY_SERVICE_TOKEN, BRAVE_API_KEY, OPENROUTER_API_KEY, TAVILY_API_KEY, TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_PHONE_NUMBER). GEMINI_API_KEY is absent; the runbook migration/BRAIN-ON-CONTAINERS.md:259-264 lists it among the 17 secrets to `wr | Ops + two config lines, then prove it: (1) `npx wrangler secret put GEMINI_API_KEY -c migration/config/wrangler.brain.jsonc`; (2) add `"ANTICIPY_LLM_ORDER": "openrouter,gemini"` to the `vars` block in migration/config/wrangler.brain.jsonc so the live primary stays deepseek/deepseek-v3.2 and Gemini is the backup (as overnight/is_the_gateway_live.py:52-55 pres |
| F33 | GAP | Reproduced, and the truth is worse than the sweep's "unmeasured". 1. The gate reads the wrong surface, as claimed. `overnight/is_the_gateway_live.py:111-117` shells `railway logs -s worker`; running it today gives `[FAIL] 1 THE BANNER — the newest banner carries no fallback= field`. The newest Railway banner is `worker up · llm=live:google/gemini-2.5-flash · ... · pb=https://backend-production-61e0a.up.railway.app ·  | (a) `wrangler secret put GEMINI_API_KEY` on anticipy-brain, and set `ANTICIPY_LLM_ORDER=openrouter,gemini` in wrangler.brain.jsonc vars (the default order gemini,openrouter would silently demote ANTICIPY_MODEL=deepseek to fallback; index.ts:55-61 must forward ANTICIPY_LLM_ORDER too). (b) Make the boot line reachable from a terminal: have brain/container_entr |
| F34 | GAP | Reproduced live 2026-09-05. `npx wrangler tail anticipy-brain --config migration/config/wrangler.brain.jsonc --format json` for 150 s, while `wrangler containers list` showed anticipy-brain-owner with 9 LIVE INSTANCES (image sha256:5909f7f6..., observability.logs.enabled=true): 13 events total — 7 OwnerBrain `rpcMethod: ensure` (logs: [], wallTime ~60 s each), 4 OwnerBrain alarms (logs: []), 2 BrainSupervisor cron ti | Give the banner a CLI-readable home and point the legs at it: have brain/worker.py main() (or brain/container_entry.py) write its startup line — sms vendor, brain fingerprint, image/commit, ANTICIPY_PB, llm primary/fallback — as one row to D1 through the API it already polls (a `worker_status` row keyed by owner_ref, refreshed with the gateway tally on each  |
| F35 | NOTE | Core claim reproduces, but three of the finding's specifics are wrong and the ledger is not contradicted the way the finding says. CONFIRMED: `gh run view 33945943140` → event=push, headBranch=jose_anticipy_system, headSha=db43db14, conclusion=success; its log reads "Source build 121; App Store Connect requires 123" and "UPLOAD SUCCEEDED with no errors / Delivery UUID 6320875b-…" at 05:00:31Z. db43db14 is "iOS: point | Ledger fix, not code: add one row to research/2026-09-05-cloudflare-era-plan.md's measured table — "TestFlight latest = 123, from jose_anticipy_system@db43db14 (source 121), Cloudflare-pointed but pre-122/123/124 fixes; do not install it as 'the Cloudflare build'" — and note in the plan (line 124/188) that `gh workflow run "iOS TestFlight" --ref cloudflare-b |
| F36 | NOTE | Reproduced. extension/agent_loop.js:6153-6161 — two regexes over `goal` (`bank\|brokerage\|credit card\|crypto…` AND `log in\|sign in\|password\|statements\|transfers\|balance\|accounts`) return `{status:"needs_user", result:"refused: operating financial accounts is protected…"}` before any tab is opened. Ran both regexes in node: "Update the bank account on my payroll portal" → true (refused), "Log in to the gym sit | Either (a) file it in the law-1 ledger as LEGAL — seatbelt-with-cost, recording the two measured false refusals, or (b) replace the wording test with a check on what the plan touches (the planner's declared touches / BLOCKED_DOMAINS at agent_loop.js:2786), and keep a `TAPE:` comment + tape_gate leg until (b) lands. Either way it must be listed; unlisted is t |
| F37 | NOTE | Reproduced, not refuted. (1) Live D1: `SELECT name FROM pragma_table_info('events')` returns 25 columns ending at `importance` — no `heard_ms`, no `heard_calls`. (2) `python3 overnight/is_the_decision_bounded.py` against https://api.anticipy.ai: "decided transcript rows in the last 24h 13 (0 measured, 13 not)" → UNPROVEN, so a deployed worker IS stamping decisions. (3) brain/worker.py:3946-3978: the stamp sends heard | In research/2026-09-04-omi-port-coverage.md row 06, replace "`is_the_decision_bounded.py` UNPROVEN until a deployed worker stamps a row" with "`is_the_decision_bounded.py` UNPROVEN until the D1 ALTER adds heard_ms/heard_calls to `events` — the deployed brain already stamps decisions (13/24h on 2026-09-05) and drops the measurement on the Worker's 400 (worker |
| F38 | NOTE | Confirmed the mechanism, with one correction to the claim's details. .github/workflows/ios-testflight.yml:2-5 triggers on push to `jose_anticipy_system` (paths app/ios/**) plus workflow_dispatch; the "Bump build number above Apple's latest" step (lines 49-58) numbers every upload as Apple's latest + 1 regardless of source. `git rev-list --left-right --count origin/jose_anticipy_system...origin/cloudflare-backend` = 2 | In .github/workflows/ios-testflight.yml line 4 change `branches: [jose_anticipy_system]` to `branches: [cloudflare-backend]` (or delete the push trigger and keep workflow_dispatch only), commit on cloudflare-backend before dispatching build 124. |
| F39 | NOTE | Reproduced from source (line numbers differ slightly from the sweep's cite but the substance holds). Hook backend/pb_hooks/password_reset.pb.js:153-155 sends `<code> is your Anticipy code to set a new password. It works for 10 minutes. If you didn't ask for this, ignore it and your password stays as it is.`; its header comment at :20-21 states this is deliberate ("The message names the app and says plainly that it wa | In migration/workers/src/routes/password_reset.ts:106 restore the hook's wording: `${code} is your Anticipy code to set a new password. It works for 10 minutes. If you didn't ask for this, ignore it and your password stays as it is.` Optionally align :213 to "Done — sign in with your new password." Coordinate with the Sendblue agents since they are touching  |
| F40 | NOTE | Source diff confirmed: migration/workers/src/routes/agent.ts:117-120 INSERTs only (id, agent_id, agent_token, pair_code, paired, created, updated); backend/pb_hooks/agent_auth.pb.js:59-60 additionally sets browser (body.browser, sliced to 500) and last_seen (server now). The extension does send both in the register body (extension/background.js:160-171). So the row is born with NULL browser/last_seen on the Worker an | Parity-only, optional: in agent.ts agentRegister, bind browser and last_seen in the INSERT — `INSERT INTO agents (id, agent_id, agent_token, pair_code, paired, browser, last_seen, created, updated) VALUES (?,?,?,?,?,?,?,?,?)` with `String(b.browser ?? "").slice(0, 500)` and `pbNow()`. Otherwise none needed. |
| F41 | NOTE | Could not refute; every claim reproduces. Stubs (read from source): - migration/workers/src/routes/agent.ts:177-178 — agentCaptcha: `if (!env.CAPSOLVER_API_KEY) return json(503, "captcha solving is not configured"); return json(503, "captcha solving not yet ported");` — unconditional 503 even when configured. - agent.ts:190-197 — agentUpgradeCredential: after constant-time service-token check, `return json(503, "cred | No shipping fix needed for build 124 / 0.14.0. Record it: add a row to research/2026-09-05-cloudflare-era-plan.md listing /agent/solve-captcha (503 stub, CAPSOLVER_API_KEY unbound), /agent/upgrade-credential (503 stub behind service-token check), and /transcription/token (blanket 401, no caller) as OPEN-on-Worker so L4 holds and a later port of CAPTCHA solvi |
| F42 | NOTE | Confirmed from source, could not refute. migration/workers/src/policy/workflow_guard.ts:288 `String(body.status ?? oldStatus ?? "")`, :361 `String(body.lineage_key ?? old?.lineage_key ?? "")`, :451 `JSON.parse(String(rowValue("approval", "") ?? ""))` where rowValue (:302) is `body[name] != null ? body[name] : old[name]`. Oracle backend/pb_hooks/workflow_guard.pb.js:28 `String(body.status \|\| oldStatus \|\| "")`, :11 | Make the three fallbacks treat "" as absent, matching the oracle: workflow_guard.ts:288 `String(body.status \|\| oldStatus \|\| "")`; :361 `String(body.lineage_key \|\| old?.lineage_key \|\| "")`; :451 `JSON.parse(String(body.approval \|\| old?.approval \|\| ""))`. Alternatively, if the stricter Worker behaviour is wanted, change the oracle to match and drop |
| F43 | NOTE | Reproduced from source; could not refute. brain/container_entry.py:172 calls `backup_state_to_s3(STATE_ROOT)` with no env override; brain/state_backup.py:162-163 builds the key as `<ANTICIPY_STATE_BACKUP_PREFIX>/state-<UTC stamp>.zip` with no owner component, and :189 `_prune(s3, bucket, prefix, keep)` deletes everything past the 14 newest `.zip` under that one prefix (:144-150). The DO forwards the prefix unchanged  | In brain/container_entry.py, pass a per-owner prefix so the prune ring is per owner: build `env = dict(os.environ); env["ANTICIPY_STATE_BACKUP_PREFIX"] = f"{os.environ.get('ANTICIPY_STATE_BACKUP_PREFIX','worker/').strip('/')}/{OWNER_REF}/"` and call `backup_state_to_s3(STATE_ROOT, env=env)` (and zip STATE_ROOT.parent, i.e. /data, so the archive is rooted at  |
| F44 | NOTE | Reproduced, not refuted. (1) Live D1 (`SELECT name FROM pragma_table_info('events')`, read-only, served_by v3-prod): 25 columns, no `heard_ms` and no `heard_calls`; `WHERE name LIKE 'heard%'` returns 0 rows. (2) `python3 overnight/is_the_decision_bounded.py` against https://api.anticipy.ai on 2026-09-05: "decided transcript rows in the last 24h 13 (0 measured, 13 not)" ... "13 decided row(s) and not one carries a mea | None needed in code. To turn the leg green the owner runs the pending ALTER on live D1 (`ALTER TABLE events ADD COLUMN heard_ms REAL NOT NULL DEFAULT 0; ALTER TABLE events ADD COLUMN heard_calls INTEGER NOT NULL DEFAULT 0;` — the shapes in migration/d1/schema.sql:243-244), restarts the brain container so `_HEARD_COLUMNS_ACCEPTED` resets to True (brain/worker |
