# Anticipy Bug Hunter Log

Append-only log of bugs found by the continuous bug hunter. No fixes are applied here. Other agents pick from this list.

Started: 2026-05-30
Hunter: bug-hunter agent (no fixes, no source code touched)

## Findings

| ID | Found | Severity | Surface | Title | Repro | Evidence | Suggested fix |
|---|---|---|---|---|---|---|---|
| B001 | 2026-05-30T16:14Z | P0 | website /dl/Anticipy.dmg | DMG Content-Length mismatch (stale prod value, off by 394,253 bytes) | curl -sI https://www.anticipy.ai/dl/Anticipy_1.0.0_aarch64.dmg returns content-length 2515666283; curl -sI https://pub-e97c6305fe2949d8a5d17885f7be2a0e.r2.dev/Anticipy_1.0.0_aarch64.dmg returns 2516060536. Local route.ts has uncommitted edit to 2516060536. Installers comparing HEAD vs real bytes will fail integrity check. | route.ts L14 DMG_BYTES; git diff src/app/dl/Anticipy_1.0.0_aarch64.dmg/route.ts | Deploy the diff in src/app/dl/Anticipy_1.0.0_aarch64.dmg/route.ts (DMG_BYTES already updated locally) OR delete the HEAD handler entirely and let Vercel proxy to R2 so it always matches reality. |
| B002 | 2026-05-30T16:14Z | P1 | engine /api/recovery/test | Endpoint accepts arbitrary failure_kind values silently, returning generic SMS body | curl -X POST -H 'Content-Type: application/json' -d '{"failure_kind":"NOT_A_REAL_KIND"}' http://127.0.0.1:8731/api/recovery/test returns ok:true with sms_body='Anticipy paused the task...' identical to known-good network kind. Allows arbitrary string input to be accepted as a recovery scenario, hiding test bugs. | endpoint /api/recovery/test response above | Validate failure_kind against the known enum (network, captcha, login_wall, paywall, dom_drift, etc.) and return 422 for unknown values. |
| B003 | 2026-05-30T16:14Z | P1 | engine /api/listen/inject | SQL-ish payloads classified as LIFE_LOG and queued as task_id; no obvious sanitization or fingerprint logging | curl -X POST -H 'Content-Type: application/json' --data-raw "{\"text\":\"x' OR 1=1; DROP TABLE users; --\"}" http://127.0.0.1:8731/api/listen/inject returns outcome:LIFE_LOG with task_queue_id assigned. SQL-injection-style strings reach planner without flagging. | response above shows task_queue_id assigned to malicious-looking payload | Add a minimal injection-pattern detector (semicolons, quote-then-DROP, --, OR 1=1) and either reject or quarantine. Even if SQLite uses parameterized queries downstream, hostile strings should not become LLM prompts unsanitized. |
| B004 | 2026-05-30T16:14Z | P2 | engine /api/listen/inject | Empty text passes through and increments window counter without warning | curl -X POST -H 'Content-Type: application/json' -d '{"text":""}' http://127.0.0.1:8731/api/listen/inject returns ok with outcome:null, window incremented. Wastes API quota tracking + pollutes logs. | response shows window:2 and ingest_id assigned for empty text | Reject empty/whitespace-only text with 400 OR drop silently without bumping window. |
| B005 | 2026-05-30T16:14Z | P2 | engine /api/coldstart/start | Coldstart returns ok:true,started:true then immediately fails on status check; no signal to caller of inevitable failure | POST /api/coldstart/start returns started:true; immediate GET /api/coldstart/status returns state:failed with errors=['loopback bridge or Chrome CDP not available']. False-positive started signal misleads UI. | response chain above | If bridge_ready=false at start time, return started:false with reason in start response instead of misleading started:true. |
| B006 | 2026-05-30T16:14Z | P3 | repo cleanup | Stale installer/install.sh on disk diverges from public/install.sh (live) | /Users/omarebrahim/Developer/Anticipy-V7/installer/install.sh (8493 bytes, v6 native bridge era) vs /Users/omarebrahim/Developer/Anticipy-V7/public/install.sh (9630 bytes, current). Risk: future scripts pick up wrong installer. | diff /tmp/bh-installsh-live.sh /Users/omarebrahim/Developer/Anticipy-V7/installer/install.sh shows complete divergence | Delete installer/install.sh OR replace with a symlink to public/install.sh. |
| B007 | 2026-05-30T16:19Z | P1 | install.sh | Script claims ~2.7 GB download but DMG is actually 2.34 GB. | `grep '2.7 GB' /Users/omarebrahim/Developer/Anticipy-V7/public/install.sh` line 207: 'downloading the real app (~2.7 GB; includes the local speech model)' vs real R2 size 2,516,060,536 bytes = 2.34 GB. | install.sh L207; HEAD https://pub-e97c6305fe2949d8a5d17885f7be2a0e.r2.dev/Anticipy_1.0.0_aarch64.dmg shows 2516060536 | Update text to '~2.3 GB' or pull size from real download HEAD at install time. |
| B008 | 2026-05-30T16:19Z | P1 | engine /api/onboarding/answer | Negative or out-of-range index values accepted silently and auto-incremented. | `curl -X POST -d '{"index":-1,"answer":""}'` http://127.0.0.1:8731/api/onboarding/answer returns `index:1,total:7` (started from -1, somehow returned to 1). `curl -X POST -d '{"index":99999,"answer":"test"}'` returns `index:2,total:7`. State is mutated regardless of input. Empty answer still accepted as valid step progress. | repro shown | Validate `index >= 0 && index < total`; reject empty answer with 400. |
| B009 | 2026-05-30T16:19Z | P2 | engine /api/listen/upload | Error message for malformed WAV leaks full /var/folders path (privacy/info leak). | curl -X POST -F audio=@/tmp/bh-empty.wav http://127.0.0.1:8731/api/listen/upload returns error string containing full tempdir path: '/var/folders/y0/kpml_mf10911r9vt3n1gqt900000gn/T/anticipy-upload-...'. Reveals macOS user folder hash. | response shown | Strip /var/folders/... prefix and substitute friendly 'audio decode failed' message. Log full path internally only. |
| B010 | 2026-05-30T16:19Z | P1 | engine memory | RSS at 615 MB after 6.5 min uptime, far above the 200 MB/1h ceiling. | `ps -o rss,etime,pid -p $(curl -s http://127.0.0.1:8731/health | jq -r .pid)` returned RSS=630224 (615 MB) at etime 06:43. Spec from runbook is <200 MB after 1h. Probe is idle (no real ASR processing during measurement window). | ps output | Profile parakeet model load + audio buffer churn. Likely the model stays resident at ~500 MB. Document the realistic baseline or reduce model footprint. |
| B011 | 2026-05-30T16:19Z | P1 | website homepage | Title is 'Anticipy App' on the marketing root page (and on /app and /flash too). | `curl -s https://www.anticipy.ai/ | grep '<title>'` returns `<title>Anticipy App</title>`. Same for /app and /flash. Should be brand value proposition for SEO, not 'App'. | curl output | Set distinct titles per route. Homepage: e.g. 'Anticipy: AI Pendant That Listens'. /app: 'Anticipy App'. /flash: 'Anticipy Connect Pendant'. |
| B012 | 2026-05-30T16:19Z | P1 | website homepage | Top-nav 'Anticipy' brand link href='#' is a dead link, and Product/How It Works/Privacy/Pricing nav items are <button> with no JS-bound action visible. | `grep '<a href="#"' /tmp/bh-home.html` shows brand link href is '#'. Nav buttons have no onclick handlers visible in HTML. | grep output | Brand href should be '/'. Nav items should be <a> tags with anchor targets (e.g. #pricing) or routed pages. |
| B013 | 2026-05-30T16:19Z | P1 | website | Homepage meta description points to /app instead of the marketing pitch. | `curl -s https://www.anticipy.ai/ | grep -oE 'description"[^"]*"[^"]*"' | head -1` returns 'Open Anticipy, install the local Mac engine, and connect the private on-device assistant to the public app shell.' which is /app's copy, not the homepage's pitch. Hurts SEO + social previews. | curl output | Set distinct meta description per route in src/app/page.tsx vs src/app/app/page.tsx. |
| B014 | 2026-05-30T16:19Z | P2 | engine /api/risk/assess | Money parser ignores 'million'/'thousand' qualifiers and returns numeric stem only. | `curl -X POST -d '{"intent":"transfer $1 million to my mom"}'` http://127.0.0.1:8731/api/risk/assess returns `money_amount:1.0`. Risk is still 'high' but the displayed amount is wrong (1 vs 1,000,000). User-facing confirm card will show $1 not $1M. | repro | Parse 'million', 'thousand', 'M', 'k' suffixes. |
| B015 | 2026-05-30T16:19Z | P2 | engine /api/risk/assess | Tiny amounts ($0.05) trigger high risk + confirm card; below-floor amounts should be silent per cost ceiling spec. | `curl -X POST -d '{"intent":"$0.05 buy book"}'` returns `level:high, mode:confirm, money:0.05`. Spec says $0.002/task ceiling - a $0.05 buy is still well within auto-mode. | repro | Add money threshold: if money_amount <= some_floor (e.g. $1), do not bump risk to high. |
| B016 | 2026-05-30T16:19Z | P3 | engine /api/listen/upload | ASR normalizes 'one two three' to '123' (numerals) and adds capitalization+punctuation. Could affect downstream string matching that expects raw transcription. | input WAV said 'hello bug hunter probe one two three'; transcript returned 'Hello Bug Hunter Probe 123.' | response from upload | Document the normalization. Provide raw transcript via separate field (already in raw_asr_transcript, but identical here). |
| B017 | 2026-05-30T16:19Z | P2 | engine /api/act | Duplicate task enqueue on every clarify-needed call; queue fills up with identical 'draft a thank-you email to Omar' entries. | `curl http://127.0.0.1:8731/api/task_queue/list` returned count=7 with multiple identical instruction='draft a thank-you email to Omar' waiting tasks (5 with same instruction). Each /api/act or inject creates a fresh task even though all need same clarification. | repro: hit /api/act with same payload 5 times | Dedupe pending clarification tasks by (account_id, instruction_hash). |
| B018 | 2026-05-30T16:19Z | P3 | engine /api/audio/devices | Two BlackHole 2ch devices reported with is_default:true; engine then binds to the virtual loopback instead of MacBook Air Microphone. | `curl http://127.0.0.1:8731/api/audio/devices` returns indices 1 and 4 both BlackHole 2ch with is_default:true. listen/start picks BlackHole even though built-in mic (index 2) is available. | response | Only one device may be the system default. Prefer built-in mic when BlackHole is virtual loopback. Or warn user. |
| B019 | 2026-05-30T16:19Z | P2 | engine /api/listen/inject | Stale 'pending' instruction persists across unrelated injects; affects what the user sees as their current intent. | After injecting 'draft a thank-you email to Omar', subsequent injects (empty, SQL-ish, etc.) all return `pending.instruction='draft a thank-you email to Omar'` from minutes prior. | response from multiple injects | Clear pending when new actionable utterance arrives OR after TTL. |
| B020 | 2026-05-30T16:19Z | P2 | engine /api/coldstart/status | Reports state 'failed' but ok:true; ambiguous error semantics for a UI to render. | `curl http://127.0.0.1:8731/api/coldstart/status` returns `{"ok":true,"state":"failed","errors":["..."]}`. ok=true + state=failed contradicts. UI may show success badge while data is broken. | response | If state == failed, ok should be false. |
| B021 | 2026-05-30T16:24Z | P0 | engine startup | Bundled engine logs `JWT_SECRET not set — using insecure development default. Set this in .env.` and `PROFILE_ENCRYPTION_KEY not set — using derived development key.` at every startup. | /tmp/integration-walker-1780157314/home/.anticipy/product-engine.log shows both warnings on startup. The bundled DMG ships these defaults; stranger installs will run with insecure JWT signing + derived crypto key for browser-profile cookies. | log file | Inject random JWT_SECRET on first run, persist to ~/.anticipy/secrets.json with 0600 perms. Same for PROFILE_ENCRYPTION_KEY. Refuse to start if either is unset AND no auto-generation happens. |
| B022 | 2026-05-30T16:24Z | P0 | Mac app /Applications/Anticipy.app | Info.plist declares only NSMicrophoneUsageDescription. Engine advertises surfaces `native_ax`, `terminal`, `notification`, `screenshot`, `file_system` but app is missing NSScreenCaptureUsageDescription, NSAppleEventsUsageDescription, NSInputMonitoringUsageDescription, NSCalendarsUsageDescription, NSContactsUsageDescription, NSRemindersUsageDescription. | grep '^.*Usage' /tmp/integration-walker-1780157314/Anticipy.app/Contents/Info.plist returns only NSMicrophoneUsageDescription. /api/state surface_runtime.surfaces lists native_ax, terminal, notification. | First permission dialog macOS pops will be a generic message or fail silently; user has no idea why Anticipy needs the capability. | Add explicit, friendly usage strings to Info.plist for each Apple-gated capability the engine will use. |
| B023 | 2026-05-30T16:24Z | P1 | Mac app codesign | App bundle identifier in codesign is `desktop-ac0d21f116671b6b` (Tauri default) while Info.plist correctly says `ai.anticipy.app`. spctl reports 'code has no resources but signature indicates they must be present'. | `codesign -dvvv /tmp/integration-walker-1780157314/Anticipy.app` returns Identifier=desktop-ac0d21f116671b6b; spctl -a -v outputs the mismatch error. | Mismatch breaks Keychain access scoping, breaks future notarization, breaks update mechanisms that pin to bundle identifier. | Set bundle.identifier in tauri.conf.json to ai.anticipy.app before signing. Re-sign with --identifier ai.anticipy.app. |
| B024 | 2026-05-30T16:24Z | P0 | engine stability | Engine 89677 (integration walker bundled engine) died silently after ~11 minutes of light probing with no entry in product-engine.log. Probe traffic = ~30 GET/POST against documented endpoints. No coredump, no panic line. | `lsof -i :8731` showed engine listening; after ~5 minutes of additional probes it was gone with no log entry. Last log line was startup, no shutdown log. | Process 89677 disappeared from `ps`; port 8731 became free. | Add crash signal handlers (SIGSEGV/SIGABRT) that flush to log before exit. Also write a heartbeat to log every 60s so post-mortem can tell whether engine was alive at time T. |
| B025 | 2026-05-30T16:24Z | P2 | engine /api/sms/inbound | When called with NO body returns response that implies a pending action exists ('Reply YES to send, NO to cancel, EDIT to revise.'); when called WITH body returns 'no pending action'. Logic order is inverted. | curl -X POST http://127.0.0.1:8731/api/sms/inbound returns 'did not recognise that' (implies pending). curl -X POST -d 'From=&Body=X' returns 'no pending action to confirm'. Reverse of expected. | repro | Check pending state FIRST; if no pending, return 'no pending action'. If pending exists but body doesn't match YES/NO/EDIT, return 'did not recognise'. |
| B026 | 2026-05-30T16:24Z | P3 | engine source | server.py uses deprecated FastAPI `@on_event` decorator in 7 locations (lines 691, 715, 2778, 2807, 2849, 11743, 11773) - generates DeprecationWarnings on every startup. Will break on FastAPI removal. | /tmp/integration-walker-1780157314/home/.anticipy/product-engine.log shows 7 'on_event is deprecated' warnings. | Migrate to lifespan context manager (FastAPI Lifespan Events docs). |
| B027 | 2026-05-30T16:24Z | P2 | engine /api/dossier | All dossier endpoints return mock_mode:true; events:[]; dossier_writes:[] (no real writes even when /api/listen/inject produced LIFE_LOG outcomes). | curl http://127.0.0.1:8731/api/dossier/events returns mock_mode:true with empty arrays after ~5 LIFE_LOG-producing injects. | response | If mock_mode is intentional in unprovisioned state, expose `mock_mode_reason` so UI can show 'sign in to enable'. Otherwise wire real persistence in dossier_router. |
| B028 | 2026-05-30T16:24Z | P3 | engine /api/dossier/inbound | Returns TwiML `<Response><Say>Service not yet available.</Say></Response>` from a JSON endpoint - mismatched media-type semantics for an `inbound` endpoint. | curl -X POST -H 'Content-Type: application/json' -d '{}' http://127.0.0.1:8731/api/dossier/inbound returns XML. | response | Either rename to /api/voice/inbound or return JSON {ok:false, error:'not yet available'}. |
| B029 | 2026-05-30T16:24Z | P2 | engine /api/cost/stats | Daily budget=$0.55 and weekly budget=$3.85 are hardcoded but the per-user $200/year ceiling in CLAUDE.md works out to $0.55/day. Current totals all 0.0 with 0 tasks (no real cost incurred yet). However per_task_ceiling_usd=$0.002 and hard_cap=$0.005, but stats only count `daily_calls` (counted=0). Need verification under load. | response in B005 area. | Test: hit /api/listen/upload 10 times in 1 hour; verify daily_calls>=10 and running_daily_total_usd > 0. (Will probe.) |
| B030 | 2026-05-30T16:24Z | P3 | engine | engine process VSZ reaches 439 GB virtual (mmap'd model weights + buffers), which can confuse process explorers but is normal for arm64. Just worth documenting for ops. | ps -o vsz -p 89677 shows 439864032 KB | n/a (cosmetic) | Document expected RSS/VSZ ranges in runbook. |
| B031 | 2026-05-30T16:24Z | P1 | engine /api/risk/assess | irreversibility_score is constant 0.7 across very different intents ('send sms to mom urgent', 'transfer $1 million', 'cancel my subscription', 'say hi', 'send email'). Default-baked value masks real classification. | python3 /tmp/risk_test.py for many intents - all return irreversibility_score=0.7 except 'delete email' which is 1.0. | response | The 0.7 sentinel is the fallback; real classifier is silently no-op. Wire actual risk model or change sentinel name (e.g., `irreversibility_score_source='default'`). |
| B032 | 2026-05-30T16:30Z | P0 | engine /api/listen/inject trivia | Trivia cache returns wrong answer when question has NEW noun the cache lacks. 'wait who is the current president of france' returns 'Donald Trump is the current president of the United States' because matcher ignores 'france' (drops it as content word but matches 2/3 of cleaned tokens 'current president'). | curl -X POST -d '{"text":"wait who is the current president of france"}' http://127.0.0.1:8745/api/listen/inject returns proposal about Trump/USA. Score 0.909 vs 0.45 threshold. | Live response + sqlite3 inspection of /tmp/bug-hunter-probe-99970/.anticipy/trivia_cache.db shows alias 'who is the president' under topic 'current US president'. | Add country/proper-noun preservation in scoring: if question has a proper-noun token not in cache alias, score must drop OR return no_answer. Equivalent: do not strip 'france', 'england', 'china', etc. from stopwords AND require >= 95% token overlap when proper nouns differ. |
| B033 | 2026-05-30T16:30Z | P0 | engine trivia | 'wait when was the Roman Empire founded' returns DECLARATION OF INDEPENDENCE answer (score 0.802). The cache matches 'founded' against 'founding of Rome' / 'Declaration of Independence' aliases. | curl response shown. Lane: cache. Topic returned: 'Declaration of Independence'. | response + recent fires log | Same as B032 plus stricter alias engineering. The cache aliases for `Declaration of Independence` likely include 'when was america founded' or similar; need negative training or stronger scoring. |
| B034 | 2026-05-30T16:30Z | P0 | engine trivia | 'wait when did England win the world cup' returns 'Brazil has won the most FIFA World Cups, with five titles' (score 0.494). Both wrong info AND a non-sequitur (asks about England, answers about Brazil). | curl response. Topic 'World Cup most wins' fired at 0.494 (barely above 0.45 threshold). | response | Bump MIN_SCORE to 0.6+ for cache hits OR require exact country/year match when present in question. |
| B035 | 2026-05-30T16:30Z | P1 | engine trivia | Trivia fires on incomplete utterances. 'wait when did the Roman Empire' (no verb) returns the fall-of-Rome answer with score 0.696. ASR mid-stream cutoffs will trigger false fires. | curl + recent fires log | Require minimum 4 content tokens (after stopword removal) before firing trivia. Or detect sentence completion via terminal punctuation/silence pause. |
| B036 | 2026-05-30T16:30Z | P2 | engine /api/onboarding/answer | After answering all 7 questions (index 0-6), engine state remains onboarded:false; index=7,8 returns empty response with no completion signal. | curl loop 0..8 then GET /api/state shows onboarded:False, profile:null. | response sequence | Index 6 final answer should: (1) write profile, (2) flip onboarded=True, (3) return completion ack like {done:true, profile_summary:...}. |
| B037 | 2026-05-30T16:30Z | P2 | engine /api/onboarding/answer | Answers index >= total (e.g., 7, 99) silently return empty 200 instead of 4xx. | curl -X POST -d '{"index":99,"answer":"x"}' returns empty body, HTTP 200. | response | Return 422 `index out of range` for index >= total. |
| B038 | 2026-05-30T16:30Z | P1 | engine /api/onboarding/chat_complete | Field `transcript` expects List of objects (not string). Contract is undocumented and misleading. | curl -X POST -d '{"transcript":"...string..."}' returns 'Input should be a valid list'. curl with '[\"a\",\"b\",\"c\"]' returns 'Input should be a valid dictionary or object'. | response | Either accept string transcript (most callers will send) OR document the list-of-objects shape with example. |
| B039 | 2026-05-30T16:30Z | P0 | engine /api/onboarding/call_stub | Endpoint accepts non +1 numbers (e.g., '+447911123456' UK) AND premium prefixes (e.g., '+19005550000') without validation. Should be rejected per +1-only / no-premium policy enforced elsewhere. | curl -X POST -d '{"phone":"+447911123456"}' returns ok:true. Same for +19005550000. | repro | Add E164 check + premium-prefix block matching /api/twilio/relay validatePayload/isAllowedDestination. |
| B040 | 2026-05-30T16:30Z | P2 | engine /api/coldstart/sources | DELETE method returns 405 'Method Not Allowed'. Users can only POST a new list; no way to remove a single source short of POSTing the full minus-one list. | curl -X DELETE returns 405. | n/a | Either accept DELETE for clearing OR document the POST-all-or-nothing model. |
| B041 | 2026-05-30T16:30Z | P3 | engine /api/coldstart/sources | POST with `{"sources":[]}` succeeds (sets sources to []) and returns ok:true with no warning that previous sources are gone. Silent data loss. | repro | Warn caller when sources count drops by >50% (likely accidental). |
| B042 | 2026-05-30T16:30Z | P3 | engine onboarding question copy | Question 5 reads: 'What do you want Anticipy to do for you, and what is strictly off limits, the do not touch list?' — 'do not touch list' is awkward English. | curl GET /api/onboarding/start, walk to index=4 | Reword: 'What do you want Anticipy to do, and what should it never touch?' |



