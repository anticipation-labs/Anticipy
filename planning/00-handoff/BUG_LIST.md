# Anticipy Bug Catalog

**Methodology**: Continuous static audit walking 54+ surfaces (pages, API routes, engine modules, scripts).
**Numbering**: B### sequential. Dedup by hash(Surface + Title).
**Severity**: P0 (security/data loss/prod broken), P1 (functional break for users), P2 (poor UX/perf/SEO), P3 (cosmetic/minor).
**Scope**: Catalog only, no fixes.

## Iteration Log
- Iter 1 (2026-05-30 09:47 PDT): Fresh start of catalog; previous "iter 5" narrative had no persisted artifact. Beginning B001+.

## Bug Table

| ID | Surface | Severity | Title | Evidence |
|----|---------|----------|-------|----------|
| B001 | src/lib/analytics-auth.ts | P0 | Default analytics password "Anticipy123" baked into source | Line 31: `const expected = process.env.ANALYTICS_PASSWORD \|\| "Anticipy123";` Anyone reading public repo / decompiled SSR bundle can log in to /analytics if env var unset. |
| B002 | src/lib/analytics-auth.ts | P0 | Default ANALYTICS_SECRET fallback enables forging session cookies | Line 6: `"anticipy-analytics-default-secret-not-for-prod"` is used when neither ANALYTICS_SECRET nor SUPABASE_SERVICE_ROLE_KEY is set. HMAC token is deterministic + universally knowable; forge cookie + skip password gate entirely. |
| B003 | src/lib/analytics-auth.ts | P1 | Session token is static (no per-user / no expiry) | `getSessionToken()` always returns same HMAC of fixed payload "anticipy-analytics-v1". Token never rotates; cannot revoke without rotating secret; no replay protection. |
| B004 | src/lib/analytics-auth.ts | P2 | Length-check before constant-time compare leaks password length to attacker | Line 33: `if (password.length !== expected.length) return false;` early-returns before `timingSafeEqual`. Timing measurable diff reveals exact password length. |
| B005 | src/app/api/analytics/login/route.ts | P1 | No rate limit on analytics login endpoint | Endpoint accepts unlimited password attempts. Combined with default "Anticipy123" (B001), trivial dictionary attack. |
| B006 | src/lib/release-meta.ts | P0 | Hardcoded DMG filename + path leaks internal hardware version to public release-meta | `DMG_PATH = "/dl/Anticipy_1.0.0_aarch64.dmg"` returned by GET /api/release-meta. Any visitor learns macOS aarch64-only restriction + version 1.0.0 even pre-launch. |
| B007 | src/lib/release-meta.ts | P2 | sha256 from manifest.json may be wrong when DMG is regenerated without manifest rebuild | `buildManifest.latest_sha256` returned to clients but DMG_PATH is hardcoded; if scripts/bundle-parakeet-model overwrites DMG without rebuilding manifest, sha256 mismatch breaks integrity check on client side. |
| B008 | src/app/api/internal-gate/route.ts | P0 | Dev passcode "123" usable in any non-production NODE_ENV (preview deploys included) | Lines 22-26: only blocks the dev default in `NODE_ENV === "production"`. Vercel preview deploys typically run NODE_ENV=production but staging builds and self-hosted forks running development build expose 3-char passcode. |
| B009 | src/app/api/internal-gate/route.ts | P2 | Lowercases input + expected, weakens case-mixed passcodes by ~5x search space | Lines 65-66: `.toLowerCase()` both sides. A passcode like "Xa72BqY9" gets compared lowercase, halving effective entropy per alpha character. |
| B010 | src/lib/gate-cookie.ts | P1 | HMAC secret defaults to SUPABASE_SERVICE_ROLE_KEY (cross-purpose secret reuse) | Lines 21-23: reuses SERVICE_ROLE_KEY for cookie HMAC. If service role key is later rotated for unrelated reason, all gate cookies invalidated, sessions broken. Also if HMAC implementation leaks (e.g. via DPA), reveals service-role-key bits. |
| B011 | src/lib/engine-transfer-gate.ts | P1 | Same HMAC reuse pattern with JWT_SECRET fallback + dev default | Lines 9-14: defaults to "anticipy-engine-transfer-gate-default-secret"; lets attackers forge gate cookie if both env vars unset (which on a fresh dev clone is the case). |
| B012 | src/lib/engine-transfer-gate.ts | P1 | Signed token value is literal string "valid" — pre-computable | Verifier hardcoded `if (value !== "valid") return false` and signs the same string; once secret leaks (B011), forge cookie trivially. No nonce/expiry inside signed payload. |
| B013 | src/lib/gate-cookie.ts | P2 | Cookie name `anticipy_internal_gate` not marked __Host- prefix | Could be set/overwritten by attacker via subdomain in some hostings. Missing __Host- prefix means Domain attribute may be honored, weakening scope guarantees. |
| B014 | src/app/api/log/route.ts | P1 | Authorization bypass via valid CRM gate cookie regardless of agent identity | Lines 23-29: any signed-in operator with CRM cookie can post arbitrary `crm_agent_events` rows claiming any agent_name. No audit trail of which operator forged the event. |
| B015 | src/app/api/log/route.ts | P2 | Arbitrary payload JSON written to DB with no schema validation | Line 65: `payload_jsonb: body.payload ?? null` accepts any object up to default JSON size. Could be used to bloat DB or store XSS payloads later rendered unsafely. |
| B016 | src/app/api/waitlist/route.ts | P1 | IP rate limit trivially bypassed via x-forwarded-for spoofing | Lines 33-36: trusts `x-forwarded-for` first hop. On Vercel that header is set by edge, but if behind some proxy chains or in self-host, attacker spoofs IP. 3/hour limit easily bypassed. |
| B017 | src/app/api/waitlist/route.ts | P2 | Email stored as plaintext + sent to admin without unsubscribe link in welcome flow | `sendWaitlistWelcome` + `sendOwnerWaitlistNotification` fire on every signup; CAN-SPAM requires unsubscribe path. Check `src/lib/email.ts` for compliance. |
| B018 | src/app/api/waitlist/route.ts | P3 | EMAIL_REGEX rejects TLDs of length 1 character | `/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/` requires TLD >= 2 chars. Valid emails using new gTLDs of 1 char (none exist yet but standard allows) would be rejected; mainly concerns local-dev tests. |
| B019 | src/app/api/test-meta-monitor/route.ts | P0 | Default `META_MONITOR_TEST_SECRET = "test-secret"` accepted in non-production | Lines 36-39: when NODE_ENV is anything except "production" (Vercel preview deploys often have NODE_ENV=production though), endpoint is publicly callable with header `x-test-secret: test-secret`. Calls buildUserProfile for arbitrary UUIDs, hitting Gemini with caller's expense. |
| B020 | src/app/api/test-meta-monitor/route.ts | P1 | `process.env.META_MONITOR_TEST_FORCE_MALFORMED` mutation is process-global not request-local | Lines 67-77: mutates process.env, then restores in finally. But concurrent requests can race: req A sets var, req B reads it, req A restores, req B's malformed-path activates unexpectedly. Comment claims isolation, code does not deliver it. |
| B021 | src/app/api/engine/model/route.ts | P0 | LLM broker allows requests with any client-supplied `provider` object | Lines 48-50: forwards client-controlled `provider: {...}` straight to OpenRouter. Attacker can route requests to expensive providers (Anthropic Opus etc.), bypassing the intended Cerebras/Mistral/Gemini whitelist and inflating bill on Omar's account. |
| B022 | src/app/api/engine/model/route.ts | P0 | Model allowlist contains models that don't exist on OpenRouter | `deepseek/deepseek-v4-flash`, `deepseek/deepseek-v4-pro`, `moonshotai/kimi-k2.6` — none of these are real OpenRouter model identifiers as of catalog. Production calls always 4xx; engine broken or silently failing over. |
| B023 | src/app/api/engine/model/route.ts | P1 | reasoning param accepts arbitrary object via spread | Lines 41-43: any object passed becomes the reasoning param. OpenRouter charges for reasoning tokens — attacker sets `reasoning.max_tokens: 65535` to drain budget per call. |
| B024 | src/app/api/engine/transcribe/route.ts | P1 | Legacy null user_id rows on anticipy_sessions usable by ANY authed user | Lines 28-31: `return !data.user_id \|\| data.user_id === userId`. Comment says "treat as owned-by-anyone-authed" but this is a security regression — any authed user can write transcript rows into legacy sessions. |
| B025 | src/app/api/engine/auto-proceed/route.ts | P2 | executeAction called without explicit ownership re-check | The ownership check uses `intent.session_id → sessions.user_id`. If session has null user_id (legacy), code path falls through (no else for `!sess`), block returns 403 only when sess exists with mismatched user_id. Combined with B024 same data: legacy intents become world-executable. |
| B026 | src/app/api/engine/analyze/route.ts | P1 | TEST_USER_PHONE is a single env var, not per-user; SMS goes to fixed number | Line 949: `const notifyPhone = process.env.TEST_USER_PHONE;` Real wearers would never get their own SMS; instead admin/dev gets all of them. Violates "channel by urgency" memory. |
| B027 | src/app/api/engine/analyze/route.ts | P2 | ADMIN_EMAIL defaults to omar@anticipy.ai hardcoded | Line 889: `process.env.ADMIN_EMAIL \|\| "omar@anticipy.ai"`. Fork or deploy without env var sends notifications to Omar's personal-brand inbox. |
| B028 | src/app/api/engine/analyze/route.ts | P2 | Cross-session memory query has no rate limit per-user across requests | Lines 247-275: each /analyze call walks 50 sessions × all intents to build cross-session context. With analyze allowing 600/hr/user (line 45), can spam Supabase reads. |
| B029 | src/app/api/engine/analyze/route.ts | P2 | Realtime broadcast service-role-key used in body apikey + Authorization headers but no scoping | Lines 853-877: anycaller can subscribe to "anticipy-intents" topic per the comment ("the broadcast topic is anon-accessible by design"). Cross-user leakage of intent metadata to any extension connecting. |
| B030 | src/app/api/engine/confirm/route.ts | P1 | GET endpoint mutates DB state (changes intent status) — CSRF risk | Lines 33+: a GET request flips status to confirmed/rejected based on `action` query param. Any image embedded in email or visited URL fires execution. Comment acknowledges via legacy "intentId" + token but token's TTL not enforced via signed cookie binding to user. |
| B031 | src/app/api/engine/trajectory/route.ts | P1 | CORS Allow-Origin = "*" with credentialed-looking header X-Anticipy-Code | Lines 41-45 (and 192-198): `*` origin + custom header that contains a secret access code. Hostile websites can extract or POST a user's trajectories cross-origin. |
| B032 | src/app/api/engine/trajectory/route.ts | P2 | task_summary embedded only for 'success' rows poisons retrieval set on partial successes | Lines 173-185: only "success" outcome triggers Voyage embed. Real engineering proves "partial" or "fail" trajectories teach more, but more importantly: an attacker who can produce success: true reduces engineering signal. |
| B033 | src/app/api/agent/verify/route.ts | P3 | Dead/disabled endpoint returns 503 but defines full handler unused | Lines 41-159: defines VERIFIER_SYSTEM + helpers but always returns 503 at line 155-158. Dead code shipped to client routes increases bundle size + creates audit confusion. |
| B034 | src/app/api/agent/plan/route.ts | P3 | Identical dead/disabled pattern as B033 | Lines 36-110: full handler defined but disabled. Imports `callAgentJson, agentLLMAvailable` left unused. |
| B035 | src/app/api/agent/reflect/route.ts | P3 | Identical dead/disabled pattern | Same as B033/B034. Unused imports + system prompt shipped. |
| B036 | src/app/api/agent/critic/route.ts | P3 | Identical dead/disabled pattern | Same as B033/B034. |
| B037 | src/app/api/pre-orders/checkout/route.ts | P1 | IP-based 5/hr rate limit can be bypassed via x-forwarded-for spoofing | Lines 42-45: same x-forwarded-for spoof vector. Combined with a cancelled-order amplification, an attacker can spin many anticipy_preorders rows DB writes and Stripe checkout sessions, costing Stripe API quota. |
| B038 | src/app/api/pre-orders/checkout/route.ts | P2 | No CSRF protection — checkout is started by any cross-origin POST | Endpoint accepts JSON POST without origin verification. Attacker on hostile.com can trigger checkout against current user's email if they're on a phishing form, building Stripe customer rows under arbitrary emails. |
| B039 | src/app/api/pre-orders/checkout/route.ts | P3 | `customer_name` from client metadata copied verbatim into Stripe metadata without sanitization | Line 112: `customer_name: name \|\| ""`. Stripe metadata stored as-is; downstream email rendering must escape. Future surface bug. |
| B040 | src/app/api/webhooks/stripe/route.ts | P1 | refunded_at + refund_reason updated regardless of whether row exists | Lines 154-161: an `update().eq(payment_intent)` matching zero rows silently succeeds. A spoofed test webhook (if STRIPE_WEBHOOK_SECRET ever leaks) referencing nonexistent payment_intent shows no errors; status_only audit gap. |
| B041 | src/app/api/webhooks/stripe/route.ts | P3 | Agreement version defaults to hardcoded "v1-2026-05-27" | Line 103: `?? "v1-2026-05-27"`. If Stripe metadata ever drops the field (migration), old timestamps mis-stamped. Should be UNKNOWN_VERSION or fail. |
| B042 | src/app/api/twilio/sms-inbound/route.ts | P0 | GET endpoint allows ANY caller to claim and consume inbound SMS rows | Lines 109-198: no authentication on GET. Anyone who knows or guesses an account_id can poll-and-consume that user's inbound SMS replies, mark them consumed, denial of service against actual engine pollers. Critical privacy hole. |
| B043 | src/app/api/twilio/sms-inbound/route.ts | P2 | Falls back to include_unmapped=1 mode if include_unmapped=1 + accountId='' | Lines 122-152: when no accountId and `includeUnmapped=true`, query.is("account_id", null). Combined with B042: an unauthed attacker can drain all unmapped inbound SMS. |
| B044 | src/app/api/twilio/sms-inbound/route.ts | P3 | XML escape misses HTML entity refs in Body when echoing as TwiML acknowledgement | Body never put in TwiML response, but `params.From/To/Body` not sanitized when logged via `console.error`. Log injection trivially possible. |
| B045 | src/app/api/twilio/relay/route.ts | P2 | TWILIO_BROKER_FROM is single shared number; rate-limited globally, not per recipient | A single user spamming relay 10/hr can monopolize broker number's reputation; no per-recipient cap (e.g. 1 SMS to same number every 5 min). Combined with abuse, brings down the broker for all users. |
| B046 | src/app/api/twilio/relay/route.ts | P2 | STATUS_CALLBACK_URL hardcoded to https://www.anticipy.ai/api/twilio/status | Line 41: dev/staging deploys will set CallbackURL to production, polluting production status_only audit table. |
| B047 | src/app/api/twilio/status/route.ts | P2 | Insert placeholder includes `to_e164` from spoofed Twilio params even though signature was verified | Verification good, but body parsed AFTER signature check. If status callback is signed with right token but with arbitrary fields, attacker who has TWILIO_BROKER_TOKEN can poison anticipy_twilio_sends with arbitrary placeholders. |
| B048 | src/app/api/extension/auth/route.ts | P0 | Hardcoded Cerebras API key shipped in source code as fallback | Lines 97-98: `process.env.CEREBRAS_API_KEY \|\| "csk-jw66w22nrhcfkwckkv82jpjdep6rjdhvy96nce5hf94dfcpv";` Key is in public repo + Vercel build artifacts; anyone with network access to the source can extract & burn the free 1M tok/day quota. Comment dismisses risk but free-tier abuse still denies service. |
| B049 | src/app/api/extension/auth/route.ts | P0 | All LLM API keys returned to ANY extension presenting a valid access code | Lines 109-121: response includes cerebrasApiKey, groqApiKey, geminiApiKey, mistralApiKey, deepseekApiKey raw values. Extension code runs in user-controlled Chrome. ANY user who logs in once gets all 5 API keys + can use them outside extension for unlimited LLM expense. Should be opaque per-request proxy. |
| B050 | src/app/api/extension/access-code/route.ts | P1 | Access code generated with biased modulo over 31-char alphabet | Lines 12-15: `bytes[i] % 31`. Since 256 % 31 = 8, first 8 chars of alphabet get +1 probability vs others. Bias = ~3% per char. Reduces effective entropy from 5log2(31)=4.95 to slightly less. 10-char code = 49.5 bits → cumulative bias is small but not crypto-correct. |
| B051 | src/app/api/extension/access-code/route.ts | P3 | password_hash filled with `crypto.randomBytes(32).toString("hex")` (not a real bcrypt) | Line 56-65: filler is raw hex, not a bcrypt prefix `$2b$`. If legacy username/password engine path ever uses bcrypt.compare on this, it returns false but the field doesn't visibly signal "no password" — risk of confusion in audits. |
| B052 | src/app/api/extension/agent-config/route.ts | P2 | Endpoint returns identical config for ANY valid access code | Lines 139-172: no per-user customization despite passing through access-code check. Reveals current SYSTEM_PROMPT + tier_order + per_tier settings to anyone with any code. Tier_order leak helps competitors map provider strategy. |
| B053 | src/app/api/extension/agent-config/route.ts | P3 | feature_flags claims verifier_enabled=true but /api/agent/verify always returns 503 | Lines 129-131: extension will read this and try to call verify endpoint, get 503. Misleading flag desynced from actual /api/agent/* endpoints. |
| B054 | src/app/download/route.ts | P2 | DMG_BYTES hardcoded (2515615248) differs from /dl route (2515666283) | Line 13 in /download/route.ts: 2515615248. Line 14 in /dl route.ts: 2515666283. Difference 51035 bytes between Content-Length HEAD responses. Acceptance harness using one will mismatch the other. |
| B055 | src/app/dl/Anticipy_1.0.0_aarch64.dmg/route.ts | P1 | Public R2 bucket URL hardcoded; no integrity check / no signature | Line 12: `https://pub-e97c6305fe2949d8a5d17885f7be2a0e.r2.dev/...` is public-readable forever. Anyone who scrapes URL gets unlimited downloads; no per-user URL tracking. Compromise R2 bucket = malware substitution with no detection. |
| B056 | src/app/api/auth/exchange/route.ts | P2 | CORS Allow-Origin = "*" on credentialed auth exchange | Lines 8-12: returns access_token + refresh_token with `*` origin. Combined with any token leakage path, hostile.com can complete the exchange and steal session. |
| B057 | src/app/api/auth/google/route.ts | P1 | OAuth `state` cookie not verified against returned state in callback | Sets `anticipy_oauth_state` cookie but reading the callback file would tell. The state itself is a random UUID; without verifying it in /api/auth/google/callback this is just CSRF defense theater. (Need to check callback.) |
| B058 | src/app/api/auth/google/route.ts | P2 | access_token passed in URL query string is logged in Vercel/server access logs | Line 23: `url.searchParams.get("token")`. URL params get logged in Vercel function logs + intermediate proxies. Supabase tokens leak to logging systems. |
| B059 | src/app/api/auth/signup/route.ts | P1 | No rate limiting; no captcha — endpoint can spam-create Supabase auth users | Lines 26-122: no rateLimit() call. Attacker can call this 1000s of times to inflate auth.users row count, exhaust Supabase project quota / monthly active users metric. |
| B060 | src/app/api/auth/signup/route.ts | P2 | Password min length 8, no complexity / no zxcvbn / no breach check | Line 45: `password.length < 8`. Accepts "12345678" / "password". Standard password policy missing. |
| B061 | src/app/api/auth/signup/route.ts | P2 | Email not validated with EMAIL_REGEX before createUser | Line 37-39: only trim + empty check. Empty trim might pass when string is " "; createUser will then fail server-side but with arbitrary error message. |
| B062 | src/app/api/extension/auth/route.ts | P2 | Per-code rate limit shared across all IPs, no per-IP+per-code combined limit | Line 58: `ext-auth:code:${trimmedCode}` — 200/day shared. If 1000 attackers share a leaked code, each gets 0.2 req/day; legitimate user starved out. Should be per-code AND per-IP. |
| B063 | src/app/api/app/state/route.ts | P3 | Hardcoded engine address "http://127.0.0.1:8731" in user-visible detail string | Line 31: leaks internal port to anyone hitting /api/app/state, including via public browser dev tools. Minor recon info. |
| B064 | src/app/api/app/run/route.ts | P1 | Returns 200 OK even when engine fails or is gated | Lines 48-58: catches all errors, returns status 200 + `gated: true`. Clients can't distinguish real success from failure via HTTP status. Breaks monitoring + retry logic. |
| B065 | src/app/api/app/run/route.ts | P2 | maxDuration 300 seconds but AbortSignal.timeout(280_000) — tight 20s margin | If engine takes >280s response is dropped before Vercel kills lambda; if upstream takes 281-300s, lambda terminated mid-write of response. |
| B066 | src/app/api/admin/backfill-sessions/route.ts | P0 | Timing-fallback "guesses" user by signup proximity (within 7 days) | Lines 121-140: when no email match found, picks the user whose signup time is closest to session start. Attaches private session+intent data to potentially the WRONG user, irrecoverable PII leak. |
| B067 | src/app/api/admin/backfill-sessions/route.ts | P1 | ADMIN_EMAIL hardcoded "omar@anticipy.ai" | Line 7: any backfill from a fork uses Omar's email as filter. |
| B068 | src/app/api/admin/backfill-sessions/route.ts | P2 | No rate limit; long-running migration callable by any admin token | maxDuration 60s but iterates over potentially thousands of sessions × notification lookups. Can be triggered repeatedly to exhaust supabase compute. |
| B069 | src/app/api/admin/stats/route.ts | P3 | Day boundary computed in server local time, not UTC | Line 41: `new Date(year, month, date)` uses local TZ. On Vercel (UTC) but admin viewing from PST gets stale "today" counts. |
| B070 | src/app/api/cron/daily-digest/route.ts | P1 | `!secret` fallback authorizes all callers when CRON_SECRET is unset | Line 39: `return !secret`. Fresh deploys without CRON_SECRET allow ANYONE to trigger the daily digest, sending emails to all CRM users. Email-spam DoS vector. |
| B071 | src/app/api/cron/daily-digest/route.ts | P2 | Iterates `for (const r of recipients)` with no rate limit or batching | If recipients table is large, sequential sendEmail blocks the 60s maxDuration. Cron fails silently with partial sends. |
| B072 | src/app/api/dossiers/upsert/route.ts | P2 | `body.field_count` from client treated as authoritative | Line 50-52: takes `Math.max(client_value, computed_count)` — client can inflate field_count by sending huge number. Field misleading downstream. |
| B073 | src/app/api/flash/log_stub/route.ts | P3 | Hardcoded local engine port 127.0.0.1:8731 in serverless function | Line 20: Vercel lambda will NEVER reach a local engine — this is by definition. Always returns localstorage path. Code is misleading "best effort proxy" that has zero chance of success. |
| B074 | src/app/api/resolution-traces/insert/route.ts | P2 | Client supplies `layer_used` and `confidence` directly without validation | Lines 52-53: `Math.max(0, Math.floor(num(body.layer_used)))` accepts any number. Inflate confidence=1, layer_used=0 to make resolution traces look perfect to analytics. |
| B075 | src/app/api/crm/users/[id]/password/route.ts | P1 | Admin can set password as short as 4 characters | Line 31: `body.password.length < 4`. Industry standard is 8+. Admin reset to "1234" trivially. |
| B076 | src/app/api/crm/gate/route.ts | P0 | First-time login silently sets the typed password as the user's password | Lines 64-78: when user_id has no password_hash, the FIRST string anyone types becomes that user's password permanently. Anyone with user_id (visible in `for=login` listing per B077) can claim an account. |
| B077 | src/app/api/crm/users/route.ts | P1 | `GET ?for=login` returns user_id + email of every CRM user to unauth callers | Lines 25-36: rate-limited 30/min but completely unauthenticated. Combined with B076, attacker scrapes user list then claims any user without `has_password:true` with arbitrary password. |
| B078 | src/app/api/crm/gate/route.ts | P2 | Min password length 4 also applies on first-time login | Line 52: `password.length < 4`. Combined with B076 means any user account can be claimed with "1234" before legitimate user logs in. |
| B079 | src/app/api/crm/files/route.ts | P1 | ILIKE search injection via unescaped `%` and `_` | Lines 24-26: `q.or(\`filename.ilike.%${search}%,description.ilike.%${search}%\`)` — search containing `,` or `)` will break the .or() syntax. Also wildcards `%`/`_` unescaped, attacker enumerates all files via wildcard scan. |
| B080 | src/app/api/crm/files/route.ts | P2 | No MIME-type whitelist on file upload — accepts any content type | Line 55: `contentType: f.type \|\| "application/octet-stream"`. SVG with embedded JS, HTML file with XSS payload — stored as-is in Supabase storage, later served. |
| B081 | src/app/api/crm/files/route.ts | P2 | No file size limit (no per-file or aggregate cap) | Endpoint runs maxDuration 120s and iterates all files. A 5GB upload would 504 but the Supabase bucket could already be poisoned. |
| B082 | src/app/api/crm/files/[id]/route.ts | P1 | DELETE happens before storage cleanup (no transaction); partial state on failure | Lines 39-43: deletes DB row first, then `db.storage.remove`. If storage call fails, file stays in bucket forever orphaned; bucket grows unboundedly. |
| B083 | src/app/api/crm/extract-receipt/route.ts | P2 | Storage upload succeeds even when Gemini extraction fails, but returns 200 with error in body | Lines 56-67: extraction error wrapped in 200 response. Caller can't distinguish success vs failure via HTTP status; orphan files accumulate in bucket. |
| B084 | src/app/api/crm/voice/route.ts | P0 | Deepgram still imported despite v-final-prototype forbidden list | Line 6: `import { transcribe } from "@/lib/crm/deepgram";` — CLAUDE.md memory says Deepgram is forbidden per project handoff. Violation of provider whitelist. |
| B085 | src/app/api/crm/voice/route.ts | P2 | Storage upload succeeds even when transcription fails — empty transcript stored | Lines 61-67: try/catch swallows error, sets transcript="". UI later shows blank memos. |
| B086 | engine/app/main.py | P1 | CORS allow_credentials=True with broad origin list including localhost | Lines 225-238: includes `http://localhost:3000` AND `http://localhost:8000` with credentials. Hostile localhost-bound app (any user-installed software running a webserver) can hijack engine sessions on the user's machine. |
| B087 | engine/app/main.py | P2 | Rate-limit dict `_task_timestamps` grows to 50k users in-memory | Lines 53-76: process-local state; restart loses bucket. Multiple engine processes don't share state. |
| B088 | engine/app/main.py | P2 | `TRUST_FORWARDED_FOR` env opt-in good, but no validation that var format is sound | Line 273: `os.environ.get("TRUST_FORWARDED_FOR", "").lower() in {"1", "true", "yes", "on"}`. Any other value silently false — operator misconfiguration with `TRUST_FORWARDED_FOR=yes ` (trailing space) appears to be on but isn't. |
| B089 | src/app/internal/layout.tsx | P0 | DUAL client-side password gates: layout.tsx + PasswordGate.tsx, BOTH hardcoded "123" | Layout.tsx Lines 26: `cleaned === "123" || cleaned === "onetwothree"` runs on client only. Pages render server-side: `curl https://anticipy.ai/internal/docs/bom` returns full Bill of Materials. Anyone scrapes hardware spec, manufacturing partners (Bittele Toronto), pricing, ShipBob fulfillment address. PasswordGate.tsx (line 74) literally says `placeholder="Access code (123)"`. |
| B090 | src/app/internal/page.tsx | P0 | Internal page leaks proprietary supplier list with direct order links | Lines 137-183: links to JLCPCB orders, Seeed XIAO orders, Bittele Toronto, Packlane boxes, ShipBob Surrey BC. Anyone scraping unauth /internal sees the entire supply chain + tooling cost (~$37 USD/unit). |
| B091 | src/app/internal/page.tsx | P0 | Internal page exposes Vercel project URL + Supabase project ID + GitHub repo | Lines 211-241: links to `vercel.com/omar-ebrahims-projects-022b18ec/anticipy`, `supabase.com/dashboard/project/ogbxpqkmsdrcuilafycn`, `github.com/omize10/Anticipy`. Combined with B048's Cerebras key + GH repo, supplier list, leaks full operational graph. |
| B092 | src/app/analytics/login/page.tsx | P3 | autoFocus on password field triggers Chrome's "this site is suspicious" warning patterns | Combined with autoComplete="current-password" but no associated username field; password manager fingerprinting may misbehave. |
| B093 | src/app/admin/page.tsx | P2 | Hardcoded supabase URL fallback "http://localhost:54321" | Line 8: if `NEXT_PUBLIC_SUPABASE_URL` is unset, ALL admin requests go to localhost. Fresh forks have silently broken auth. |
| B094 | src/app/admin/login/page.tsx | P1 | No CSRF/origin check on Supabase signin; trusts client redirect target | Standard Supabase pattern but `router.push("/admin")` after success — combined with the supabase signin happening on client side without server-side verification, no audit trail of admin logins. |
| B095 | src/app/admin/login/page.tsx | P3 | Page returns "Invalid credentials." for any error (incl. network) | Line 30: catches all errors as "Invalid credentials" — operator can't distinguish wrong pass vs supabase down. |
| B096 | src/app/pre-orders/success/page.tsx | P1 | session_id from query string trusted to be stripe checkout session ID without verification | Lines 23-44: only prefix check `cs_`. Attacker who knows another customer's session_id (often leaked in referrers, screenshots, etc.) can view the email + name + city/state + amount on success page. PII exposure. |
| B097 | src/app/pre-orders/success/page.tsx | P2 | Server-side fetch to Stripe on every page load (no cache) | `stripe.checkout.sessions.retrieve` on every load × bots crawling success URLs = Stripe API quota burn. |
| B098 | src/middleware.ts | P0 | Middleware only redirects /engine to /app; does NOT enforce server-side gate on /internal, /admin, /analytics | Lines 18-20: `matcher: ["/engine", "/engine/:path*"]`. Nothing prevents scrapers from hitting /internal, /admin, /analytics directly. Combined with B089 (client-only gate on /internal) and B098, all "private" surfaces are publicly indexable. |
| B099 | src/middleware.ts | P3 | 301 redirect /engine → /app cached permanently; users with bookmarks can't roll back | 301 (not 302) is permanently cached. If the /engine page is ever reinstated, users with cached redirects will keep hitting /app. |
| B100 | src/app/engine/extension/page.tsx | P2 | Download URL hardcoded with cache-buster `?v=20260523-v6` but no integrity check | Lines 50-77: `/anticipy-extension-v6.zip?v=20260523-v6`. No SHA256, no signature, anyone who can MITM the download or compromise R2 bucket replaces the extension with malware. |
| B101 | extension/background.js | P0 | Subscribes to `realtime:anticipy-intents` topic with anon key — anyone listening sees ALL users' intents | Lines 192-200: extension subscribes to a shared "anticipy-intents" broadcast channel using SUPABASE_ANON_KEY (now public in source, line 9). Any installed extension or curl with the anon key listens to every confirmed intent across all users. Massive privacy leak: all wearer commands + emails fan-out cross-user. |
| B102 | extension/background.js | P2 | postgres_changes channel on `anticipy_intents` table with anon key — requires RLS to be tight | Lines 182-189: anon-key subscribe to INSERT/UPDATE on anticipy_intents. If RLS on the table allows anon SELECT (comment says "works if RLS allows anon SELECT"), every wearer's intents leak via Realtime. |
| B103 | extension/background.js | P3 | SUPABASE_ANON_KEY value embedded as constant string in shipped extension | Line 9: anon key in source. While "safe to embed" in theory, key rotation breaks all old extension copies; users with stale extensions fail silently. Should be fetched at runtime from /api/extension/auth. |
| B104 | extension/manifest.json | P1 | host_permissions: ["<all_urls>"] grants extension full access to every site | Line 16: full any-site access on install. Combined with B048 (Cerebras key in source) and B049 (all LLM keys returned on auth), compromised extension can exfiltrate any tab content + keys. Standard ask for browser agents but elevated risk in this stack. |
| B105 | extension/manifest.json | P3 | `notifications` and `alarms` permissions broaden attack surface but no notif strings in source require them | Lines 10-12. Useful for keepalive (alarms) but unused notifications add abuse surface. |
| B106 | extension/background.js | P2 | __anticipy_debug_run_intent globally exposed in SW for Playwright/DevTools | Lines 46-53: `globalThis.__anticipy_debug_run_intent = (intent) => {...}`. Any code with access to the SW global can bypass Realtime auth and trigger arbitrary intent execution. |
| B107 | extension/background.js | P2 | seenNewIntentIds Set capped at 500 — long-lived SW that gets >500 intents will drop old; possible re-execute of replayed intents | Lines 27-41: when dedup set hits 500, oldest entry dropped. Attacker replays an old-but-recent intent (within 500-event window) and it executes a second time. |
| B108 | engine/app/safety.py | P2 | LLM safety check truncates at 1500 chars; injection payload >1500 may smuggle past | Lines 355, 376: text truncated. If injection is positioned >1500 chars in, classifier sees benign-looking lead and approves. |
| B109 | engine/app/safety.py | P3 | Auto-dismiss includes "consent" as substring match — clicks ANY button text containing "consent" | Line 187: pattern "consent" too broad; "Manage consent preferences", "withdraw consent" might be wrongly clicked. |
| B110 | engine/app/safety.py | P2 | Sanitize patterns leave significant injection vectors unblocked | INJECTION_PATTERNS list (lines 191-210) doesn't include polyglot UNICODE injection, base64-encoded prompts, or `/system` style escapes. Sanitizer easily bypassed. |
| B111 | engine/app/config.py | P2 | SUPABASE_URL hardcoded to project-specific URL as fallback | Lines 24-27: `https://ogbxpqkmsdrcuilafycn.supabase.co` is Omar's project. Any fork without env var pings Omar's Supabase reads/writes. |
| B112 | engine/app/config.py | P1 | PASSWORD_MIN_LEN=8 but no complexity / breach check | Line 120: standard but weak. |
| B113 | engine/app/config.py | P3 | DEFAULT JWT_SECRET literal in source kept for "dev" — discoverable | Line 51: `_JWT_DEFAULT = "anticipy-engine-secret-change-me"`. Even with prod guard, dev env may be tested with leaked default. |
| B114 | engine/app/auth.py | P2 | Login rate-limit is per-IP, not per-account | Lines 67-73: 5 failures per IP blocks for 30 min. Attacker rotating IPs (residential proxy) trivially probes a target username. |
| B115 | engine/app/auth.py | P2 | `_login_failures` is module-level global; gunicorn workers don't share | Multi-worker deploy: each worker has separate counter. Attacker with N IPs and N workers gets 5*N failures total. |
| B116 | engine/app/auth.py | P1 | JWT_EXPIRY_HOURS = 72 — long-lived session token without refresh | Line 137: 3-day sessions. Leaked token = 3 days of unrevoked access. No refresh-token mechanism. |
| B117 | engine/app/auth.py | P2 | Login response distinguishes "invalid" vs "rate_limited" — leak of probing state | Lines 219-258: error codes differ, attacker knows when rate limit hit (don't retry that IP). Should always return same error. |
| B118 | engine/app/main.py | P3 | REQUIRED_TABLES list incomplete | Line 159: only checks engine_users, browser_profiles, engine_tasks. Many tables (anticipy_sessions, anticipy_intents, dossiers) not checked. Misleading boot-time warning. |
| B119 | engine/app/main.py | P3 | _start_time global is set during lifespan but never used for the /admin/stats display | Line 144, 177: dead instrumentation. |
| B120 | extension/agent.js | P2 | TASK_TIMEOUT_MS = 600_000 (10 min) without per-call cost cap enforcement at agent level | Line 26: agent could burn 60 LLM calls * 4 providers in 10 min. Cost watch (B048's CEREBRAS key) only at provider level. |
| B121 | src/app/api/engine/twilio/sms-reply/route.ts | P1 | Disambiguation by sent_at order leaks recent intent summaries to any phone | Lines 105-122: when 2+ pending intents exist, responds with first 4 chars of intent ID + 60-char summaries. Anyone who hijacks a wearer's number (SIM swap) sees recent intent summaries before they confirm. PII leak via SMS. |
| B122 | src/app/api/engine/twilio/sms-reply/route.ts | P2 | Body parsed for ID prefix using regex `[0-9a-f]{4,12}` matches sometimes inside user message text | Lines 88-89. A wearer replying "yes a1b2c3" — but also "yes 12345678" might collide with random intent UUID prefix patterns. False matching. |
| B123 | src/app/api/engine/twilio/voice-callback/route.ts | P1 | intentId from query string trusted; signature verifies body, but URL itself can be tampered | Lines 16-19: `url.searchParams.get("intentId")` not part of Twilio signature payload. Twilio signs URL+body but a relay or replay attack with altered intentId param would still pass HMAC check because URL is reconstructed from req. |
| B124 | src/app/api/engine/twilio/voice-callback/route.ts | P3 | Voice "yes"/"no" speech matching is `String.includes()` not whole-word | Lines 34-42: `speechResult?.includes("no")` matches "now", "no problem", "another" — too broad. Voice user saying "now then" can accidentally trigger reject. |
| B125 | src/lib/email.ts | P2 | Hardcoded CAL_LINK to omar-anticipy 30-min booking | Line 7. Investor welcome emails always point to Omar's specific cal.com URL. Fork or reseller sends investor mail to wrong destination. |
| B126 | src/lib/email.ts | P1 | sendInvestorWelcome silently no-ops if SENDGRID_API_KEY unset | Line 19, 74, 121, 169, 225: all 5 functions `if (!SENDGRID_API_KEY) return;`. Production deploy missing the key silently swallows all email; investor + owner notifications dropped. No alert. |
| B127 | src/lib/email.ts | P2 | FROM_NAME hardcoded "Omar from Anticipy" — leaks founder identity in every email | Line 5. Any spam complaint or sender reputation issue ties directly to Omar's personal brand. Should be configurable per-fork. |
| B128 | src/lib/email.ts | P3 | Investor welcome includes "Building since age 8" line — questionable signal in mass email | Line 59: hardcoded age-15 footer. Strong personal info that some recipients may screenshot/share. |
| B129 | src/lib/notification-adapter.ts | P2 | Resend client initialized with placeholder "re_placeholder" at module load | Line 35: `new Resend(process.env.RESEND_API_KEY ?? "re_placeholder")` — module loads OK without key. requireEnv check happens only on send. Initialization noise + silent placeholder may pass linting/checks. |
| B130 | src/lib/notification-adapter.ts | P2 | ANTICIPY_NOTIFY_FROM_ALLOWLIST defaults to aevoy.com domains; not Anticipy domain | Lines 113-122: allowlist `notifications@aevoy.com,aevoy@aevoy.com`. Confusing brand — emails come from "aevoy.com" while site is anticipy.ai. Bounce/spam attributable to wrong domain. |
| B131 | src/lib/twilio-verify.ts | P1 | reconstructWebhookUrl trusts x-forwarded-host without normalizing | Lines 60-67: `url.host = forwardedHost`. If proxy doesn't set, falls back to request host (likely 0.0.0.0 in serverless). Signature mismatch breaks all valid webhooks. If attacker sets x-forwarded-host to known value, can possibly compute valid signature. |
| B132 | src/lib/twilio-verify.ts | P2 | Fails closed (returns false) when TWILIO_AUTH_TOKEN unset — but cron may need different handling | Line 48. Good security default but combined with B070 (cron auth bypass), CRON_SECRET unset triggers similar issue. |
| B133 | src/lib/confirm-token.ts | P0 | Default secret fallback "anticipy-confirm-token-default-secret" allows token forgery | Lines 27-31: cascades JWT_SECRET → SUPABASE_SERVICE_ROLE_KEY → default-secret. Combined with bare-UUID grace period of 24h after process start (B147), unsigned tokens accepted. |
| B134 | src/lib/confirm-token.ts | P2 | legacyGraceActive() resets PROCESS_START_MS on every cold-start | Lines 144, 147-164: 24h grace window resets after Vercel lambda cold-start (lambdas wake/sleep frequently). Bare UUID tokens get continuous fresh 24h windows. |
| B135 | src/lib/require-auth.ts | P3 | New supabase client created per request; no client reuse | Lines 17: `createClient` called every request. Should be module-level singleton like supabaseAdmin. Performance + memory leak vector. |
| B136 | src/lib/rate-limit.ts | P0 | clientIp ALWAYS trusts x-forwarded-for first hop | Lines 50-53: zero opt-in for trust. In Vercel this is fine, but in any other host (self-host, fly.io, etc.) the header is attacker-controllable. ENTIRE rate-limit + waitlist anti-abuse system trivially bypassed by spoofing x-forwarded-for. Engine has correct opt-in via TRUST_FORWARDED_FOR (B088), but web side does not. |
| B137 | src/lib/rate-limit.ts | P2 | "unknown" bucket conflates all anonymous traffic — DoS by anyone consuming the global bucket | Line 56: When XFF missing, single shared bucket. Attacker sends 1 req without XFF to lock out all other "unknown" callers. |
| B138 | src/lib/rate-limit.ts | P1 | In-memory state means multi-lambda Vercel deploys have N independent counters | Comment line 4-7 admits this. Production rate limits are N× too lax where N = lambdas serving the route. |
| B139 | src/lib/execute-action.ts | P2 | Hardcoded sender "notifications@aevoy.com" baked into email_draft path | Line 417: hardcoded. Same B130 problem. |
| B140 | src/lib/execute-action.ts | P0 | buildBrowserTask leaks user body content to plaintext browser task (no encryption at rest) | Lines 47, 67, 156-157, etc.: full email body / message body in browser_task field stored in anticipy_intents.parameters. Anyone with DB read (compromised SUPABASE_SERVICE_ROLE_KEY or RLS bug) reads every wearer's drafted message verbatim. |
| B141 | src/lib/execute-action.ts | P2 | recordPreferenceSignal not awaited inside executeAction (route is awaited but not here) | Returns ActionResult but secondary writes (note insert at line 254-259) use `.then()` fire-and-forget. Vercel may kill before write completes. |
| B142 | src/lib/execute-action.ts | P3 | Switch-on-string actionType with default case routes unknown actions to notes | Line 477: any new action_type the LLM invents gets silently dropped to "saved note" without telling the user "I didn't know how to do that". Misleading UX. |
| B143 | src/lib/stripe.ts | P0 | PREORDER_PRICE_ID and PREORDER_PRODUCT_ID hardcoded as literal strings | Lines 34-38: `price_1TbxFiBMF3gCPOsen6FHtsa8`, `prod_Ub9YYo4OVgXz2L`. Any fork without env vars uses Omar's Stripe prices and triggers his Aevoy Stripe account on every checkout. |
| B144 | src/lib/stripe.ts | P2 | stripe API version pinned to "2026-05-27.dahlia" but Stripe regularly releases new versions | Line 14. Version drift over time means deprecation warnings + eventual breakage. |
| B145 | src/lib/stripe.ts | P3 | Proxy wraps Stripe client; symbol/proxy interactions can cause unexpected throws in TypeScript strict mode | Lines 23-32: Proxy with `_target` empty. Some bundlers/transpilers (esbuild minify) mangle Proxy behavior. |
| B146 | src/app/api/twilio/sms-inbound/route.ts | P2 | account_id forced to null in INSERT — entirely defeats per-account routing | Line 63-75: comment admits "intentionally a no-op". Every inbound SMS lands in null-bucket; B042 (unauthed poll) drains them all. |
| B147 | engine/app/agent.py | P3 | Browser timeout env vars set with setdefault, runs at module load | Lines 32-43: `os.environ.setdefault` for TIMEOUT_*. If parent already set different value, this is a no-op but the comment claims setting before browser_use import is critical. Race with reloads. |
| B148 | src/lib/supabase-admin.ts | P0 | Placeholder service key fallback "placeholder-service-key" used silently | Line 6: defaults to fake key. Routes call supabaseAdmin without checking; queries fail downstream with cryptic errors. Should hard-throw at module load if missing. |
| B149 | src/lib/supabase.ts | P0 | Same issue — placeholder anon key fallback | Line 6: `"placeholder-anon-key"`. Client-side init silently broken. |
| B150 | src/lib/crm/gate.ts | P1 | CRM_GATE_TTL_SECONDS = 30 days — very long sessions for CRM | Line 12: 30 days. Stolen cookie persists a month. Compare to /internal which is 15 min (B015 comment). |
| B151 | src/lib/crm/gate.ts | P3 | secret() concatenates ":crm-v2" but no version field in cookie body | Line 31: if you bump to v3, all old cookies fail without graceful migration. Should support multiple keys. |
| B152 | src/lib/crm/password.ts | P1 | Password min length 4 — too short for production | Line 25-27: `password.length < 4`. Combined with B076 (first-time login), all CRM accounts trivially claimed. |
| B153 | src/lib/crm/password.ts | P2 | scrypt cost N=16384 below modern recommendation (N=32768+) | Line 9: minimal scrypt cost. For passwords stored hash brute force is faster. |
| B154 | src/app/api/crm/integrations/test/route.ts | P0 | Deepgram still tested despite v-final-prototype provider whitelist forbidding it | Lines 56-67: imports/tests DEEPGRAM_API_KEY. Same as B084 — provider violation. |
| B155 | src/app/api/crm/integrations/test/route.ts | P1 | Test endpoint exposes which API keys are configured to ANY CRM user | Lines 14-22: returns ok/message for supabase, gemini, deepgram, sendgrid, resend. Even a non-admin CRM user sees infrastructure inventory + can probe for misconfig. |
| B156 | src/app/api/crm/expenses/export/route.ts | P2 | No filter or rate limit on export — full DB dump via simple GET | Lines 6-67: any signed-in CRM user can export ALL expenses with date filters. No daily download limit; quick exfil vector. |
| B157 | src/app/api/crm/expenses/route.ts | P2 | Insert accepts arbitrary `notes`, `raw_extraction_jsonb` from body | Lines 91, 96: unchecked JSON blob. Combined with sysadmin endpoints, can poison DB with massive blobs. |
| B158 | src/app/api/crm/expenses/route.ts | P3 | clampInt accepts negative `lo` values; insertion of negative cents | Line 108-112: clampInt(value, 0, ...) is fine, but `clampInt(body.gst_cents, 0, ...)` allows 0 which is correct, but no minimum guarantees for status string ("confirmed" defaults). Test path may set status="" silently. |
| B159 | src/app/api/crm/contacts/import/gmail/route.ts | P2 | OAuth flow not tied to current CRM session — pulls Google contacts from globally-stored tokens | Lines 32-40: fetchAllPeople() reads the cookie set by /api/auth/google. If the CRM cookie is from User A but google_tokens cookie is from User B's prior session on same browser, contacts get mixed. |
| B160 | public/install.sh | P1 | Bash installer downloads from anticipy.ai but doesn't verify GPG signature or sha256 | Lines 207-214: `curl -fL --retry 3 -o "$DMG" "$URL"`. No checksum verification. If anticipy.ai or R2 is compromised, every install gets malware. |
| B161 | public/install.sh | P0 | `xattr -dr com.apple.quarantine /Applications/Anticipy.app` removes macOS Gatekeeper protection | Line 231: forcibly strips quarantine flag. Combined with B160 (no checksum) and no Apple notarization (script comment line 2), any compromised binary runs trusted. |
| B162 | public/install.sh | P2 | `xargs kill -KILL` of arbitrary PIDs from lsof | Lines 60-67: takes lsof output as PIDs to kill. Race condition or malicious lsof shim could kill other processes. |
| B163 | public/install.sh | P3 | Installer runs `pip install` into venv without --require-hashes | Lines 151-155: any package mirror compromise lets attacker swap deps (httpx, supabase, cryptography). |
| B164 | public/install.sh | P2 | EXTENSION_ID hardcoded "npnpagopediecennpleihemoochikggb" tied to specific Chrome Web Store publish | Line 13: if extension republished under different ID, installs are broken silently — no Chrome Web Store check. |
| B165 | public/robots.txt | P3 | All routes including /internal and /admin Allow: / for all bots | Lines 4-5: `Allow: /` to all. Combined with B089 (client-only gate on /internal) and B098 (no middleware gating), bots index full internal hardware spec page contents. |
| B166 | public/llms.txt | P2 | Same — anti-leak doc explicitly tells AI scrapers about the company | Lines 1-41: explicit instructions for GPTBot/ClaudeBot/PerplexityBot to ingest hardware specs, pricing, founder location. Combined with B091, internal supplier list also ingested. |
| B167 | src/app/internal/render/page.tsx | P2 | iframe to /render_product.html — risk of clickjack if not X-Frame-Options | Line 9-13: full-viewport iframe of static HTML. If render_product.html ever gets X-Frame-Options removed, this becomes a clickjack vector. |
| B168 | src/app/internal/order/page.tsx | P2 | Same iframe pattern; static order.html may leak BOM via crawler | Line 9-13: iframe `/order.html` is publicly accessible to any GET. Bypasses the client-side /internal gate. |
| B169 | src/app/funded/page.tsx | P0 | $1.5M raise at $15M cap + investor data hardcoded in public client bundle | Lines 12-21: full raise terms, valuation, projectedReturn 92x, projectedYear 2031, minInvestment $1000. Public JavaScript bundle. Competitors see exact deal terms; could be misleading "92x" projection legally risky. |
| B170 | src/app/funded/page.tsx | P3 | Hardcoded cal.com link for fundraising bookings | Lines 22-23: tied to Omar's cal.com. Same as B125. |
| B171 | src/app/onboarding/audio/page.tsx | P2 | Posts audio file to LOCAL_ENGINE 127.0.0.1:8731 — relies on user's mac having engine | Lines 20, 65-80: doesn't handle "engine never running" case gracefully (status string says "open Anticipy"). On Linux/Windows users this just hangs. |
| B172 | src/app/onboarding/audio/page.tsx | P2 | "Audio never leaves the device" claim in comment but page also calls Supabase /api endpoints | Lines 6-8 comment: "audio never leaves the device", but file is uploaded as multipart to engine. If engine internally proxies to cloud (we saw deepgram in CRM), claim false. |
| B173 | src/app/onboarding/call/page.tsx | P2 | "Stub" call flow but page submits phone numbers without warning that no call is placed | Lines 4-12: describes itself as stubbed but UI shows "queued" state misleading the user that they'll get a call. |
| B174 | src/lib/notification-adapter.ts | P3 | mockMode returns true unconditionally when env var "true"; no per-recipient override | Line 92-95: `ANTICIPY_NOTIFY_MOCK === "true"` mocks ALL notifications globally. No per-test-recipient way to mock vs real send. |
| B175 | src/lib/notification-adapter.ts | P2 | recordNotificationAttempt fails silently on Supabase error | Lines 282-300: try/catch swallows. Failed audit log = no record of who got SMS spam, can't reconcile during incidents. |
| B176 | engine/app/main.py | P2 | ALLOWED_ORIGINS includes both anticipy.ai and anticipy-beta.vercel.app | Lines 226-227: legacy beta URL still trusted. Any deploys of `anticipy-beta` on Vercel can call engine with credentials. |
| B177 | engine/app/main.py | P3 | `allow_methods=["GET", "POST", "OPTIONS"]` — no PUT/PATCH/DELETE blocked but routes use POST only | Lines 236. Defense in depth: routes accept POST, CORS only allows POST, OK. But other engine endpoints (PATCH for sessions) may break. |
| B178 | src/lib/cerebras.ts | P0 | DUPLICATE hardcoded Cerebras key in second file | Line 32: same `csk-jw66w22nrhcfkwckkv82jpjdep6rjdhvy96nce5hf94dfcpv` key as B048 (extension/auth) hardcoded again. Two surface points to leak the same key; rotation harder. |
| B179 | src/lib/cerebras.ts | P1 | cerebrasAvailable() returns true even when env var unset because of FALLBACK_KEY | Line 35: `Boolean(process.env.CEREBRAS_API_KEY \|\| FALLBACK_KEY)` always true. Callers see "Cerebras available" and never failover to other providers when free-tier exhausted. |
| B180 | src/components/Footer.tsx | P3 | Footer hardcodes "2026 Anticipation Labs" — won't auto-update yearly | Line 14: hardcoded year. Recurring annual bug. |
| B181 | src/components/Footer.tsx | P3 | Footer email "hello@anticipy.ai" — exposes the support email to spam scrapers | Line 44: spammable address in plain text. Should be obfuscated or routed via form. |
| B182 | src/components/EngineTransferGate.tsx | P3 | Reloads whole window on success rather than client-side rerender | Line 22: `window.location.reload()`. Wastes the SSR pass. Should set state and trigger re-render. |
| B183 | src/components/WaitlistCTA.tsx | P3 | Hardcoded `text-gold` Tailwind class but theme color var is `--gold` | Line 88: minor inconsistency with rest of codebase using CSS vars. |
| B184 | src/components/PostHogProvider.tsx | P2 | NEXT_PUBLIC_POSTHOG_KEY exposed in client bundle if set | Lines 8, 17: posthog public key, exposed by design but means anyone can spam analytics with fake events under your project. |
| B185 | src/components/PostHogProvider.tsx | P3 | maskInputOptions does NOT mask email | Line 27: `email: false`. PostHog session recordings capture every typed email. PII violation under GDPR/CCPA. |
| B186 | src/app/sitemap.ts | P3 | /engine listed in sitemap with priority 0.6 but middleware 301s to /app | Lines 37-42: tells crawlers about route that doesn't exist. Confuses indexers. |
| B187 | src/app/sitemap.ts | P3 | Hardcoded baseUrl https://www.anticipy.ai — preview/staging deploys point search engines to prod | Line 4: `const baseUrl = "https://www.anticipy.ai";`. Preview branch sitemap.xml indexed by Google = duplicate content. |
| B188 | src/app/layout.tsx | P3 | JSON-LD WebSite name "Anticipy" but URL is /app — inconsistent | Lines 92-96: standard nav URL is /app, but homepage at /. JSON-LD says WebSite URL is /app. Confuses crawlers/SEO. |
| B189 | src/app/refund/page.tsx | P2 | Hardware "30-day satisfaction guarantee" advertised before product exists | Lines 117-165: full return process documented as available. Pre-order hardware doesn't ship until Aug 2026; advertising 30-day return on un-shipped device is consumer-protection risky if customer believes it applies to pre-order deposit. |
| B190 | src/components/Pricing.tsx | P1 | "first year included, then $99/yr" appears in Pricing component but no public ToS sets $99/yr | Line 159. Contradicts CLAUDE.md memory (which says first year included). Customers buying pre-order may not know they'll be charged $99/yr after. Consumer protection / disclosure issue. |
| B191 | src/app/api/twilio/relay/MIGRATION.sql | P3 | SQL migration file references Omar's Supabase project ID in comments | Lines 6-7: ogbxpqkmsdrcuilafycn called out. Same supplier-list-style leak. |
| B192 | src/components/EngineTransferDoc.tsx | P3 | Mentions Supabase project ref ogbxpqkmsdrcuilafycn in user-facing doc | Line 467 (per grep). Same B091 supplier leak. |
| B193 | engine/app/agent.py | P3 | Cerebras key, Groq key, Google key all logged via api_key= positional | Lines 486, 497, 505: api_key=X explicit. If logger captures locals (some Python loggers do), keys leak to stdout. |
| B194 | src/lib/notification-adapter.ts | P3 | `ANTICIPY_NOTIFY_FROM_ALLOWLIST` env var defaults to aevoy.com domains, conflicts with hello@anticipy.ai used in email.ts | B127 + B130. Two parallel email systems use different from-addresses. Confusion + bounce attribution scattered. |
| B195 | engine/app/server.py | P0 | DUPLICATE engine server with `allow_origins=["*"]` + `allow_methods=["*"]` + `allow_headers=["*"]` | Lines 27-30: a second FastAPI app in same engine module. CORS completely open. Any website can make any HTTP method call to user's local engine. Combined with /journey/run POST endpoint (no auth), any visited site triggers the engine pipeline. |
| B196 | engine/app/server.py | P1 | /journey/run accepts arbitrary body without auth or rate limit | Lines 38-62: no token check, no IP limit. Body's `spoken_text` triggers REAL parakeet ASR + REAL frozen reasoning + REAL browser action. Combined with B195 CORS, any tab user visits POSTs to localhost:port (if engine is bound there) and exfiltrates user data via real pipeline. |
| B197 | src/app/api/engine/deepgram-key/route.ts | P1 | Route returns 503 but src/app/engine/page.tsx still hits it on Listen press | Engine page.tsx line 712 fetches /api/engine/deepgram-key. Always 503 per B084-related fix. Engine page Listen button is permanently broken (silent failure). |
| B198 | src/app/engine/page.tsx | P0 | Page contains direct Deepgram WebSocket client code (wss://api.deepgram.com) violating provider whitelist | Lines 758-761: `new WebSocket(wss://api.deepgram.com/v1/listen?...)`. CLAUDE.md memory: Deepgram is forbidden by v-final-prototype. Even though key endpoint returns 503, attacker who somehow obtains a deepgram token can connect via this UI. |
| B199 | src/app/engine/page.tsx | P3 | createScriptProcessor is deprecated Web Audio API | Line 765: `audioCtx.createScriptProcessor` deprecated. Should use AudioWorklet for forward compat. |
| B200 | src/lib/crm/deepgram.ts | P0 | Deepgram transcription wrapper used by /api/crm/voice — provider whitelist violation | Lines 7-37: full Deepgram client in src/lib/crm/. Imports from /api/crm/voice/route.ts. CLAUDE.md says forbidden. |
| B201 | src/lib/google-calendar.ts | P1 | ENCRYPTION_KEY required at module load with `process.env.ENCRYPTION_KEY!` (non-null assertion) | Line 6: TS-strict bypassed with `!`. Module loads with no error if unset; first encrypt() call throws cryptic Buffer.from error. |
| B202 | src/lib/google-calendar.ts | P0 | AES-256-CBC without HMAC — vulnerable to padding-oracle attacks | Lines 21-38: AES-CBC alone has no authentication. Standard recommendation is AES-GCM or AES-CBC+HMAC. Cookie storage at rest means a compromised Supabase row reveals partial plaintext via padding-oracle if there's any decrypt endpoint. |
| B203 | src/lib/google-calendar.ts | P3 | Token refresh keeps same refresh_token forever | Lines 137-141: `refresh_token: tokens.refresh_token, // Keep existing refresh token`. Google may rotate refresh tokens; ignoring the response field means future refreshes fail when Google rotates. |
| B204 | src/lib/google-calendar.ts | P3 | Timezone hardcoded America/Vancouver as default | Lines 169, 173: user in any other tz silently gets Vancouver-time event. |
| B205 | src/lib/intent-extract.ts | P3 | 3-pass extraction cost: 2.7x baseline; comment claims acceptable but ignores Mistral fallback chain | Lines 24-31. With Mistral fallback adding extra calls in retry path (B026), cost could be 5-6x. Comment is misleading. |
| B206 | src/app/api/crm/dashboard/route.ts | P3 | No filter for archived/disabled status; lists deleted users | Lines 24-62: parallel queries don't filter is_active or deleted_at. May show deleted users' todos. |
| B207 | src/app/api/crm/feed/route.ts | P1 | ILIKE search injection: same pattern as B079 | Line 17: `q.or(\`summary.ilike.%${search}%,action.ilike.%${search}%\`)`. Same fragility — search with `,` breaks OR. Also same wildcard issue. |
| B208 | src/app/api/crm/feed/route.ts | P2 | No pagination on feed — always returns 500 rows | Line 15: `.limit(500)`. Large CRM = browser hangs on 1MB+ response. |
| B209 | src/app/api/crm/contacts/import/gmail/route.ts | P2 | maxDuration 60s but unbounded loop over Google Contacts pages | Lines 16, 32-40: a user with 10k contacts will 504. Should be batched. |
| B210 | src/app/api/crm/voice/route.ts | P0 | Same Deepgram violation as B084 + transcript path stores full audio in CRM storage | Lines 6, 54-56: audio buffer uploaded to Supabase. Includes raw user voice — sensitive personal data. No retention policy. |
| B211 | src/components/EngineTransferDoc.tsx | P3 | Internal engineering doc rendered when password gate verified | Per B192. Contains the architecture diagram. |
| B212 | engine/app/product/server.py | P2 | _ALLOWED_ORIGINS includes http://127.0.0.1:3000 — local dev port | Lines 56-61: local-dev port trusted. Anyone running Anticipy locally is also vulnerable to malicious sites that bind 127.0.0.1:3000. |
| B213 | engine/app/product/server.py | P1 | Middleware sets `Access-Control-Allow-Private-Network: true` when site requests private network access | Lines 132, 138: opens up Private Network Access protections that Chrome added specifically to prevent malicious sites from accessing localhost. |
| B214 | engine/app/product/server.py | P3 | Machine-id stored in ~/.anticipy/machine_id with chmod 0600 silently fallback to 644 if chmod fails | Lines 188-191: `except Exception: pass`. On Windows/some filesystems, chmod no-op; file readable by other users. |
| B215 | engine/app/product/server.py | P2 | `_SESS` is module-global, NO multi-user — only one session at a time | Lines 142-143: global dict shared across all callers. Multi-user broken at engine level. |
| B216 | extension/popup.js | P0 | All LLM API keys stored in chrome.storage.local (persisted to disk) | Lines 95-125: stores cerebrasApiKey, groqApiKey, geminiApiKey, mistralApiKey, deepseekApiKey to local storage. Any other extension with `storage` permission on the same Chrome profile can read these keys (or compromised content scripts can exfil via DOM). Combined with B049 (server hands them out freely), keys are persistent secrets in user's browser. |
| B217 | extension/popup.js | P3 | accessCode also stored persistently in chrome.storage.local | Line 122: stored for proxy calls. Combined with B216, full credential set persists. |
| B218 | extension/engine_handshake.js | P3 | Triple postMessage retries (immediate, 800ms, 2500ms) — page can detect probe pattern | Lines 14-34: 3 announcements; any page that listens for the postMessage pattern can fingerprint that user has Anticipy installed. |
| B219 | src/app/api/crm/contacts/route.ts | P1 | Same ILIKE injection as B079: 3-column OR | Line 17: `q.or(\`name.ilike.%${search}%,email.ilike.%${search}%,role.ilike.%${search}%\`)`. Search with `,` breaks query. |
| B220 | src/app/api/crm/contacts/route.ts | P2 | No phone format validation on contact POST | Line 34: stores raw phone. No E164 / regex check. Downstream Twilio relay will reject malformed numbers but only at send time. |
| B221 | src/app/api/crm/todos/route.ts | P2 | sendEmail awaited inside POST — if email provider hangs, request stalls | Line 65-70: blocking send. Should be queued / fire-forget if request is user-facing. |
| B222 | src/app/api/crm/todos/route.ts | P3 | No status enum validation | Line 48: `status: body.status \|\| "todo"`. Client can submit any string. |
| B223 | src/app/api/crm/burn/route.ts | P2 | No pagination on yearRows; can return many MB | Lines 14-17: pulls entire year of expenses. Large org = slow. |
| B224 | src/app/api/crm/google/start/route.ts | P3 | Reuses /api/auth/google/callback URL for CRM-flow; same callback can be triggered from either context with no source check | Line 14: redirect URI reuse means a CRM /start flow's state cookie races with /api/auth/google's state. Potential CSRF in cross-flow scenarios. |
| B225 | extension/agent.js | P2 | AGENT_SYSTEM_PROMPT contains the literal "Never delete accounts, send money..." but no enforcement code | Line 160: prompt-only safety. Combined with engine/safety.py (B108-B110 LLM safety), no deterministic browser-side block. Agent can still navigate to /delete-account URL. |
| B226 | extension/agent.js | P3 | done(success:true) only verified by LLM self-assessment | Lines 142-145: agent self-declares done. No external verification step. If verifier (B033) is permanently 503, agent's success is unchecked. |
| B227 | extension/content.js | P2 | pierceQuery recurses ALL elements + into iframes; large DOM = quadratic time | Lines 11-58: O(N×M) scan. On a page with 10k elements + 5 iframes with their own 10k, takes seconds. Tab freeze during agent step. |
| B228 | extension/content.js | P3 | pierceQuery walks closed shadow roots only because background.js patches attachShadow | Line 8 comment: depends on world_patch.js running document_start. If race condition, some shadow roots stay closed. Agent miscounts elements. |
| B229 | src/app/api/crm/google/status/route.ts | P1 | Returns first google_tokens row, NOT the row for the calling user | Lines 9-14: `select("email, updated_at").order(updated_at).limit(1).maybeSingle()`. Returns the GLOBAL most-recent token holder, not the caller's. CRM user A sees user B's Google email address. PII cross-leak. |
| B230 | src/app/api/crm/cron-status/route.ts | P3 | Last digest_cron row exposed without filter on which user/org | Lines 8-14. Same as B229 — single global event row exposed. |
| B231 | src/app/api/internal-gate/route.ts | P2 | Strips whitespace via `\s+` then lowercase compare; passcodes with literal spaces (e.g. "two two two") never match | Lines 65-66: `replace(/\s+/g, "")`. Passcode "one two three" → "onetwothree" matches dev default, but accepted only when env default | accepts both. Confusing UX. |
| B232 | engine/app/product/server.py | P3 | Engine started with `--server --port 8731` flag-only no auth | install.sh line 253. Anything that can reach 127.0.0.1:8731 talks to engine. Combined with B195+B213 PNA, hostile browser tab calls. |
| B233 | src/app/api/crm/extract-receipt/route.ts | P3 | Stores receipt photos to permanent storage even if extract failed | B083 — orphans accumulate. Combined with B081 no size cap, bucket fills. |
| B234 | src/lib/escape.ts | P3 | sanitizeHeader truncates at maxLen-1 + "…" but actual maxLen-1 chars may slice UTF-8 multibyte char | Line 43: `cleaned.slice(0, maxLen - 1) + "…"`. JS string indexing is UTF-16 unit, not codepoint. Emoji at boundary becomes broken surrogate. |
| B235 | src/lib/notification-adapter.ts | P3 | Cloudflare email API: scope token + account ID embedded in URL not header | Lines 252-259. The Authorization header has token, account ID in URL. Standard but URL gets logged. |
| B236 | src/lib/crm/google.ts | P0 | `getAnyStoredTokens` returns globally most-recent Google tokens, not the caller's | Lines 76-90: any CRM user with valid session impersonates the most-recent Google-connected user's calendar AND contacts. Cross-tenant data leak. |
| B237 | src/lib/crm/google.ts | P0 | Same AES-256-CBC without HMAC pattern as B202 | Lines 51-68: padding-oracle vulnerability. |
| B238 | src/lib/crm/email.ts | P1 | FROM default "omar@anticipy.ai" hardcoded | Line 9: `process.env.CRM_EMAIL_FROM \|\| "omar@anticipy.ai"`. Any fork or unset env sends CRM mail as Omar. |
| B239 | src/lib/crm/email.ts | P3 | sendEmail short-circuits at first provider — no fallback if Resend down | Lines 18-36: if Resend present + failing, NEVER tries SendGrid even when configured. |
| B240 | src/lib/groq.ts | P3 | GROQ_API_KEY captured at module load via non-null assertion | Line 1: `process.env.GROQ_API_KEY!`. If env unset, undefined captured. Type-safety lie. |
| B241 | src/lib/gemini.ts | P3 | Free-tier rate limit comments claim "1500 RPM" but Gemini's free tier is 1500 RPD (per day) | Lines 47-49. Misleading. Free tier of Gemini 2.5 Flash is 1500 requests/day. Calls in analyze (B045 user limit 600/hr) overwhelm by 13x. |
| B242 | src/app/demo/page.tsx | P0 | Client-side passcode gate using `process.env.NEXT_PUBLIC_DEMO_PASSCODE` | Lines 25-26: NEXT_PUBLIC_* env vars are baked into client bundle. Anyone reading JS source sees the passcode in plaintext. |
| B243 | src/app/demo/page.tsx | P0 | engineUrl hardcoded fallback to https://anticipy-engine.up.railway.app | Lines 18-20: contradicts CLAUDE.md memory ("No Cloud Run / Fly.io engine hosts"). Railway is a cloud engine host. Plus URL exposed in public source. |
| B244 | src/app/demo/page.tsx | P2 | sessionStorage auth check trusts client state — refresh resets, /demo content unauthenticated for that moment | Line 13-15: gate UX flickers. |
| B245 | src/app/demo/page.tsx | P3 | Iframe to external URL with no sandbox attribute | Lines 63-68: `allow="clipboard-read; clipboard-write"` granted to remote iframe. If engine URL ever compromised, can read clipboard. |
| B246 | src/app/api/crm/contacts/[id]/route.ts | P1 | PATCH allows changing `source` field after creation — bypass gmail-vs-manual provenance | Line 10: any field including `source` can be updated. Audit trail tampering. |
| B247 | src/app/api/crm/expenses/[id]/route.ts | P2 | PATCH lets caller change `status` to "approved" without admin check | Lines 22: `status` in allowed list. Any signed-in CRM user can approve their own expense. |
| B248 | src/app/api/crm/files/[id]/comments/route.ts | P2 | No content moderation; comment body unlimited length | Line 23: only trim/empty check. 1MB comments allowed. |
| B249 | src/app/api/crm/files/[id]/comments/route.ts | P3 | author_user_id resolves to null silently | Line 27: `author_user_id: me` where me may be null → DB constraint violation if NOT NULL, silent null insert otherwise. |
| B250 | src/app/api/crm/todos/[id]/comments/route.ts | P2 | Same issues as B248-B249 | Identical pattern. |
| B251 | src/app/api/crm/decisions/[id]/route.ts | P3 | No DELETE confirmation; soft-delete absent | Lines 16-22: hard delete. No way to recover. |
| B252 | src/lib/crm/identity.ts | P0 | Legacy `x-crm-user-id` / `x-crm-user-name` headers TRUSTED when cookie session is missing | Lines 22-23: reads client-controllable headers. Combined with B077 (user list publicly readable), attacker scrapes user_id then sends spoofed headers to write under that identity. Comments + writes attributed to wrong user. |
| B253 | src/lib/crm/userContext.tsx | P1 | crmFetch wrapper unconditionally sends spoofable identity headers to every API call | Lines 47-52: ALL client requests include `x-crm-user-id` / `x-crm-user-name`. Any compromised page (XSS, hostile script) can rewrite cachedUser, forge identity. |
| B254 | src/app/crm/settings/page.tsx | P2 | Uses native `prompt()` for setting new user password | Line 68: `prompt(...)` exposes the password to display, no masking, browser-keychain may auto-save the previous prompt text. UX + security. |
| B255 | src/app/crm/settings/page.tsx | P3 | Uses native `alert()` for error display, blocks UI | Lines 60, 79, 94, 105: alerts cannot be styled, look unprofessional, block React render cycle. |
| B256 | src/app/crm/settings/page.tsx | P2 | "Send digest now" button (line 155-157) directly invokes cron path | If cron is broken or rate-limited, alert dies; users spam button → email storm. |
| B257 | engine/app/supabase_client.py | P3 | _DEFAULT_RETRIES=2 means up to 3 attempts with exponential backoff 0.5s/1s/1.5s | Lines 49, 67-69: total max wait ~3s, but if 429 spike persists, no backoff beyond. Should support exponential cap and jitter. |
| B258 | engine/app/bridge_extension.py | P2 | SUPABASE_SERVICE_ROLE_KEY at module load via os.environ.get | Line 55: captured once. Restart needed if env rotated. Same pattern as B051. |
| B259 | src/app/ambient-intent/page.tsx | P3 | Faq item claims "on-device voice activity detection to filter audio before any cloud processing" | Line 31. Engine does cloud processing for analyze/intent (B045 user limit 600/hr/user). Claim potentially misleading if user reads as "audio never reaches cloud". |
| B260 | src/app/pre-orders/agreement/page.tsx | P3 | AGREEMENT_VERSION hardcoded "v2-2026-05-28" — must be manually synced with src/lib/stripe.ts AGREEMENT_VERSION | Line 14 + src/lib/stripe.ts line 40. Two sources of truth. |
| B261 | src/app/pre-orders/purchase/PurchaseForm.tsx | P3 | Client trusts response.data.url and `window.location.href = data.url` | Line 67: if backend ever returned an open-redirect URL, hijack possible. Should validate URL host. |
| B262 | src/lib/crm/google.ts | P3 | CRM_GOOGLE_SCOPES requests calendar.events even though CRM only needs contacts | Lines 18-20. Granted scope is broader than need. Violates principle of least privilege. |
| B263 | extension/agent.js | P3 | Hardcoded MAX_STEPS=60 with comment "10 minute hard limit" (TASK_TIMEOUT_MS=600000) — comment misaligned | Lines 25-26. 60 LLM steps × ~250ms (Cerebras) + DOM actions = realistic ~30-60s. 10min is much higher than what 60 steps consume. Misleading docstring. |
| B264 | engine/app/agent.py | P3 | TIMEOUT_BrowserKillEvent default 60s — engine waits 60s to forcekill broken browser | Line 41. Combined with MAX_SECONDS=300 (B068), a single bad browser action drains 20% of task budget on kill alone. |
| B265 | src/app/api/log/route.ts | P3 | Returns event_id in response but no idempotency check on (agent_name, action, summary) duplicates | Line 73. Two retries of same event create two rows. Webhook caller (twilio status, etc.) can race-double-insert. |
| B266 | src/app/api/dossiers/upsert/route.ts | P2 | Source field hardcoded to "local_engine" | Line 60. No way to distinguish dossier upserts from different engine builds, browser, or extension. |
| B267 | src/lib/crm/auth.ts | P3 | requireCrmGate has no logging of unauthorized attempts | Lines 28-31. No audit trail of failed gate checks. Brute-force visibility = zero. |
| B268 | src/app/crm/PasswordGate.tsx | P0 | Page renders list of all CRM users (id + email) BEFORE login | Lines 21-39: fetch happens on mount, before any auth. Anyone visiting /crm sees all user emails + ids. Combined with B077 + B076 (first-time login claim), trivial account takeover. |
| B269 | src/app/crm/PasswordGate.tsx | P3 | Re-fetches user list on every mount; no caching | Same line 21. Each /crm page load hits /api/crm/users?for=login. Spammable for resource consumption (CPU + DB read). |
| B270 | engine/app/agent.py | P2 | Browser timeout env vars cap at 60s but real-world canvas-heavy apps need 3-5 minutes | Lines 32-43. The TIMEOUT_BrowserStartEvent set to 180s for codespaces. On user's mac with slow disk, Chrome takes 200+ seconds first launch. False timeout. |
| B271 | src/lib/llm-cascade.ts | P2 | Provider health Map is module-level (one process); multi-lambda Vercel = N independent caches | Lines 58-59: same B138 pattern. Each lambda warm-state has separate cooldown clock; provider may be reported down by one but up by another. |
| B272 | src/lib/llm-cascade.ts | P3 | Cascade always tries gemini → groq → mistral → deepseek order; no learning | Lines 119-156: priority static. If gemini is 429 for 5+ minutes (mid-day), every request still tries it first. Should reorder by recent success rate. |
| B273 | src/components/ProductSplit.tsx | P3 | headline prop uses dangerouslySetInnerHTML — accepts ANY HTML | Line 70: `dangerouslySetInnerHTML={{ __html: headline }}`. Currently only used with literal strings, but type is `string` so future user-supplied content goes raw to innerHTML. Defense-in-depth violation. |
| B274 | src/app/layout.tsx | P3 | JSON-LD scripts use dangerouslySetInnerHTML with JSON.stringify — escaping inadequate | Lines 110-123: if the JSON-LD contained `</script>` substring (it doesn't currently, but anyone editing the literal could break), XSS. Should use `JSON.stringify(...).replace(/</g, '\\u003c')`. |
| B275 | src/app/guide/ai-wearables-2026/page.tsx | P3 | Same JSON-LD pattern as B274 | Lines 821, 825. |
| B276 | src/app/vs/limitless/page.tsx | P3 | Same | Line 335. |
| B277 | src/app/vs/friend/page.tsx | P3 | Same | Line 334. |
| B278 | src/app/vs/bee/page.tsx | P3 | Same | Line 354. |
| B279 | src/app/pre-orders/purchase/page.tsx | P3 | Same | Line 53. |
| B280 | engine/app/main.py | P0 | /execute-intent fail-closed when ENGINE_INTERNAL_TOKEN unset — good. But ENGINE_INTERNAL_TOKEN may default to empty in dev | Lines 668-671: 503 if unset, 401 if mismatch. But if dev sets to empty value via `ENGINE_INTERNAL_TOKEN=`, request also matches empty token in mismatch check → bypass. |
| B281 | engine/app/main.py | P2 | /stats endpoint accepts admin token via query param (logged in URL) | Lines 612, 625: `token: str | None = None` query param. URL gets logged in nginx/Vercel access logs. JWT in URL = leaked credentials. |
| B282 | engine/app/code_sandbox.py | P2 | Sandbox falls back to "degraded isolation" when bwrap missing (developer laptops, macOS) | Lines 74-80: degrades silently with warning. Production deploy on macOS (which CLAUDE.md says is the platform) ALWAYS degrades because bwrap is Linux-only. ALL sandbox calls run effectively without isolation on user's Mac. |
| B283 | engine/app/code_sandbox.py | P3 | RLIMIT_NPROC default 32 may be hit on dev machines with many existing procs | Line 58. Per-user cap; engine runs as user already has 30+ procs. Sandbox call fails with EAGAIN. |
| B284 | engine/app/main.py | P3 | _total_tasks / _total_errors counters are module-globals, not per-user | Lines 144-146. Stats endpoint shows aggregate not per-user. Cannot detect single bad actor. |
| B285 | src/lib/preference-record.ts | P2 | callLlm with `max_tokens: 1024` for a one-sentence reasoning — wasteful budget | Line 75: 1024 max_tokens for ~22 word reasoning. Wastes free-tier quota especially on Mistral fallback. |
| B286 | src/lib/intent-extract.ts | P2 | parseJsonWithRepair allowLLMRepair=true triggers another Gemini call on every parse failure | Line 189: if JSON parse fails, tiny Flash repair call. Cost: ~$0.00002 per repair. On bad-output streak (5-10 in a row during a quota throttle), all calls fail repeatedly. |
| B287 | engine/app/proactive_routes.py | P2 | No auth on proactive routes (POST /proactive/chunk, /confirm, /flush, GET /events) | Engine main.py includes proactive_router with no per-route auth. Combined with B086 + B195 CORS open + Private Network Access (B213), hostile browser tab can stream fake transcript chunks into engine. |
| B288 | extension/agent.js | P3 | LESSON_DISTILL_PROMPT_TEMPLATE / REWRITE_PROMPT_TEMPLATE shipped in extension bundle | extension/agent.js + extension/agent-config payload. Prompts exposed to attacker; can craft adversarial inputs that trigger known-prompt outputs. |
| B289 | src/lib/release-meta.ts | P3 | buildManifest imported at module load — if missing, build fails | Line 1: `import buildManifest from "../../state/builds/manifest.json"`. CI/CD must keep manifest fresh; otherwise build breaks. |
| B290 | src/app/api/cron/daily-digest/route.ts | P2 | Authorization grants permission for non-admin CRM users via gate cookie | Lines 30-36: any signed-in CRM user (not just admin) can trigger digest via "Run now" button. Spam vector. |
| B291 | engine/app/main.py | P0 | /admin/trigger-task accepts JWT_SECRET as fallback admin secret | Line 534: `expected = os.environ.get("ADMIN_TRIGGER_SECRET") or os.environ.get("JWT_SECRET") or ""`. JWT_SECRET is used for SESSION token signing (universal); if leaked from any session inspection, attacker can inject arbitrary browser-agent tasks for any user. Combined with B116 (3-day JWT lifetime), risk multiplied. |
| B292 | engine/app/main.py | P2 | /admin/trigger-task spawns task with 240s wall-clock timeout — long-running attack | Lines 567-606: tasks run autonomously for 4 minutes. Attacker with admin secret can chain rapid task triggers for stealth long-running browser actions. |
| B293 | engine/app/main.py | P1 | /auth/me accepts token in query param (URL logged) | Line 500-505: `token: str` as path/query param. Same B281 leak pattern. |
| B294 | engine/app/router.py | P3 | classify() degrades to "ambiguous" on all errors — no telemetry | Lines 56-82: silent fallback. Operator never sees that the LLM cascade is fully down. |
| B295 | engine/app/planner.py | P3 | _ALLOWED_SCHEMES = {"http", "https"} but planner output not validated against this on URL extraction in agent.py | Line 51. Defense in depth, but separate validation in agent.py may diverge. |
| B296 | engine/app/ws_bridge.py | P2 | COMMAND_TIMEOUT_SECONDS = 8.0 — extension might take 10+s for navigate on slow page | Line 56: comment says 30s but value is 8. Mismatch + tight timeout. |
| B297 | engine/app/main.py | P2 | _active_agent_bridges, _active_orchestrator_runners are module-globals | Lines 300-305. Multi-worker engine = workers don't share state. Trigger task to user_id=X works only if X is connected to SAME worker. |
| B298 | engine/app/main.py | P3 | _ws_connection_release clamps to 0 — silent drift | Lines 331-337. If release is called more than admit, true count may already be wrong. |
| B299 | engine/app/main.py | P2 | Per-IP WS cap MAX_WS_CONCURRENT_PER_IP=10 — household NAT users compete | Line 291. Multiple wearers behind one ISP CGNAT share the 10-cap. Family of 5 each opening Chrome popup hits limit. |
| B300 | src/app/api/engine/auto-proceed/route.ts | P2 | recordPreferenceSignal awaited inside auto-proceed loop, adding ~600ms to GET response | Lines 129-167: await blocks user-facing response. Latency tax. Should be background. |
| B301 | engine/app/anticipy/durable.py | P2 | durable.py file exists at engine/app/anticipy/ — separate module per CLAUDE.md flagged "anticipy" subpath | Per ls of anticipy/, there's a parallel module tree. May have its own bugs not in main scan. |
| B302 | extension/manifest.json | P3 | Extension version 7.0.0 but engine references v6 in /api/extension/auth comments | engine/app/extension/auth/route.ts comments mention v7 builds; manifest is 7.0.0. Coordination with build script needed. |
| B303 | src/components/Hero.tsx | P3 | Empty subtitle element rendered as h1 | Lines 47-50: empty `<motion.p>` for subtitle. Empty heading element fails accessibility lint. |
| B304 | engine/app/main.py | P3 | crm_log_event imported but only called for action_engine — partial telemetry | Lines 42, 720-727: log "task_started" but not "task_failed_validation" etc. Audit gaps. |
| B305 | engine/app/main.py | P2 | _CONFIRMATION_EXPIRY_SECONDS = 600 (10 min) but tokens are issued by website route /api/engine/confirm; clock-skew could reject valid tokens | Lines 368-369. If engine and website clocks drift >10s, race window. |
| B306 | engine/app/main.py | P1 | /ws/task WebSocket accepts JWT in query string `?token=...` | Lines 921-923: `websocket.query_params.get("token")`. URL gets logged in proxies/reverse proxies. JWT leaks. Should accept via headers only. |
| B307 | engine/app/main.py | P1 | /ws/agent WebSocket accepts `userId` and `code` in query params | Lines 1233-1238. Access code logged in URL. Should be header-based auth. |
| B308 | engine/app/main.py | P3 | "B287 retracted" — proactive_routes.py DOES enforce Bearer JWT per _user_from_request | B287 in this catalog was incorrect; proactive routes are JWT-authenticated per engine/app/proactive_routes.py L211-220. |
| B309 | engine/app/proactive_routes.py | P2 | Module-level `_user_sessions` dict; multi-worker engine = sessions not shared | Lines 151-152. Different gunicorn workers will fragment session state. ProactiveEngine's conversation context only consistent if routed to same worker. |
| B310 | engine/app/proactive_routes.py | P3 | `_get_or_create_session` has no TTL — sessions accumulate forever in memory | Lines 160-166. Long-running engine = unbounded memory growth as users come and go. |
| B311 | engine/app/proactive_routes.py | P2 | ANTICIPY_USER_PHONE env var falls back to TWILIO_NOTIFY_TO + TWILIO_TEST_TO_REAL_NUMBER_E164 | Lines 96-100: 3 env vars considered for "user's phone". Confusing precedence + test number could leak into prod. |
| B312 | engine/app/main.py | P2 | sanitize_input strips injection patterns AFTER block_reason check | Lines 1012-1013 vs 1019. block_reason runs on raw text, sanitize_input cleans afterward. Injection in benign-looking phrase passes block_reason, then sanitized version goes to LLM with safety patterns stripped. |
| B313 | engine/app/main.py | P3 | _record_task increments AFTER rate_limit check — race condition | Lines 1038-1091. Two simultaneous tasks both pass rate check (both see N-1 < limit) and both call _record_task → N+1. Slight over-spend per user. |
| B314 | engine/app/main.py | P3 | Anonymous WS connections receive client_ip-bucketed rate limit; legitimate browsing user behind shared IP affected | Line 1037 `rate_user = user_id or f"anon:{client_ip}"`. Per-IP for anon users; CGNAT-hostile. |
| B315 | engine/app/main.py | P2 | Client message frame > WS_MAX_MESSAGE_BYTES (8KB) silently dropped; large transcript chunks fail | Line 958: 8KB cap. Real transcripts > 8K chars at long meetings. Engine drops them. |
| B316 | src/lib/intent-prompt.ts | P3 | Extensive 80+ lines of natural-language rules in system prompt — token count high | First 80 lines I saw. Each call burns ~3-5K input tokens for the system prompt. Cached but uncached calls cost real money. |
| B317 | engine/app/anticipy/durable.py | P3 | Module structure suggests "durable" execution but no separate audit | Not yet read in detail. Flagged for follow-up. |
| B318 | engine/app/anticipy/harness.py | P3 | Module structure suggests "harness" with platform-adapter coupling — no separate audit | Flagged for follow-up. |
| B319 | src/components/Privacy.tsx | P0 | "On-Device Only" + "Text Only" + "Never audio. Never a transcript. Never anything you actually said." claims contradict engine reality | Lines 8-19. Engine sends transcript text to Gemini Flash (cloud LLM) in /analyze + /confirm + meta-monitor flows. Marketing claim is materially false. FTC/EU consumer-protection risk if customer complains. |
| B320 | src/components/HowItWorks.tsx | P0 | "Nothing goes to the cloud" claim line 12 | Lines 10-23: "Your phone figures out what matters... and throws away the audio. No transcript, no recording." Then "Our action engine takes it from there." — implies on-device throughout. But engine module clearly uses Cloud LLMs (gemini.ts callGemini, llm-cascade.ts cascading to Cloud APIs). False claim. |
| B321 | src/components/Pricing.tsx | P3 | Comparison "Personal Assistant $45,000/year" vs Anticipy is hypothetical, not citable | Lines 122-138: comparing against a fabricated $45k/yr "personal assistant" without source citation. Marketing puffery, no factual basis. |
| B322 | src/components/Pricing.tsx | P2 | "You spend more on subscriptions you forget to cancel" — no citation | Line 169: stat-like claim. Should cite Truebill / Rocket Money survey or similar. |
| B323 | src/components/Nav.tsx | P3 | Scroll listener attached without throttle/debounce; fires every scroll event (60+/sec) | Lines 19-39: setIsLight + check on each scroll event. Hurts mobile battery + jitter. |
| B324 | src/components/Nav.tsx | P3 | querySelectorAll(".section-cream") runs every scroll event | Line 23: DOM walk per event. Should be cached. |
| B325 | src/app/for/founders/page.tsx | P0 | "Anticipy uses a high confidence threshold for intent detection" claim has no enforced threshold in code | Lines 40-41. Code: intent-extract.ts allows extraction regardless; gates filter post-hoc. No actual "high confidence threshold" pre-filter. Misleading. |
| B326 | src/app/for/founders/page.tsx | P0 | "It does not create transcripts, recordings, or meeting notes" — false | Lines 29-31: anticipy_transcripts table populated by /api/engine/transcribe. anticipy_sessions stored. Audio uploaded to Supabase storage in crm voice flow (B210). Marketing claim contradicts code. |
| B327 | engine/app/cost_watch.py | P2 | Module-level globals + Supabase REST calls without auth header validation | Lines 70-88: anyone reachable via the cost log table can poison it. No write rate-limit at module level. |
| B328 | engine/app/browser.py | P2 | Fernet encrypts entire profile JSON; key rotation invalidates ALL profiles | Lines 32-42: single key encrypts all data. Rotation = mass log-out, no per-record keys. |
| B329 | engine/app/browser.py | P2 | humanize jitter unrealistic; mouse moves to random coordinates without focus context | Lines 47-55: 15% chance of mouse to random (100-800, 100-500) — could click off-screen or trigger unexpected hover. Real users move toward target, not random. |
| B330 | engine/app/code_sandbox.py | P3 | DEFAULT_CPU_SECONDS = 5 but DEFAULT_WALL_TIMEOUT_S = 8 — narrow margin | Lines 54-55: 3s headroom for proc spawn + 5s CPU. Race condition on busy host. |
| B331 | engine/app/code_sandbox.py | P3 | Output truncated AFTER reading all data — large stdout still goes through pipe buffer | Lines 292-293: cap applied post-communicate. 100MB stderr fits in pipe before truncate. |
| B332 | engine/app/dossier/call.py | P3 | Returns "Service not yet available" XML if Twilio inbound hits this endpoint | Lines 21: dead handler. If Twilio webhook configured to hit /dossier/inbound, returns this generic message instead of forwarding. |
| B333 | src/lib/crm/db.ts | P0 | Bucket name `crm-files` exposed via publicFileUrl helper indicates PUBLIC storage | Lines 29-34: `publicFileUrl` returns `${base}/storage/v1/object/public/...`. Anyone with the path scrapes files without auth. Expense receipts + voice memos in B210 leak. |
| B334 | engine/app/anticipy/platform_adapter.py | P0 | Same fake model IDs `deepseek/deepseek-v4-flash` + `moonshotai/kimi-k2.6` in engine | Lines 100-103. Both don't exist on OpenRouter as of catalog. Kimi additionally on v-final-prototype forbidden list (B154). |
| B335 | engine/app/anticipy/platform_adapter.py | P1 | dotenv loaded from ~/.anticipy/.env — single shared file across users | Lines 57-63. Multi-tenant assumption; single .env means no per-user secrets. Combined with B215 `_SESS` global, full single-tenant assumption baked in. |
| B336 | engine/app/anticipy/platform_adapter.py | P2 | model_provisioned() returns true if OPENROUTER_API_KEY starts with "sk-or-" — no validity check | Line 257: prefix match only. Stale revoked key passes check; first model_call fails 401. |
| B337 | engine/app/anticipy/platform_adapter.py | P2 | Pricing dict `_PRICING` hardcoded for two models; cost report wrong for any other model | Lines 131-134. If model changes (planned migration), cost report silently shows $0. |
| B338 | engine/app/anticipy/platform_adapter.py | P2 | `_PROVIDER_ROUTING = {"order": ["deepseek"], "allow_fallbacks": True}` — pinned to first-party provider | Line 126. Single point of failure; if deepseek native goes down for hours, fallbacks kick in but each fallback uses different precision (acknowledged in comment), causing inconsistent results. |
| B339 | src/lib/crm/google.ts | P1 | refreshAccessToken reuses old refresh_token from local even if Google rotated one | Lines 92-110: ignores `j.refresh_token` from response. Same bug as B203 in google-calendar.ts. |
| B340 | src/lib/voyage.ts | P3 | voyageAvailable() returns true if GOOGLE_API_KEY present even without VOYAGE_API_KEY | Lines 28-31: misleading function name. Should be `embedAvailable()`. |
| B341 | src/lib/voyage.ts | P3 | GEMINI_EMBED_URL hardcoded; doesn't fall back to v1 endpoint if v1beta deprecated | Line 24-25. |
| B342 | engine/app/anticipy/platform_adapter.py | P2 | Anticipy broker token sent as `Authorization: Bearer <broker_token>` over HTTPS | Line 397. If MITM somehow happens (compromised cert), token leaks. Should also use HMAC + timestamp for replay defense. |
| B343 | engine/app/anticipy/harness.py | P2 | _MAX_WORKERS = 24 thread pool — without GIL release, model_call requests serialize | Line 28: ThreadPoolExecutor with 24 workers, but requests library uses urllib3 connection pool default 10. Bottleneck at 10; rest queue. |
| B344 | engine/app/proactive_routes.py | P3 | `_get_or_create_session` does not lock — race creates 2 sessions for same user | Lines 160-166: read-write race. Two parallel requests for new user both see `None`, both call `_make_user_session`, second one overwrites first; first request's state lost. |
| B345 | engine/app/proactive_routes.py | P3 | UserSession.events list O(n) per event; trim is O(n) | Lines 132-147. Better data structure: deque(maxlen=EVENT_CAP). |
| B346 | src/components/Hero.tsx | P3 | "By Anticipation Labs" tag in hero exposes legal entity to bots/SEO indexers | Line 35: SEO-indexable corporate name. Combined with funded page (B169) raise terms public, doxxes corporate structure. |
| B347 | src/lib/episode-recall.ts | P3 | TERMINAL_STATUSES list includes "failed" but failed embeddings poison the corpus | Lines 32-38. Including failed intents in vector recall means future intent extraction may follow patterns that previously failed. Negative training signal. |
| B348 | src/lib/meta-monitor.ts | P3 | recallUserProfile returns "" when signal_count<3 — silent omission | Line 116: no telemetry that profile was skipped. Operator can't see how often early-stage users miss the recall. |
| B349 | src/lib/intent-prompt.ts | P3 | System prompt very long; gemini's cache TTL is 5 min, full cost on first call | Per B205 comment. |
| B350 | src/app/api/crm/contacts/import/outreach/route.ts | P2 | Iterates all unique recipients in for-loop — N round-trips to Supabase | Lines 54-88: each contact = one INSERT. 1000 contacts = 1000 requests. Should be bulk insert. |
