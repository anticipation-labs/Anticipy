# dependencies
/node_modules
**/node_modules/
.pnp
.pnp.js

# next.js
/.next/
/out/
/build
/dist

# vercel
.vercel

# local env files — NEVER COMMIT SECRETS
.env
.env.local
.env*.local
.env.development.local
.env.production.local

# claude / mcp config (may contain tokens)
.mcp.json
.claude/

# logs / debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
.playwright-mcp/
logs/trace/
logs/last_realday.json
logs/codex/build_*.jsonl
logs/codex/build_*.last.txt
logs/codex/judge_*.jsonl
logs/codex/judge_*.last.txt
logs/verdicts/**/*.json
logs/verdicts/**/*.jsonl
logs/verdicts/**/*.tsv
logs/verdicts/**/*.csv
logs/verdicts/**/*_content.txt
*.transcript
*.transcript.*
*.transcript.json
*.transcript.jsonl
*transcript*.json
*transcript*.jsonl

# local realday audio. Keep it on this Mac, not in git.
realdays/raw/*.mp3
realdays/marked/*.mp3
realdays/holdout/**

# os / editor
.DS_Store
*.pem
.idea/
.vscode/

# python (engine)
.venv/
**/.venv/
# durable browser-use bridge venv (3.11; runs the open-source browser arm as a
# separate process the 3.10 engine shells out to). NEVER commit it.
engine/.bu-venv/
**/.bu-venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# swift / macapp build artifacts
.build/
macapp/.build/
*.xcodeproj/xcuserdata/
DerivedData/

# local engine data stores (memory drawers live here in later rooms)
.anticipy-data/

# ---- Factory (successor loop) ----
logs/factory/laps/
logs/factory/runs/
logs/factory/ESCALATIONS/
logs/factory/spend.csv
logs/factory/launchd.*.log
logs/factory/aborted/
factory/.lock/
factory/.halt
factory/config/owner_literals.local.txt
# holdout personas are judge-only, local-only, never in git
factory/personas/holdout/**
logs/factory/loop_journal.md
DONE_CERTIFICATION_BUNDLE/

# session junk / heavy backups
engine/.venv.bak.310/
nohup.out
app/*.bak.*
.anticipy-data-hoe/
