# THE PLAN — INSTALL THE TOILET AND FLUSH IT (every real task, end-to-end)

> Omar's words: "You keep plumbing and break the pipes, plumb again, break the pipes. You never install
> the toilet and flush it once." A **FLUSH** = a COMPLETE real-world outcome you can SEE, verified by
> reading the real artifact back. Deciding correctly is NOT a flush. A partial is NOT a flush. The
> plumbing is fine — STOP touching it except to make a flush actually complete. Drive every canonical
> task to a verified flush, twice. Then Omar's real days confirm §4. That is fully done.

## 1. WHERE WE ARE (honest, 2026-06-25)
- DECISIONS across a full messy day: ✅ works (vents ignored, money held, sends drafted, web auto) — proven.
- READ flushes: ✅ real answers delivered (Eiffel "330 m", Costco "$65/yr", competitor pricing, Gmail read).
- **WRITE flushes: ❌ NEVER FLUSHED.** The draft attempt created NO draft (agent bailed in Gmail compose;
  read-back caught the judge falsely claiming success). A calendar event was never even attempted.
  → The toilet has never been flushed on anything that CHANGES Omar's world. That is the gap. Everything else is built.

## 2. THE CANONICAL FLUSHES (each = drive to completion, then VERIFY by reading the real artifact back)
| # | Flush | Verify by | Status |
|---|---|---|---|
| F1 | "look up X" → real answer | the answer is real + correct | ✅ |
| F2 | "research X" → real summary | the summary is real | ✅ |
| F3 | **"draft an email to X" → a real DRAFT in Gmail Drafts (never sent)** | read Drafts back: the draft is there; Sent has nothing | ❌ → DO |
| F4 | **"put X on my calendar" → a real EVENT on Google Calendar** | read Calendar back: the event is there | ❌ → DO |
| F5 | **multi-step: "find an Italian place Thurs + put it on my calendar"** | read Calendar back: event with the place | ❌ → DO |
| F6 | money → held + warm ask; vent → silent; irreversible → ask | the card disposition | ✅ |
| F7 | a FULL realistic day → every line right AND every doable one actually flushed | run it + read artifacts back | ⏳ |

## 3. THE RULE (so a flush is never faked)
A flush counts ONLY when the REAL artifact is read back and confirmed (the draft IS in Drafts, the event
IS on Calendar). Never the agent's say-so, never the judge alone — the judge already false-claimed a draft
that didn't exist. Read the real world back, every time.

## 4. ALL-DONE GATE
F3, F4, F5 each flush + verify **twice**; F7 (a full day) runs clean **twice**; suite stays 109/0
throughout (REVERT anything that regresses it). Then it is robust → Omar's real days are the §4 stamp.

## 5. THE LOOP
Every 3 min: wake, check the lowest un-flushed F#, drive it to a verified real-world flush, commit it,
update LEDGER, reschedule. Fix only what's needed to COMPLETE a flush — never re-plumb working pipes,
never refactor infra, never break what flushes. Try and fail loudly over not trying. Don't stop until
the gate is green.
